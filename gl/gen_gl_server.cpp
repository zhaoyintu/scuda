/* AUTO-GENERATED by gl_codegen.py — DO NOT EDIT */
#include <GL/gl.h>
#include <string.h>
#include <stdint.h>
#include "gen_gl_api.h"
#include "gen_gl_server.h"
#include "rpc.h"

/* Forward declarations for manual handlers */
int handle_glTexParameterfv_manual(conn_t *conn);
int handle_glTexParameteriv_manual(conn_t *conn);
int handle_glTexImage1D_manual(conn_t *conn);
int handle_glTexImage2D_manual(conn_t *conn);
int handle_glReadPixels_manual(conn_t *conn);
int handle_glGetBooleanv_manual(conn_t *conn);
int handle_glGetDoublev_manual(conn_t *conn);
int handle_glGetFloatv_manual(conn_t *conn);
int handle_glGetIntegerv_manual(conn_t *conn);
int handle_glGetString_manual(conn_t *conn);
int handle_glGetTexImage_manual(conn_t *conn);
int handle_glGetTexParameterfv_manual(conn_t *conn);
int handle_glGetTexParameteriv_manual(conn_t *conn);
int handle_glGetTexLevelParameterfv_manual(conn_t *conn);
int handle_glGetTexLevelParameteriv_manual(conn_t *conn);
int handle_glCallLists_manual(conn_t *conn);
int handle_glBitmap_manual(conn_t *conn);
int handle_glColor3bv_manual(conn_t *conn);
int handle_glColor3dv_manual(conn_t *conn);
int handle_glColor3fv_manual(conn_t *conn);
int handle_glColor3iv_manual(conn_t *conn);
int handle_glColor3sv_manual(conn_t *conn);
int handle_glColor3ubv_manual(conn_t *conn);
int handle_glColor3uiv_manual(conn_t *conn);
int handle_glColor3usv_manual(conn_t *conn);
int handle_glColor4bv_manual(conn_t *conn);
int handle_glColor4dv_manual(conn_t *conn);
int handle_glColor4fv_manual(conn_t *conn);
int handle_glColor4iv_manual(conn_t *conn);
int handle_glColor4sv_manual(conn_t *conn);
int handle_glColor4ubv_manual(conn_t *conn);
int handle_glColor4uiv_manual(conn_t *conn);
int handle_glColor4usv_manual(conn_t *conn);
int handle_glEdgeFlagv_manual(conn_t *conn);
int handle_glIndexdv_manual(conn_t *conn);
int handle_glIndexfv_manual(conn_t *conn);
int handle_glIndexiv_manual(conn_t *conn);
int handle_glIndexsv_manual(conn_t *conn);
int handle_glNormal3bv_manual(conn_t *conn);
int handle_glNormal3dv_manual(conn_t *conn);
int handle_glNormal3fv_manual(conn_t *conn);
int handle_glNormal3iv_manual(conn_t *conn);
int handle_glNormal3sv_manual(conn_t *conn);
int handle_glRasterPos2dv_manual(conn_t *conn);
int handle_glRasterPos2fv_manual(conn_t *conn);
int handle_glRasterPos2iv_manual(conn_t *conn);
int handle_glRasterPos2sv_manual(conn_t *conn);
int handle_glRasterPos3dv_manual(conn_t *conn);
int handle_glRasterPos3fv_manual(conn_t *conn);
int handle_glRasterPos3iv_manual(conn_t *conn);
int handle_glRasterPos3sv_manual(conn_t *conn);
int handle_glRasterPos4dv_manual(conn_t *conn);
int handle_glRasterPos4fv_manual(conn_t *conn);
int handle_glRasterPos4iv_manual(conn_t *conn);
int handle_glRasterPos4sv_manual(conn_t *conn);
int handle_glRectdv_manual(conn_t *conn);
int handle_glRectfv_manual(conn_t *conn);
int handle_glRectiv_manual(conn_t *conn);
int handle_glRectsv_manual(conn_t *conn);
int handle_glTexCoord1dv_manual(conn_t *conn);
int handle_glTexCoord1fv_manual(conn_t *conn);
int handle_glTexCoord1iv_manual(conn_t *conn);
int handle_glTexCoord1sv_manual(conn_t *conn);
int handle_glTexCoord2dv_manual(conn_t *conn);
int handle_glTexCoord2fv_manual(conn_t *conn);
int handle_glTexCoord2iv_manual(conn_t *conn);
int handle_glTexCoord2sv_manual(conn_t *conn);
int handle_glTexCoord3dv_manual(conn_t *conn);
int handle_glTexCoord3fv_manual(conn_t *conn);
int handle_glTexCoord3iv_manual(conn_t *conn);
int handle_glTexCoord3sv_manual(conn_t *conn);
int handle_glTexCoord4dv_manual(conn_t *conn);
int handle_glTexCoord4fv_manual(conn_t *conn);
int handle_glTexCoord4iv_manual(conn_t *conn);
int handle_glTexCoord4sv_manual(conn_t *conn);
int handle_glVertex2dv_manual(conn_t *conn);
int handle_glVertex2fv_manual(conn_t *conn);
int handle_glVertex2iv_manual(conn_t *conn);
int handle_glVertex2sv_manual(conn_t *conn);
int handle_glVertex3dv_manual(conn_t *conn);
int handle_glVertex3fv_manual(conn_t *conn);
int handle_glVertex3iv_manual(conn_t *conn);
int handle_glVertex3sv_manual(conn_t *conn);
int handle_glVertex4dv_manual(conn_t *conn);
int handle_glVertex4fv_manual(conn_t *conn);
int handle_glVertex4iv_manual(conn_t *conn);
int handle_glVertex4sv_manual(conn_t *conn);
int handle_glClipPlane_manual(conn_t *conn);
int handle_glFogfv_manual(conn_t *conn);
int handle_glFogiv_manual(conn_t *conn);
int handle_glLightfv_manual(conn_t *conn);
int handle_glLightiv_manual(conn_t *conn);
int handle_glLightModelfv_manual(conn_t *conn);
int handle_glLightModeliv_manual(conn_t *conn);
int handle_glMaterialfv_manual(conn_t *conn);
int handle_glMaterialiv_manual(conn_t *conn);
int handle_glPolygonStipple_manual(conn_t *conn);
int handle_glTexEnvfv_manual(conn_t *conn);
int handle_glTexEnviv_manual(conn_t *conn);
int handle_glTexGendv_manual(conn_t *conn);
int handle_glTexGenfv_manual(conn_t *conn);
int handle_glTexGeniv_manual(conn_t *conn);
int handle_glFeedbackBuffer_manual(conn_t *conn);
int handle_glSelectBuffer_manual(conn_t *conn);
int handle_glMap1d_manual(conn_t *conn);
int handle_glMap1f_manual(conn_t *conn);
int handle_glMap2d_manual(conn_t *conn);
int handle_glMap2f_manual(conn_t *conn);
int handle_glEvalCoord1dv_manual(conn_t *conn);
int handle_glEvalCoord1fv_manual(conn_t *conn);
int handle_glEvalCoord2dv_manual(conn_t *conn);
int handle_glEvalCoord2fv_manual(conn_t *conn);
int handle_glPixelMapfv_manual(conn_t *conn);
int handle_glPixelMapuiv_manual(conn_t *conn);
int handle_glPixelMapusv_manual(conn_t *conn);
int handle_glDrawPixels_manual(conn_t *conn);
int handle_glGetClipPlane_manual(conn_t *conn);
int handle_glGetLightfv_manual(conn_t *conn);
int handle_glGetLightiv_manual(conn_t *conn);
int handle_glGetMapdv_manual(conn_t *conn);
int handle_glGetMapfv_manual(conn_t *conn);
int handle_glGetMapiv_manual(conn_t *conn);
int handle_glGetMaterialfv_manual(conn_t *conn);
int handle_glGetMaterialiv_manual(conn_t *conn);
int handle_glGetPixelMapfv_manual(conn_t *conn);
int handle_glGetPixelMapuiv_manual(conn_t *conn);
int handle_glGetPixelMapusv_manual(conn_t *conn);
int handle_glGetPolygonStipple_manual(conn_t *conn);
int handle_glGetTexEnvfv_manual(conn_t *conn);
int handle_glGetTexEnviv_manual(conn_t *conn);
int handle_glGetTexGendv_manual(conn_t *conn);
int handle_glGetTexGenfv_manual(conn_t *conn);
int handle_glGetTexGeniv_manual(conn_t *conn);
int handle_glLoadMatrixf_manual(conn_t *conn);
int handle_glLoadMatrixd_manual(conn_t *conn);
int handle_glMultMatrixf_manual(conn_t *conn);
int handle_glMultMatrixd_manual(conn_t *conn);
int handle_glDrawElements_manual(conn_t *conn);
int handle_glGetPointerv_manual(conn_t *conn);
int handle_glCopyTexImage1D_manual(conn_t *conn);
int handle_glCopyTexImage2D_manual(conn_t *conn);
int handle_glTexSubImage1D_manual(conn_t *conn);
int handle_glTexSubImage2D_manual(conn_t *conn);
int handle_glDeleteTextures_manual(conn_t *conn);
int handle_glGenTextures_manual(conn_t *conn);
int handle_glColorPointer_manual(conn_t *conn);
int handle_glEdgeFlagPointer_manual(conn_t *conn);
int handle_glIndexPointer_manual(conn_t *conn);
int handle_glInterleavedArrays_manual(conn_t *conn);
int handle_glNormalPointer_manual(conn_t *conn);
int handle_glTexCoordPointer_manual(conn_t *conn);
int handle_glVertexPointer_manual(conn_t *conn);
int handle_glAreTexturesResident_manual(conn_t *conn);
int handle_glPrioritizeTextures_manual(conn_t *conn);
int handle_glIndexubv_manual(conn_t *conn);
int handle_glDrawRangeElements_manual(conn_t *conn);
int handle_glTexImage3D_manual(conn_t *conn);
int handle_glTexSubImage3D_manual(conn_t *conn);
int handle_glCompressedTexImage3D_manual(conn_t *conn);
int handle_glCompressedTexImage2D_manual(conn_t *conn);
int handle_glCompressedTexImage1D_manual(conn_t *conn);
int handle_glCompressedTexSubImage3D_manual(conn_t *conn);
int handle_glCompressedTexSubImage2D_manual(conn_t *conn);
int handle_glCompressedTexSubImage1D_manual(conn_t *conn);
int handle_glGetCompressedTexImage_manual(conn_t *conn);
int handle_glMultiTexCoord1dv_manual(conn_t *conn);
int handle_glMultiTexCoord1fv_manual(conn_t *conn);
int handle_glMultiTexCoord1iv_manual(conn_t *conn);
int handle_glMultiTexCoord1sv_manual(conn_t *conn);
int handle_glMultiTexCoord2dv_manual(conn_t *conn);
int handle_glMultiTexCoord2fv_manual(conn_t *conn);
int handle_glMultiTexCoord2iv_manual(conn_t *conn);
int handle_glMultiTexCoord2sv_manual(conn_t *conn);
int handle_glMultiTexCoord3dv_manual(conn_t *conn);
int handle_glMultiTexCoord3fv_manual(conn_t *conn);
int handle_glMultiTexCoord3iv_manual(conn_t *conn);
int handle_glMultiTexCoord3sv_manual(conn_t *conn);
int handle_glMultiTexCoord4dv_manual(conn_t *conn);
int handle_glMultiTexCoord4fv_manual(conn_t *conn);
int handle_glMultiTexCoord4iv_manual(conn_t *conn);
int handle_glMultiTexCoord4sv_manual(conn_t *conn);
int handle_glLoadTransposeMatrixf_manual(conn_t *conn);
int handle_glLoadTransposeMatrixd_manual(conn_t *conn);
int handle_glMultTransposeMatrixf_manual(conn_t *conn);
int handle_glMultTransposeMatrixd_manual(conn_t *conn);
int handle_glMultiDrawArrays_manual(conn_t *conn);
int handle_glMultiDrawElements_manual(conn_t *conn);
int handle_glPointParameterfv_manual(conn_t *conn);
int handle_glPointParameteriv_manual(conn_t *conn);
int handle_glFogCoordfv_manual(conn_t *conn);
int handle_glFogCoorddv_manual(conn_t *conn);
int handle_glFogCoordPointer_manual(conn_t *conn);
int handle_glSecondaryColor3bv_manual(conn_t *conn);
int handle_glSecondaryColor3dv_manual(conn_t *conn);
int handle_glSecondaryColor3fv_manual(conn_t *conn);
int handle_glSecondaryColor3iv_manual(conn_t *conn);
int handle_glSecondaryColor3sv_manual(conn_t *conn);
int handle_glSecondaryColor3ubv_manual(conn_t *conn);
int handle_glSecondaryColor3uiv_manual(conn_t *conn);
int handle_glSecondaryColor3usv_manual(conn_t *conn);
int handle_glSecondaryColorPointer_manual(conn_t *conn);
int handle_glWindowPos2dv_manual(conn_t *conn);
int handle_glWindowPos2fv_manual(conn_t *conn);
int handle_glWindowPos2iv_manual(conn_t *conn);
int handle_glWindowPos2sv_manual(conn_t *conn);
int handle_glWindowPos3dv_manual(conn_t *conn);
int handle_glWindowPos3fv_manual(conn_t *conn);
int handle_glWindowPos3iv_manual(conn_t *conn);
int handle_glWindowPos3sv_manual(conn_t *conn);
int handle_glGenQueries_manual(conn_t *conn);
int handle_glDeleteQueries_manual(conn_t *conn);
int handle_glGetQueryiv_manual(conn_t *conn);
int handle_glGetQueryObjectiv_manual(conn_t *conn);
int handle_glGetQueryObjectuiv_manual(conn_t *conn);
int handle_glDeleteBuffers_manual(conn_t *conn);
int handle_glGenBuffers_manual(conn_t *conn);
int handle_glBufferData_manual(conn_t *conn);
int handle_glBufferSubData_manual(conn_t *conn);
int handle_glGetBufferSubData_manual(conn_t *conn);
int handle_glMapBuffer_manual(conn_t *conn);
int handle_glUnmapBuffer_manual(conn_t *conn);
int handle_glGetBufferParameteriv_manual(conn_t *conn);
int handle_glGetBufferPointerv_manual(conn_t *conn);
int handle_glDrawBuffers_manual(conn_t *conn);
int handle_glBindAttribLocation_manual(conn_t *conn);
int handle_glGetActiveAttrib_manual(conn_t *conn);
int handle_glGetActiveUniform_manual(conn_t *conn);
int handle_glGetAttachedShaders_manual(conn_t *conn);
int handle_glGetAttribLocation_manual(conn_t *conn);
int handle_glGetProgramiv_manual(conn_t *conn);
int handle_glGetProgramInfoLog_manual(conn_t *conn);
int handle_glGetShaderiv_manual(conn_t *conn);
int handle_glGetShaderInfoLog_manual(conn_t *conn);
int handle_glGetShaderSource_manual(conn_t *conn);
int handle_glGetUniformLocation_manual(conn_t *conn);
int handle_glGetUniformfv_manual(conn_t *conn);
int handle_glGetUniformiv_manual(conn_t *conn);
int handle_glGetVertexAttribdv_manual(conn_t *conn);
int handle_glGetVertexAttribfv_manual(conn_t *conn);
int handle_glGetVertexAttribiv_manual(conn_t *conn);
int handle_glGetVertexAttribPointerv_manual(conn_t *conn);
int handle_glShaderSource_manual(conn_t *conn);
int handle_glUniform1fv_manual(conn_t *conn);
int handle_glUniform2fv_manual(conn_t *conn);
int handle_glUniform3fv_manual(conn_t *conn);
int handle_glUniform4fv_manual(conn_t *conn);
int handle_glUniform1iv_manual(conn_t *conn);
int handle_glUniform2iv_manual(conn_t *conn);
int handle_glUniform3iv_manual(conn_t *conn);
int handle_glUniform4iv_manual(conn_t *conn);
int handle_glUniformMatrix2fv_manual(conn_t *conn);
int handle_glUniformMatrix3fv_manual(conn_t *conn);
int handle_glUniformMatrix4fv_manual(conn_t *conn);
int handle_glVertexAttrib1dv_manual(conn_t *conn);
int handle_glVertexAttrib1fv_manual(conn_t *conn);
int handle_glVertexAttrib1sv_manual(conn_t *conn);
int handle_glVertexAttrib2dv_manual(conn_t *conn);
int handle_glVertexAttrib2fv_manual(conn_t *conn);
int handle_glVertexAttrib2sv_manual(conn_t *conn);
int handle_glVertexAttrib3dv_manual(conn_t *conn);
int handle_glVertexAttrib3fv_manual(conn_t *conn);
int handle_glVertexAttrib3sv_manual(conn_t *conn);
int handle_glVertexAttrib4Nbv_manual(conn_t *conn);
int handle_glVertexAttrib4Niv_manual(conn_t *conn);
int handle_glVertexAttrib4Nsv_manual(conn_t *conn);
int handle_glVertexAttrib4Nubv_manual(conn_t *conn);
int handle_glVertexAttrib4Nuiv_manual(conn_t *conn);
int handle_glVertexAttrib4Nusv_manual(conn_t *conn);
int handle_glVertexAttrib4bv_manual(conn_t *conn);
int handle_glVertexAttrib4dv_manual(conn_t *conn);
int handle_glVertexAttrib4fv_manual(conn_t *conn);
int handle_glVertexAttrib4iv_manual(conn_t *conn);
int handle_glVertexAttrib4sv_manual(conn_t *conn);
int handle_glVertexAttrib4ubv_manual(conn_t *conn);
int handle_glVertexAttrib4uiv_manual(conn_t *conn);
int handle_glVertexAttrib4usv_manual(conn_t *conn);
int handle_glVertexAttribPointer_manual(conn_t *conn);
int handle_glUniformMatrix2x3fv_manual(conn_t *conn);
int handle_glUniformMatrix3x2fv_manual(conn_t *conn);
int handle_glUniformMatrix2x4fv_manual(conn_t *conn);
int handle_glUniformMatrix4x2fv_manual(conn_t *conn);
int handle_glUniformMatrix3x4fv_manual(conn_t *conn);
int handle_glUniformMatrix4x3fv_manual(conn_t *conn);
int handle_glGetBooleani_v_manual(conn_t *conn);
int handle_glGetIntegeri_v_manual(conn_t *conn);
int handle_glTransformFeedbackVaryings_manual(conn_t *conn);
int handle_glGetTransformFeedbackVarying_manual(conn_t *conn);
int handle_glVertexAttribIPointer_manual(conn_t *conn);
int handle_glGetVertexAttribIiv_manual(conn_t *conn);
int handle_glGetVertexAttribIuiv_manual(conn_t *conn);
int handle_glVertexAttribI1iv_manual(conn_t *conn);
int handle_glVertexAttribI2iv_manual(conn_t *conn);
int handle_glVertexAttribI3iv_manual(conn_t *conn);
int handle_glVertexAttribI4iv_manual(conn_t *conn);
int handle_glVertexAttribI1uiv_manual(conn_t *conn);
int handle_glVertexAttribI2uiv_manual(conn_t *conn);
int handle_glVertexAttribI3uiv_manual(conn_t *conn);
int handle_glVertexAttribI4uiv_manual(conn_t *conn);
int handle_glVertexAttribI4bv_manual(conn_t *conn);
int handle_glVertexAttribI4sv_manual(conn_t *conn);
int handle_glVertexAttribI4ubv_manual(conn_t *conn);
int handle_glVertexAttribI4usv_manual(conn_t *conn);
int handle_glGetUniformuiv_manual(conn_t *conn);
int handle_glBindFragDataLocation_manual(conn_t *conn);
int handle_glGetFragDataLocation_manual(conn_t *conn);
int handle_glUniform1uiv_manual(conn_t *conn);
int handle_glUniform2uiv_manual(conn_t *conn);
int handle_glUniform3uiv_manual(conn_t *conn);
int handle_glUniform4uiv_manual(conn_t *conn);
int handle_glTexParameterIiv_manual(conn_t *conn);
int handle_glTexParameterIuiv_manual(conn_t *conn);
int handle_glGetTexParameterIiv_manual(conn_t *conn);
int handle_glGetTexParameterIuiv_manual(conn_t *conn);
int handle_glClearBufferiv_manual(conn_t *conn);
int handle_glClearBufferuiv_manual(conn_t *conn);
int handle_glClearBufferfv_manual(conn_t *conn);
int handle_glGetStringi_manual(conn_t *conn);
int handle_glDeleteRenderbuffers_manual(conn_t *conn);
int handle_glGenRenderbuffers_manual(conn_t *conn);
int handle_glGetRenderbufferParameteriv_manual(conn_t *conn);
int handle_glDeleteFramebuffers_manual(conn_t *conn);
int handle_glGenFramebuffers_manual(conn_t *conn);
int handle_glGetFramebufferAttachmentParameteriv_manual(conn_t *conn);
int handle_glMapBufferRange_manual(conn_t *conn);
int handle_glDeleteVertexArrays_manual(conn_t *conn);
int handle_glGenVertexArrays_manual(conn_t *conn);
int handle_glDrawElementsInstanced_manual(conn_t *conn);
int handle_glGetUniformIndices_manual(conn_t *conn);
int handle_glGetActiveUniformsiv_manual(conn_t *conn);
int handle_glGetActiveUniformName_manual(conn_t *conn);
int handle_glGetUniformBlockIndex_manual(conn_t *conn);
int handle_glGetActiveUniformBlockiv_manual(conn_t *conn);
int handle_glGetActiveUniformBlockName_manual(conn_t *conn);
int handle_glDrawElementsBaseVertex_manual(conn_t *conn);
int handle_glDrawRangeElementsBaseVertex_manual(conn_t *conn);
int handle_glDrawElementsInstancedBaseVertex_manual(conn_t *conn);
int handle_glMultiDrawElementsBaseVertex_manual(conn_t *conn);
int handle_glFenceSync_manual(conn_t *conn);
int handle_glIsSync_manual(conn_t *conn);
int handle_glDeleteSync_manual(conn_t *conn);
int handle_glClientWaitSync_manual(conn_t *conn);
int handle_glWaitSync_manual(conn_t *conn);
int handle_glGetInteger64v_manual(conn_t *conn);
int handle_glGetSynciv_manual(conn_t *conn);
int handle_glGetInteger64i_v_manual(conn_t *conn);
int handle_glGetBufferParameteri64v_manual(conn_t *conn);
int handle_glGetMultisamplefv_manual(conn_t *conn);
int handle_glBindFragDataLocationIndexed_manual(conn_t *conn);
int handle_glGetFragDataIndex_manual(conn_t *conn);
int handle_glGenSamplers_manual(conn_t *conn);
int handle_glDeleteSamplers_manual(conn_t *conn);
int handle_glSamplerParameteriv_manual(conn_t *conn);
int handle_glSamplerParameterfv_manual(conn_t *conn);
int handle_glSamplerParameterIiv_manual(conn_t *conn);
int handle_glSamplerParameterIuiv_manual(conn_t *conn);
int handle_glGetSamplerParameteriv_manual(conn_t *conn);
int handle_glGetSamplerParameterIiv_manual(conn_t *conn);
int handle_glGetSamplerParameterfv_manual(conn_t *conn);
int handle_glGetSamplerParameterIuiv_manual(conn_t *conn);
int handle_glGetQueryObjecti64v_manual(conn_t *conn);
int handle_glGetQueryObjectui64v_manual(conn_t *conn);
int handle_glVertexAttribP1uiv_manual(conn_t *conn);
int handle_glVertexAttribP2uiv_manual(conn_t *conn);
int handle_glVertexAttribP3uiv_manual(conn_t *conn);
int handle_glVertexAttribP4uiv_manual(conn_t *conn);
int handle_glVertexP2uiv_manual(conn_t *conn);
int handle_glVertexP3uiv_manual(conn_t *conn);
int handle_glVertexP4uiv_manual(conn_t *conn);
int handle_glTexCoordP1uiv_manual(conn_t *conn);
int handle_glTexCoordP2uiv_manual(conn_t *conn);
int handle_glTexCoordP3uiv_manual(conn_t *conn);
int handle_glTexCoordP4uiv_manual(conn_t *conn);
int handle_glMultiTexCoordP1uiv_manual(conn_t *conn);
int handle_glMultiTexCoordP2uiv_manual(conn_t *conn);
int handle_glMultiTexCoordP3uiv_manual(conn_t *conn);
int handle_glMultiTexCoordP4uiv_manual(conn_t *conn);
int handle_glNormalP3uiv_manual(conn_t *conn);
int handle_glColorP3uiv_manual(conn_t *conn);
int handle_glColorP4uiv_manual(conn_t *conn);
int handle_glSecondaryColorP3uiv_manual(conn_t *conn);
int handle_glDrawArraysIndirect_manual(conn_t *conn);
int handle_glDrawElementsIndirect_manual(conn_t *conn);
int handle_glUniform1dv_manual(conn_t *conn);
int handle_glUniform2dv_manual(conn_t *conn);
int handle_glUniform3dv_manual(conn_t *conn);
int handle_glUniform4dv_manual(conn_t *conn);
int handle_glUniformMatrix2dv_manual(conn_t *conn);
int handle_glUniformMatrix3dv_manual(conn_t *conn);
int handle_glUniformMatrix4dv_manual(conn_t *conn);
int handle_glUniformMatrix2x3dv_manual(conn_t *conn);
int handle_glUniformMatrix2x4dv_manual(conn_t *conn);
int handle_glUniformMatrix3x2dv_manual(conn_t *conn);
int handle_glUniformMatrix3x4dv_manual(conn_t *conn);
int handle_glUniformMatrix4x2dv_manual(conn_t *conn);
int handle_glUniformMatrix4x3dv_manual(conn_t *conn);
int handle_glGetUniformdv_manual(conn_t *conn);
int handle_glGetSubroutineUniformLocation_manual(conn_t *conn);
int handle_glGetSubroutineIndex_manual(conn_t *conn);
int handle_glGetActiveSubroutineUniformiv_manual(conn_t *conn);
int handle_glGetActiveSubroutineUniformName_manual(conn_t *conn);
int handle_glGetActiveSubroutineName_manual(conn_t *conn);
int handle_glUniformSubroutinesuiv_manual(conn_t *conn);
int handle_glGetUniformSubroutineuiv_manual(conn_t *conn);
int handle_glGetProgramStageiv_manual(conn_t *conn);
int handle_glPatchParameterfv_manual(conn_t *conn);
int handle_glDeleteTransformFeedbacks_manual(conn_t *conn);
int handle_glGenTransformFeedbacks_manual(conn_t *conn);
int handle_glGetQueryIndexediv_manual(conn_t *conn);
int handle_glShaderBinary_manual(conn_t *conn);
int handle_glGetShaderPrecisionFormat_manual(conn_t *conn);
int handle_glGetProgramBinary_manual(conn_t *conn);
int handle_glProgramBinary_manual(conn_t *conn);
int handle_glCreateShaderProgramv_manual(conn_t *conn);
int handle_glDeleteProgramPipelines_manual(conn_t *conn);
int handle_glGenProgramPipelines_manual(conn_t *conn);
int handle_glGetProgramPipelineiv_manual(conn_t *conn);
int handle_glProgramUniform1iv_manual(conn_t *conn);
int handle_glProgramUniform1fv_manual(conn_t *conn);
int handle_glProgramUniform1dv_manual(conn_t *conn);
int handle_glProgramUniform1uiv_manual(conn_t *conn);
int handle_glProgramUniform2iv_manual(conn_t *conn);
int handle_glProgramUniform2fv_manual(conn_t *conn);
int handle_glProgramUniform2dv_manual(conn_t *conn);
int handle_glProgramUniform2uiv_manual(conn_t *conn);
int handle_glProgramUniform3iv_manual(conn_t *conn);
int handle_glProgramUniform3fv_manual(conn_t *conn);
int handle_glProgramUniform3dv_manual(conn_t *conn);
int handle_glProgramUniform3uiv_manual(conn_t *conn);
int handle_glProgramUniform4iv_manual(conn_t *conn);
int handle_glProgramUniform4fv_manual(conn_t *conn);
int handle_glProgramUniform4dv_manual(conn_t *conn);
int handle_glProgramUniform4uiv_manual(conn_t *conn);
int handle_glProgramUniformMatrix2fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix3fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix4fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix2dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix3dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix4dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix2x3fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix3x2fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix2x4fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix4x2fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix3x4fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix4x3fv_manual(conn_t *conn);
int handle_glProgramUniformMatrix2x3dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix3x2dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix2x4dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix4x2dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix3x4dv_manual(conn_t *conn);
int handle_glProgramUniformMatrix4x3dv_manual(conn_t *conn);
int handle_glGetProgramPipelineInfoLog_manual(conn_t *conn);
int handle_glVertexAttribL1dv_manual(conn_t *conn);
int handle_glVertexAttribL2dv_manual(conn_t *conn);
int handle_glVertexAttribL3dv_manual(conn_t *conn);
int handle_glVertexAttribL4dv_manual(conn_t *conn);
int handle_glVertexAttribLPointer_manual(conn_t *conn);
int handle_glGetVertexAttribLdv_manual(conn_t *conn);
int handle_glViewportArrayv_manual(conn_t *conn);
int handle_glViewportIndexedfv_manual(conn_t *conn);
int handle_glScissorArrayv_manual(conn_t *conn);
int handle_glScissorIndexedv_manual(conn_t *conn);
int handle_glDepthRangeArrayv_manual(conn_t *conn);
int handle_glGetFloati_v_manual(conn_t *conn);
int handle_glGetDoublei_v_manual(conn_t *conn);
int handle_glDrawElementsInstancedBaseInstance_manual(conn_t *conn);
int handle_glDrawElementsInstancedBaseVertexBaseInstance_manual(conn_t *conn);
int handle_glGetInternalformativ_manual(conn_t *conn);
int handle_glGetActiveAtomicCounterBufferiv_manual(conn_t *conn);
int handle_glClearBufferData_manual(conn_t *conn);
int handle_glClearBufferSubData_manual(conn_t *conn);
int handle_glGetFramebufferParameteriv_manual(conn_t *conn);
int handle_glGetInternalformati64v_manual(conn_t *conn);
int handle_glInvalidateFramebuffer_manual(conn_t *conn);
int handle_glInvalidateSubFramebuffer_manual(conn_t *conn);
int handle_glMultiDrawArraysIndirect_manual(conn_t *conn);
int handle_glMultiDrawElementsIndirect_manual(conn_t *conn);
int handle_glGetProgramInterfaceiv_manual(conn_t *conn);
int handle_glGetProgramResourceIndex_manual(conn_t *conn);
int handle_glGetProgramResourceName_manual(conn_t *conn);
int handle_glGetProgramResourceiv_manual(conn_t *conn);
int handle_glGetProgramResourceLocation_manual(conn_t *conn);
int handle_glGetProgramResourceLocationIndex_manual(conn_t *conn);
int handle_glDebugMessageControl_manual(conn_t *conn);
int handle_glDebugMessageInsert_manual(conn_t *conn);
int handle_glDebugMessageCallback_manual(conn_t *conn);
int handle_glGetDebugMessageLog_manual(conn_t *conn);
int handle_glPushDebugGroup_manual(conn_t *conn);
int handle_glObjectLabel_manual(conn_t *conn);
int handle_glGetObjectLabel_manual(conn_t *conn);
int handle_glObjectPtrLabel_manual(conn_t *conn);
int handle_glGetObjectPtrLabel_manual(conn_t *conn);
int handle_glBufferStorage_manual(conn_t *conn);
int handle_glClearTexImage_manual(conn_t *conn);
int handle_glClearTexSubImage_manual(conn_t *conn);
int handle_glBindBuffersBase_manual(conn_t *conn);
int handle_glBindBuffersRange_manual(conn_t *conn);
int handle_glBindTextures_manual(conn_t *conn);
int handle_glBindSamplers_manual(conn_t *conn);
int handle_glBindImageTextures_manual(conn_t *conn);
int handle_glBindVertexBuffers_manual(conn_t *conn);
int handle_glCreateTransformFeedbacks_manual(conn_t *conn);
int handle_glGetTransformFeedbackiv_manual(conn_t *conn);
int handle_glGetTransformFeedbacki_v_manual(conn_t *conn);
int handle_glGetTransformFeedbacki64_v_manual(conn_t *conn);
int handle_glCreateBuffers_manual(conn_t *conn);
int handle_glNamedBufferStorage_manual(conn_t *conn);
int handle_glNamedBufferData_manual(conn_t *conn);
int handle_glNamedBufferSubData_manual(conn_t *conn);
int handle_glClearNamedBufferData_manual(conn_t *conn);
int handle_glClearNamedBufferSubData_manual(conn_t *conn);
int handle_glMapNamedBuffer_manual(conn_t *conn);
int handle_glMapNamedBufferRange_manual(conn_t *conn);
int handle_glUnmapNamedBuffer_manual(conn_t *conn);
int handle_glGetNamedBufferParameteriv_manual(conn_t *conn);
int handle_glGetNamedBufferParameteri64v_manual(conn_t *conn);
int handle_glGetNamedBufferPointerv_manual(conn_t *conn);
int handle_glGetNamedBufferSubData_manual(conn_t *conn);
int handle_glCreateFramebuffers_manual(conn_t *conn);
int handle_glNamedFramebufferDrawBuffers_manual(conn_t *conn);
int handle_glInvalidateNamedFramebufferData_manual(conn_t *conn);
int handle_glInvalidateNamedFramebufferSubData_manual(conn_t *conn);
int handle_glClearNamedFramebufferiv_manual(conn_t *conn);
int handle_glClearNamedFramebufferuiv_manual(conn_t *conn);
int handle_glClearNamedFramebufferfv_manual(conn_t *conn);
int handle_glGetNamedFramebufferParameteriv_manual(conn_t *conn);
int handle_glGetNamedFramebufferAttachmentParameteriv_manual(conn_t *conn);
int handle_glCreateRenderbuffers_manual(conn_t *conn);
int handle_glGetNamedRenderbufferParameteriv_manual(conn_t *conn);
int handle_glCreateTextures_manual(conn_t *conn);
int handle_glTextureSubImage1D_manual(conn_t *conn);
int handle_glTextureSubImage2D_manual(conn_t *conn);
int handle_glTextureSubImage3D_manual(conn_t *conn);
int handle_glCompressedTextureSubImage1D_manual(conn_t *conn);
int handle_glCompressedTextureSubImage2D_manual(conn_t *conn);
int handle_glCompressedTextureSubImage3D_manual(conn_t *conn);
int handle_glTextureParameterfv_manual(conn_t *conn);
int handle_glTextureParameterIiv_manual(conn_t *conn);
int handle_glTextureParameterIuiv_manual(conn_t *conn);
int handle_glTextureParameteriv_manual(conn_t *conn);
int handle_glGetTextureImage_manual(conn_t *conn);
int handle_glGetCompressedTextureImage_manual(conn_t *conn);
int handle_glGetTextureLevelParameterfv_manual(conn_t *conn);
int handle_glGetTextureLevelParameteriv_manual(conn_t *conn);
int handle_glGetTextureParameterfv_manual(conn_t *conn);
int handle_glGetTextureParameterIiv_manual(conn_t *conn);
int handle_glGetTextureParameterIuiv_manual(conn_t *conn);
int handle_glGetTextureParameteriv_manual(conn_t *conn);
int handle_glCreateVertexArrays_manual(conn_t *conn);
int handle_glVertexArrayVertexBuffers_manual(conn_t *conn);
int handle_glGetVertexArrayiv_manual(conn_t *conn);
int handle_glGetVertexArrayIndexediv_manual(conn_t *conn);
int handle_glGetVertexArrayIndexed64iv_manual(conn_t *conn);
int handle_glCreateSamplers_manual(conn_t *conn);
int handle_glCreateProgramPipelines_manual(conn_t *conn);
int handle_glCreateQueries_manual(conn_t *conn);
int handle_glGetTextureSubImage_manual(conn_t *conn);
int handle_glGetCompressedTextureSubImage_manual(conn_t *conn);
int handle_glGetnCompressedTexImage_manual(conn_t *conn);
int handle_glGetnTexImage_manual(conn_t *conn);
int handle_glGetnUniformdv_manual(conn_t *conn);
int handle_glGetnUniformfv_manual(conn_t *conn);
int handle_glGetnUniformiv_manual(conn_t *conn);
int handle_glGetnUniformuiv_manual(conn_t *conn);
int handle_glReadnPixels_manual(conn_t *conn);
int handle_glGetnMapdv_manual(conn_t *conn);
int handle_glGetnMapfv_manual(conn_t *conn);
int handle_glGetnMapiv_manual(conn_t *conn);
int handle_glGetnPixelMapfv_manual(conn_t *conn);
int handle_glGetnPixelMapuiv_manual(conn_t *conn);
int handle_glGetnPixelMapusv_manual(conn_t *conn);
int handle_glGetnPolygonStipple_manual(conn_t *conn);
int handle_glGetnColorTable_manual(conn_t *conn);
int handle_glGetnConvolutionFilter_manual(conn_t *conn);
int handle_glGetnSeparableFilter_manual(conn_t *conn);
int handle_glGetnHistogram_manual(conn_t *conn);
int handle_glGetnMinmax_manual(conn_t *conn);
int handle_glSpecializeShader_manual(conn_t *conn);
int handle_glMultiDrawArraysIndirectCount_manual(conn_t *conn);
int handle_glMultiDrawElementsIndirectCount_manual(conn_t *conn);

int gl_dispatch(conn_t *conn, int func_id,
                const uint8_t *payload, size_t payload_size) {
    switch (func_id) {

    /* -----------------------------------------------------------
     *  A — Batch commands (void/scalar, payload-based)
     * ----------------------------------------------------------- */

    case GL_FUNC_glCullFace: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glCullFace(mode);
        return 0;
    }

    case GL_FUNC_glFrontFace: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glFrontFace(mode);
        return 0;
    }

    case GL_FUNC_glHint: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum mode;
        if (payload_size < 8) return -1;
        memcpy(&mode, payload + 4, 4);
        glHint(target, mode);
        return 0;
    }

    case GL_FUNC_glLineWidth: {
        GLfloat width;
        if (payload_size < 4) return -1;
        memcpy(&width, payload + 0, 4);
        glLineWidth(width);
        return 0;
    }

    case GL_FUNC_glPointSize: {
        GLfloat size;
        if (payload_size < 4) return -1;
        memcpy(&size, payload + 0, 4);
        glPointSize(size);
        return 0;
    }

    case GL_FUNC_glPolygonMode: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLenum mode;
        if (payload_size < 8) return -1;
        memcpy(&mode, payload + 4, 4);
        glPolygonMode(face, mode);
        return 0;
    }

    case GL_FUNC_glScissor: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLsizei width;
        if (payload_size < 12) return -1;
        memcpy(&width, payload + 8, 4);
        GLsizei height;
        if (payload_size < 16) return -1;
        memcpy(&height, payload + 12, 4);
        glScissor(x, y, width, height);
        return 0;
    }

    case GL_FUNC_glTexParameterf: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTexParameterf(target, pname, param);
        return 0;
    }

    case GL_FUNC_glTexParameteri: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTexParameteri(target, pname, param);
        return 0;
    }

    case GL_FUNC_glDrawBuffer: {
        GLenum buf;
        if (payload_size < 4) return -1;
        memcpy(&buf, payload + 0, 4);
        glDrawBuffer(buf);
        return 0;
    }

    case GL_FUNC_glClear: {
        GLbitfield mask;
        if (payload_size < 4) return -1;
        memcpy(&mask, payload + 0, 4);
        glClear(mask);
        return 0;
    }

    case GL_FUNC_glClearColor: {
        GLfloat red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLfloat green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLfloat blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        GLfloat alpha;
        if (payload_size < 16) return -1;
        memcpy(&alpha, payload + 12, 4);
        glClearColor(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glClearStencil: {
        GLint s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        glClearStencil(s);
        return 0;
    }

    case GL_FUNC_glClearDepth: {
        GLdouble depth;
        if (payload_size < 8) return -1;
        memcpy(&depth, payload + 0, 8);
        glClearDepth(depth);
        return 0;
    }

    case GL_FUNC_glStencilMask: {
        GLuint mask;
        if (payload_size < 4) return -1;
        memcpy(&mask, payload + 0, 4);
        glStencilMask(mask);
        return 0;
    }

    case GL_FUNC_glColorMask: {
        GLboolean red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLboolean green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLboolean blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        GLboolean alpha;
        if (payload_size < 4) return -1;
        memcpy(&alpha, payload + 3, 1);
        glColorMask(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glDepthMask: {
        GLboolean flag;
        if (payload_size < 1) return -1;
        memcpy(&flag, payload + 0, 1);
        glDepthMask(flag);
        return 0;
    }

    case GL_FUNC_glDisable: {
        GLenum cap;
        if (payload_size < 4) return -1;
        memcpy(&cap, payload + 0, 4);
        glDisable(cap);
        return 0;
    }

    case GL_FUNC_glEnable: {
        GLenum cap;
        if (payload_size < 4) return -1;
        memcpy(&cap, payload + 0, 4);
        glEnable(cap);
        return 0;
    }

    case GL_FUNC_glFinish: {
        glFinish();
        return 0;
    }

    case GL_FUNC_glFlush: {
        glFlush();
        return 0;
    }

    case GL_FUNC_glBlendFunc: {
        GLenum sfactor;
        if (payload_size < 4) return -1;
        memcpy(&sfactor, payload + 0, 4);
        GLenum dfactor;
        if (payload_size < 8) return -1;
        memcpy(&dfactor, payload + 4, 4);
        glBlendFunc(sfactor, dfactor);
        return 0;
    }

    case GL_FUNC_glLogicOp: {
        GLenum opcode;
        if (payload_size < 4) return -1;
        memcpy(&opcode, payload + 0, 4);
        glLogicOp(opcode);
        return 0;
    }

    case GL_FUNC_glStencilFunc: {
        GLenum func;
        if (payload_size < 4) return -1;
        memcpy(&func, payload + 0, 4);
        GLint ref;
        if (payload_size < 8) return -1;
        memcpy(&ref, payload + 4, 4);
        GLuint mask;
        if (payload_size < 12) return -1;
        memcpy(&mask, payload + 8, 4);
        glStencilFunc(func, ref, mask);
        return 0;
    }

    case GL_FUNC_glStencilOp: {
        GLenum fail;
        if (payload_size < 4) return -1;
        memcpy(&fail, payload + 0, 4);
        GLenum zfail;
        if (payload_size < 8) return -1;
        memcpy(&zfail, payload + 4, 4);
        GLenum zpass;
        if (payload_size < 12) return -1;
        memcpy(&zpass, payload + 8, 4);
        glStencilOp(fail, zfail, zpass);
        return 0;
    }

    case GL_FUNC_glDepthFunc: {
        GLenum func;
        if (payload_size < 4) return -1;
        memcpy(&func, payload + 0, 4);
        glDepthFunc(func);
        return 0;
    }

    case GL_FUNC_glPixelStoref: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLfloat param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glPixelStoref(pname, param);
        return 0;
    }

    case GL_FUNC_glPixelStorei: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLint param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glPixelStorei(pname, param);
        return 0;
    }

    case GL_FUNC_glReadBuffer: {
        GLenum src;
        if (payload_size < 4) return -1;
        memcpy(&src, payload + 0, 4);
        glReadBuffer(src);
        return 0;
    }

    case GL_FUNC_glDepthRange: {
        GLdouble n;
        if (payload_size < 8) return -1;
        memcpy(&n, payload + 0, 8);
        GLdouble f;
        if (payload_size < 16) return -1;
        memcpy(&f, payload + 8, 8);
        glDepthRange(n, f);
        return 0;
    }

    case GL_FUNC_glViewport: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLsizei width;
        if (payload_size < 12) return -1;
        memcpy(&width, payload + 8, 4);
        GLsizei height;
        if (payload_size < 16) return -1;
        memcpy(&height, payload + 12, 4);
        glViewport(x, y, width, height);
        return 0;
    }

    case GL_FUNC_glNewList: {
        GLuint list;
        if (payload_size < 4) return -1;
        memcpy(&list, payload + 0, 4);
        GLenum mode;
        if (payload_size < 8) return -1;
        memcpy(&mode, payload + 4, 4);
        glNewList(list, mode);
        return 0;
    }

    case GL_FUNC_glEndList: {
        glEndList();
        return 0;
    }

    case GL_FUNC_glCallList: {
        GLuint list;
        if (payload_size < 4) return -1;
        memcpy(&list, payload + 0, 4);
        glCallList(list);
        return 0;
    }

    case GL_FUNC_glDeleteLists: {
        GLuint list;
        if (payload_size < 4) return -1;
        memcpy(&list, payload + 0, 4);
        GLsizei range;
        if (payload_size < 8) return -1;
        memcpy(&range, payload + 4, 4);
        glDeleteLists(list, range);
        return 0;
    }

    case GL_FUNC_glListBase: {
        GLuint base;
        if (payload_size < 4) return -1;
        memcpy(&base, payload + 0, 4);
        glListBase(base);
        return 0;
    }

    case GL_FUNC_glBegin: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glBegin(mode);
        return 0;
    }

    case GL_FUNC_glColor3b: {
        GLbyte red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLbyte green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLbyte blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        glColor3b(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3d: {
        GLdouble red;
        if (payload_size < 8) return -1;
        memcpy(&red, payload + 0, 8);
        GLdouble green;
        if (payload_size < 16) return -1;
        memcpy(&green, payload + 8, 8);
        GLdouble blue;
        if (payload_size < 24) return -1;
        memcpy(&blue, payload + 16, 8);
        glColor3d(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3f: {
        GLfloat red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLfloat green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLfloat blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        glColor3f(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3i: {
        GLint red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLint green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLint blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        glColor3i(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3s: {
        GLshort red;
        if (payload_size < 2) return -1;
        memcpy(&red, payload + 0, 2);
        GLshort green;
        if (payload_size < 4) return -1;
        memcpy(&green, payload + 2, 2);
        GLshort blue;
        if (payload_size < 6) return -1;
        memcpy(&blue, payload + 4, 2);
        glColor3s(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3ub: {
        GLubyte red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLubyte green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLubyte blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        glColor3ub(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3ui: {
        GLuint red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLuint green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLuint blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        glColor3ui(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor3us: {
        GLushort red;
        if (payload_size < 2) return -1;
        memcpy(&red, payload + 0, 2);
        GLushort green;
        if (payload_size < 4) return -1;
        memcpy(&green, payload + 2, 2);
        GLushort blue;
        if (payload_size < 6) return -1;
        memcpy(&blue, payload + 4, 2);
        glColor3us(red, green, blue);
        return 0;
    }

    case GL_FUNC_glColor4b: {
        GLbyte red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLbyte green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLbyte blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        GLbyte alpha;
        if (payload_size < 4) return -1;
        memcpy(&alpha, payload + 3, 1);
        glColor4b(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4d: {
        GLdouble red;
        if (payload_size < 8) return -1;
        memcpy(&red, payload + 0, 8);
        GLdouble green;
        if (payload_size < 16) return -1;
        memcpy(&green, payload + 8, 8);
        GLdouble blue;
        if (payload_size < 24) return -1;
        memcpy(&blue, payload + 16, 8);
        GLdouble alpha;
        if (payload_size < 32) return -1;
        memcpy(&alpha, payload + 24, 8);
        glColor4d(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4f: {
        GLfloat red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLfloat green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLfloat blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        GLfloat alpha;
        if (payload_size < 16) return -1;
        memcpy(&alpha, payload + 12, 4);
        glColor4f(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4i: {
        GLint red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLint green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLint blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        GLint alpha;
        if (payload_size < 16) return -1;
        memcpy(&alpha, payload + 12, 4);
        glColor4i(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4s: {
        GLshort red;
        if (payload_size < 2) return -1;
        memcpy(&red, payload + 0, 2);
        GLshort green;
        if (payload_size < 4) return -1;
        memcpy(&green, payload + 2, 2);
        GLshort blue;
        if (payload_size < 6) return -1;
        memcpy(&blue, payload + 4, 2);
        GLshort alpha;
        if (payload_size < 8) return -1;
        memcpy(&alpha, payload + 6, 2);
        glColor4s(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4ub: {
        GLubyte red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLubyte green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLubyte blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        GLubyte alpha;
        if (payload_size < 4) return -1;
        memcpy(&alpha, payload + 3, 1);
        glColor4ub(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4ui: {
        GLuint red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLuint green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLuint blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        GLuint alpha;
        if (payload_size < 16) return -1;
        memcpy(&alpha, payload + 12, 4);
        glColor4ui(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glColor4us: {
        GLushort red;
        if (payload_size < 2) return -1;
        memcpy(&red, payload + 0, 2);
        GLushort green;
        if (payload_size < 4) return -1;
        memcpy(&green, payload + 2, 2);
        GLushort blue;
        if (payload_size < 6) return -1;
        memcpy(&blue, payload + 4, 2);
        GLushort alpha;
        if (payload_size < 8) return -1;
        memcpy(&alpha, payload + 6, 2);
        glColor4us(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glEdgeFlag: {
        GLboolean flag;
        if (payload_size < 1) return -1;
        memcpy(&flag, payload + 0, 1);
        glEdgeFlag(flag);
        return 0;
    }

    case GL_FUNC_glEnd: {
        glEnd();
        return 0;
    }

    case GL_FUNC_glIndexd: {
        GLdouble c;
        if (payload_size < 8) return -1;
        memcpy(&c, payload + 0, 8);
        glIndexd(c);
        return 0;
    }

    case GL_FUNC_glIndexf: {
        GLfloat c;
        if (payload_size < 4) return -1;
        memcpy(&c, payload + 0, 4);
        glIndexf(c);
        return 0;
    }

    case GL_FUNC_glIndexi: {
        GLint c;
        if (payload_size < 4) return -1;
        memcpy(&c, payload + 0, 4);
        glIndexi(c);
        return 0;
    }

    case GL_FUNC_glIndexs: {
        GLshort c;
        if (payload_size < 2) return -1;
        memcpy(&c, payload + 0, 2);
        glIndexs(c);
        return 0;
    }

    case GL_FUNC_glNormal3b: {
        GLbyte nx;
        if (payload_size < 1) return -1;
        memcpy(&nx, payload + 0, 1);
        GLbyte ny;
        if (payload_size < 2) return -1;
        memcpy(&ny, payload + 1, 1);
        GLbyte nz;
        if (payload_size < 3) return -1;
        memcpy(&nz, payload + 2, 1);
        glNormal3b(nx, ny, nz);
        return 0;
    }

    case GL_FUNC_glNormal3d: {
        GLdouble nx;
        if (payload_size < 8) return -1;
        memcpy(&nx, payload + 0, 8);
        GLdouble ny;
        if (payload_size < 16) return -1;
        memcpy(&ny, payload + 8, 8);
        GLdouble nz;
        if (payload_size < 24) return -1;
        memcpy(&nz, payload + 16, 8);
        glNormal3d(nx, ny, nz);
        return 0;
    }

    case GL_FUNC_glNormal3f: {
        GLfloat nx;
        if (payload_size < 4) return -1;
        memcpy(&nx, payload + 0, 4);
        GLfloat ny;
        if (payload_size < 8) return -1;
        memcpy(&ny, payload + 4, 4);
        GLfloat nz;
        if (payload_size < 12) return -1;
        memcpy(&nz, payload + 8, 4);
        glNormal3f(nx, ny, nz);
        return 0;
    }

    case GL_FUNC_glNormal3i: {
        GLint nx;
        if (payload_size < 4) return -1;
        memcpy(&nx, payload + 0, 4);
        GLint ny;
        if (payload_size < 8) return -1;
        memcpy(&ny, payload + 4, 4);
        GLint nz;
        if (payload_size < 12) return -1;
        memcpy(&nz, payload + 8, 4);
        glNormal3i(nx, ny, nz);
        return 0;
    }

    case GL_FUNC_glNormal3s: {
        GLshort nx;
        if (payload_size < 2) return -1;
        memcpy(&nx, payload + 0, 2);
        GLshort ny;
        if (payload_size < 4) return -1;
        memcpy(&ny, payload + 2, 2);
        GLshort nz;
        if (payload_size < 6) return -1;
        memcpy(&nz, payload + 4, 2);
        glNormal3s(nx, ny, nz);
        return 0;
    }

    case GL_FUNC_glRasterPos2d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        glRasterPos2d(x, y);
        return 0;
    }

    case GL_FUNC_glRasterPos2f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        glRasterPos2f(x, y);
        return 0;
    }

    case GL_FUNC_glRasterPos2i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        glRasterPos2i(x, y);
        return 0;
    }

    case GL_FUNC_glRasterPos2s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        glRasterPos2s(x, y);
        return 0;
    }

    case GL_FUNC_glRasterPos3d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        glRasterPos3d(x, y, z);
        return 0;
    }

    case GL_FUNC_glRasterPos3f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glRasterPos3f(x, y, z);
        return 0;
    }

    case GL_FUNC_glRasterPos3i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLint z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glRasterPos3i(x, y, z);
        return 0;
    }

    case GL_FUNC_glRasterPos3s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        GLshort z;
        if (payload_size < 6) return -1;
        memcpy(&z, payload + 4, 2);
        glRasterPos3s(x, y, z);
        return 0;
    }

    case GL_FUNC_glRasterPos4d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        GLdouble w;
        if (payload_size < 32) return -1;
        memcpy(&w, payload + 24, 8);
        glRasterPos4d(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glRasterPos4f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        GLfloat w;
        if (payload_size < 16) return -1;
        memcpy(&w, payload + 12, 4);
        glRasterPos4f(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glRasterPos4i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLint z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        GLint w;
        if (payload_size < 16) return -1;
        memcpy(&w, payload + 12, 4);
        glRasterPos4i(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glRasterPos4s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        GLshort z;
        if (payload_size < 6) return -1;
        memcpy(&z, payload + 4, 2);
        GLshort w;
        if (payload_size < 8) return -1;
        memcpy(&w, payload + 6, 2);
        glRasterPos4s(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glRectd: {
        GLdouble x1;
        if (payload_size < 8) return -1;
        memcpy(&x1, payload + 0, 8);
        GLdouble y1;
        if (payload_size < 16) return -1;
        memcpy(&y1, payload + 8, 8);
        GLdouble x2;
        if (payload_size < 24) return -1;
        memcpy(&x2, payload + 16, 8);
        GLdouble y2;
        if (payload_size < 32) return -1;
        memcpy(&y2, payload + 24, 8);
        glRectd(x1, y1, x2, y2);
        return 0;
    }

    case GL_FUNC_glRectf: {
        GLfloat x1;
        if (payload_size < 4) return -1;
        memcpy(&x1, payload + 0, 4);
        GLfloat y1;
        if (payload_size < 8) return -1;
        memcpy(&y1, payload + 4, 4);
        GLfloat x2;
        if (payload_size < 12) return -1;
        memcpy(&x2, payload + 8, 4);
        GLfloat y2;
        if (payload_size < 16) return -1;
        memcpy(&y2, payload + 12, 4);
        glRectf(x1, y1, x2, y2);
        return 0;
    }

    case GL_FUNC_glRecti: {
        GLint x1;
        if (payload_size < 4) return -1;
        memcpy(&x1, payload + 0, 4);
        GLint y1;
        if (payload_size < 8) return -1;
        memcpy(&y1, payload + 4, 4);
        GLint x2;
        if (payload_size < 12) return -1;
        memcpy(&x2, payload + 8, 4);
        GLint y2;
        if (payload_size < 16) return -1;
        memcpy(&y2, payload + 12, 4);
        glRecti(x1, y1, x2, y2);
        return 0;
    }

    case GL_FUNC_glRects: {
        GLshort x1;
        if (payload_size < 2) return -1;
        memcpy(&x1, payload + 0, 2);
        GLshort y1;
        if (payload_size < 4) return -1;
        memcpy(&y1, payload + 2, 2);
        GLshort x2;
        if (payload_size < 6) return -1;
        memcpy(&x2, payload + 4, 2);
        GLshort y2;
        if (payload_size < 8) return -1;
        memcpy(&y2, payload + 6, 2);
        glRects(x1, y1, x2, y2);
        return 0;
    }

    case GL_FUNC_glTexCoord1d: {
        GLdouble s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 0, 8);
        glTexCoord1d(s);
        return 0;
    }

    case GL_FUNC_glTexCoord1f: {
        GLfloat s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        glTexCoord1f(s);
        return 0;
    }

    case GL_FUNC_glTexCoord1i: {
        GLint s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        glTexCoord1i(s);
        return 0;
    }

    case GL_FUNC_glTexCoord1s: {
        GLshort s;
        if (payload_size < 2) return -1;
        memcpy(&s, payload + 0, 2);
        glTexCoord1s(s);
        return 0;
    }

    case GL_FUNC_glTexCoord2d: {
        GLdouble s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 0, 8);
        GLdouble t;
        if (payload_size < 16) return -1;
        memcpy(&t, payload + 8, 8);
        glTexCoord2d(s, t);
        return 0;
    }

    case GL_FUNC_glTexCoord2f: {
        GLfloat s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        GLfloat t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 4, 4);
        glTexCoord2f(s, t);
        return 0;
    }

    case GL_FUNC_glTexCoord2i: {
        GLint s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        GLint t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 4, 4);
        glTexCoord2i(s, t);
        return 0;
    }

    case GL_FUNC_glTexCoord2s: {
        GLshort s;
        if (payload_size < 2) return -1;
        memcpy(&s, payload + 0, 2);
        GLshort t;
        if (payload_size < 4) return -1;
        memcpy(&t, payload + 2, 2);
        glTexCoord2s(s, t);
        return 0;
    }

    case GL_FUNC_glTexCoord3d: {
        GLdouble s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 0, 8);
        GLdouble t;
        if (payload_size < 16) return -1;
        memcpy(&t, payload + 8, 8);
        GLdouble r;
        if (payload_size < 24) return -1;
        memcpy(&r, payload + 16, 8);
        glTexCoord3d(s, t, r);
        return 0;
    }

    case GL_FUNC_glTexCoord3f: {
        GLfloat s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        GLfloat t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 4, 4);
        GLfloat r;
        if (payload_size < 12) return -1;
        memcpy(&r, payload + 8, 4);
        glTexCoord3f(s, t, r);
        return 0;
    }

    case GL_FUNC_glTexCoord3i: {
        GLint s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        GLint t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 4, 4);
        GLint r;
        if (payload_size < 12) return -1;
        memcpy(&r, payload + 8, 4);
        glTexCoord3i(s, t, r);
        return 0;
    }

    case GL_FUNC_glTexCoord3s: {
        GLshort s;
        if (payload_size < 2) return -1;
        memcpy(&s, payload + 0, 2);
        GLshort t;
        if (payload_size < 4) return -1;
        memcpy(&t, payload + 2, 2);
        GLshort r;
        if (payload_size < 6) return -1;
        memcpy(&r, payload + 4, 2);
        glTexCoord3s(s, t, r);
        return 0;
    }

    case GL_FUNC_glTexCoord4d: {
        GLdouble s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 0, 8);
        GLdouble t;
        if (payload_size < 16) return -1;
        memcpy(&t, payload + 8, 8);
        GLdouble r;
        if (payload_size < 24) return -1;
        memcpy(&r, payload + 16, 8);
        GLdouble q;
        if (payload_size < 32) return -1;
        memcpy(&q, payload + 24, 8);
        glTexCoord4d(s, t, r, q);
        return 0;
    }

    case GL_FUNC_glTexCoord4f: {
        GLfloat s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        GLfloat t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 4, 4);
        GLfloat r;
        if (payload_size < 12) return -1;
        memcpy(&r, payload + 8, 4);
        GLfloat q;
        if (payload_size < 16) return -1;
        memcpy(&q, payload + 12, 4);
        glTexCoord4f(s, t, r, q);
        return 0;
    }

    case GL_FUNC_glTexCoord4i: {
        GLint s;
        if (payload_size < 4) return -1;
        memcpy(&s, payload + 0, 4);
        GLint t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 4, 4);
        GLint r;
        if (payload_size < 12) return -1;
        memcpy(&r, payload + 8, 4);
        GLint q;
        if (payload_size < 16) return -1;
        memcpy(&q, payload + 12, 4);
        glTexCoord4i(s, t, r, q);
        return 0;
    }

    case GL_FUNC_glTexCoord4s: {
        GLshort s;
        if (payload_size < 2) return -1;
        memcpy(&s, payload + 0, 2);
        GLshort t;
        if (payload_size < 4) return -1;
        memcpy(&t, payload + 2, 2);
        GLshort r;
        if (payload_size < 6) return -1;
        memcpy(&r, payload + 4, 2);
        GLshort q;
        if (payload_size < 8) return -1;
        memcpy(&q, payload + 6, 2);
        glTexCoord4s(s, t, r, q);
        return 0;
    }

    case GL_FUNC_glVertex2d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        glVertex2d(x, y);
        return 0;
    }

    case GL_FUNC_glVertex2f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        glVertex2f(x, y);
        return 0;
    }

    case GL_FUNC_glVertex2i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        glVertex2i(x, y);
        return 0;
    }

    case GL_FUNC_glVertex2s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        glVertex2s(x, y);
        return 0;
    }

    case GL_FUNC_glVertex3d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        glVertex3d(x, y, z);
        return 0;
    }

    case GL_FUNC_glVertex3f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glVertex3f(x, y, z);
        return 0;
    }

    case GL_FUNC_glVertex3i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLint z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glVertex3i(x, y, z);
        return 0;
    }

    case GL_FUNC_glVertex3s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        GLshort z;
        if (payload_size < 6) return -1;
        memcpy(&z, payload + 4, 2);
        glVertex3s(x, y, z);
        return 0;
    }

    case GL_FUNC_glVertex4d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        GLdouble w;
        if (payload_size < 32) return -1;
        memcpy(&w, payload + 24, 8);
        glVertex4d(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertex4f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        GLfloat w;
        if (payload_size < 16) return -1;
        memcpy(&w, payload + 12, 4);
        glVertex4f(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertex4i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLint z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        GLint w;
        if (payload_size < 16) return -1;
        memcpy(&w, payload + 12, 4);
        glVertex4i(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertex4s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        GLshort z;
        if (payload_size < 6) return -1;
        memcpy(&z, payload + 4, 2);
        GLshort w;
        if (payload_size < 8) return -1;
        memcpy(&w, payload + 6, 2);
        glVertex4s(x, y, z, w);
        return 0;
    }

    case GL_FUNC_glColorMaterial: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLenum mode;
        if (payload_size < 8) return -1;
        memcpy(&mode, payload + 4, 4);
        glColorMaterial(face, mode);
        return 0;
    }

    case GL_FUNC_glFogf: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLfloat param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glFogf(pname, param);
        return 0;
    }

    case GL_FUNC_glFogi: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLint param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glFogi(pname, param);
        return 0;
    }

    case GL_FUNC_glLightf: {
        GLenum light;
        if (payload_size < 4) return -1;
        memcpy(&light, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glLightf(light, pname, param);
        return 0;
    }

    case GL_FUNC_glLighti: {
        GLenum light;
        if (payload_size < 4) return -1;
        memcpy(&light, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glLighti(light, pname, param);
        return 0;
    }

    case GL_FUNC_glLightModelf: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLfloat param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glLightModelf(pname, param);
        return 0;
    }

    case GL_FUNC_glLightModeli: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLint param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glLightModeli(pname, param);
        return 0;
    }

    case GL_FUNC_glLineStipple: {
        GLint factor;
        if (payload_size < 4) return -1;
        memcpy(&factor, payload + 0, 4);
        GLushort pattern;
        if (payload_size < 6) return -1;
        memcpy(&pattern, payload + 4, 2);
        glLineStipple(factor, pattern);
        return 0;
    }

    case GL_FUNC_glMaterialf: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glMaterialf(face, pname, param);
        return 0;
    }

    case GL_FUNC_glMateriali: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glMateriali(face, pname, param);
        return 0;
    }

    case GL_FUNC_glShadeModel: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glShadeModel(mode);
        return 0;
    }

    case GL_FUNC_glTexEnvf: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTexEnvf(target, pname, param);
        return 0;
    }

    case GL_FUNC_glTexEnvi: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTexEnvi(target, pname, param);
        return 0;
    }

    case GL_FUNC_glTexGend: {
        GLenum coord;
        if (payload_size < 4) return -1;
        memcpy(&coord, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLdouble param;
        if (payload_size < 16) return -1;
        memcpy(&param, payload + 8, 8);
        glTexGend(coord, pname, param);
        return 0;
    }

    case GL_FUNC_glTexGenf: {
        GLenum coord;
        if (payload_size < 4) return -1;
        memcpy(&coord, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTexGenf(coord, pname, param);
        return 0;
    }

    case GL_FUNC_glTexGeni: {
        GLenum coord;
        if (payload_size < 4) return -1;
        memcpy(&coord, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTexGeni(coord, pname, param);
        return 0;
    }

    case GL_FUNC_glInitNames: {
        glInitNames();
        return 0;
    }

    case GL_FUNC_glLoadName: {
        GLuint name;
        if (payload_size < 4) return -1;
        memcpy(&name, payload + 0, 4);
        glLoadName(name);
        return 0;
    }

    case GL_FUNC_glPassThrough: {
        GLfloat token;
        if (payload_size < 4) return -1;
        memcpy(&token, payload + 0, 4);
        glPassThrough(token);
        return 0;
    }

    case GL_FUNC_glPopName: {
        glPopName();
        return 0;
    }

    case GL_FUNC_glPushName: {
        GLuint name;
        if (payload_size < 4) return -1;
        memcpy(&name, payload + 0, 4);
        glPushName(name);
        return 0;
    }

    case GL_FUNC_glClearAccum: {
        GLfloat red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLfloat green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLfloat blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        GLfloat alpha;
        if (payload_size < 16) return -1;
        memcpy(&alpha, payload + 12, 4);
        glClearAccum(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glClearIndex: {
        GLfloat c;
        if (payload_size < 4) return -1;
        memcpy(&c, payload + 0, 4);
        glClearIndex(c);
        return 0;
    }

    case GL_FUNC_glIndexMask: {
        GLuint mask;
        if (payload_size < 4) return -1;
        memcpy(&mask, payload + 0, 4);
        glIndexMask(mask);
        return 0;
    }

    case GL_FUNC_glAccum: {
        GLenum op;
        if (payload_size < 4) return -1;
        memcpy(&op, payload + 0, 4);
        GLfloat value;
        if (payload_size < 8) return -1;
        memcpy(&value, payload + 4, 4);
        glAccum(op, value);
        return 0;
    }

    case GL_FUNC_glPopAttrib: {
        glPopAttrib();
        return 0;
    }

    case GL_FUNC_glPushAttrib: {
        GLbitfield mask;
        if (payload_size < 4) return -1;
        memcpy(&mask, payload + 0, 4);
        glPushAttrib(mask);
        return 0;
    }

    case GL_FUNC_glMapGrid1d: {
        GLint un;
        if (payload_size < 4) return -1;
        memcpy(&un, payload + 0, 4);
        GLdouble u1;
        if (payload_size < 12) return -1;
        memcpy(&u1, payload + 4, 8);
        GLdouble u2;
        if (payload_size < 20) return -1;
        memcpy(&u2, payload + 12, 8);
        glMapGrid1d(un, u1, u2);
        return 0;
    }

    case GL_FUNC_glMapGrid1f: {
        GLint un;
        if (payload_size < 4) return -1;
        memcpy(&un, payload + 0, 4);
        GLfloat u1;
        if (payload_size < 8) return -1;
        memcpy(&u1, payload + 4, 4);
        GLfloat u2;
        if (payload_size < 12) return -1;
        memcpy(&u2, payload + 8, 4);
        glMapGrid1f(un, u1, u2);
        return 0;
    }

    case GL_FUNC_glMapGrid2d: {
        GLint un;
        if (payload_size < 4) return -1;
        memcpy(&un, payload + 0, 4);
        GLdouble u1;
        if (payload_size < 12) return -1;
        memcpy(&u1, payload + 4, 8);
        GLdouble u2;
        if (payload_size < 20) return -1;
        memcpy(&u2, payload + 12, 8);
        GLint vn;
        if (payload_size < 24) return -1;
        memcpy(&vn, payload + 20, 4);
        GLdouble v1;
        if (payload_size < 32) return -1;
        memcpy(&v1, payload + 24, 8);
        GLdouble v2;
        if (payload_size < 40) return -1;
        memcpy(&v2, payload + 32, 8);
        glMapGrid2d(un, u1, u2, vn, v1, v2);
        return 0;
    }

    case GL_FUNC_glMapGrid2f: {
        GLint un;
        if (payload_size < 4) return -1;
        memcpy(&un, payload + 0, 4);
        GLfloat u1;
        if (payload_size < 8) return -1;
        memcpy(&u1, payload + 4, 4);
        GLfloat u2;
        if (payload_size < 12) return -1;
        memcpy(&u2, payload + 8, 4);
        GLint vn;
        if (payload_size < 16) return -1;
        memcpy(&vn, payload + 12, 4);
        GLfloat v1;
        if (payload_size < 20) return -1;
        memcpy(&v1, payload + 16, 4);
        GLfloat v2;
        if (payload_size < 24) return -1;
        memcpy(&v2, payload + 20, 4);
        glMapGrid2f(un, u1, u2, vn, v1, v2);
        return 0;
    }

    case GL_FUNC_glEvalCoord1d: {
        GLdouble u;
        if (payload_size < 8) return -1;
        memcpy(&u, payload + 0, 8);
        glEvalCoord1d(u);
        return 0;
    }

    case GL_FUNC_glEvalCoord1f: {
        GLfloat u;
        if (payload_size < 4) return -1;
        memcpy(&u, payload + 0, 4);
        glEvalCoord1f(u);
        return 0;
    }

    case GL_FUNC_glEvalCoord2d: {
        GLdouble u;
        if (payload_size < 8) return -1;
        memcpy(&u, payload + 0, 8);
        GLdouble v;
        if (payload_size < 16) return -1;
        memcpy(&v, payload + 8, 8);
        glEvalCoord2d(u, v);
        return 0;
    }

    case GL_FUNC_glEvalCoord2f: {
        GLfloat u;
        if (payload_size < 4) return -1;
        memcpy(&u, payload + 0, 4);
        GLfloat v;
        if (payload_size < 8) return -1;
        memcpy(&v, payload + 4, 4);
        glEvalCoord2f(u, v);
        return 0;
    }

    case GL_FUNC_glEvalMesh1: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLint i1;
        if (payload_size < 8) return -1;
        memcpy(&i1, payload + 4, 4);
        GLint i2;
        if (payload_size < 12) return -1;
        memcpy(&i2, payload + 8, 4);
        glEvalMesh1(mode, i1, i2);
        return 0;
    }

    case GL_FUNC_glEvalPoint1: {
        GLint i;
        if (payload_size < 4) return -1;
        memcpy(&i, payload + 0, 4);
        glEvalPoint1(i);
        return 0;
    }

    case GL_FUNC_glEvalMesh2: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLint i1;
        if (payload_size < 8) return -1;
        memcpy(&i1, payload + 4, 4);
        GLint i2;
        if (payload_size < 12) return -1;
        memcpy(&i2, payload + 8, 4);
        GLint j1;
        if (payload_size < 16) return -1;
        memcpy(&j1, payload + 12, 4);
        GLint j2;
        if (payload_size < 20) return -1;
        memcpy(&j2, payload + 16, 4);
        glEvalMesh2(mode, i1, i2, j1, j2);
        return 0;
    }

    case GL_FUNC_glEvalPoint2: {
        GLint i;
        if (payload_size < 4) return -1;
        memcpy(&i, payload + 0, 4);
        GLint j;
        if (payload_size < 8) return -1;
        memcpy(&j, payload + 4, 4);
        glEvalPoint2(i, j);
        return 0;
    }

    case GL_FUNC_glAlphaFunc: {
        GLenum func;
        if (payload_size < 4) return -1;
        memcpy(&func, payload + 0, 4);
        GLfloat ref;
        if (payload_size < 8) return -1;
        memcpy(&ref, payload + 4, 4);
        glAlphaFunc(func, ref);
        return 0;
    }

    case GL_FUNC_glPixelZoom: {
        GLfloat xfactor;
        if (payload_size < 4) return -1;
        memcpy(&xfactor, payload + 0, 4);
        GLfloat yfactor;
        if (payload_size < 8) return -1;
        memcpy(&yfactor, payload + 4, 4);
        glPixelZoom(xfactor, yfactor);
        return 0;
    }

    case GL_FUNC_glPixelTransferf: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLfloat param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glPixelTransferf(pname, param);
        return 0;
    }

    case GL_FUNC_glPixelTransferi: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLint param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glPixelTransferi(pname, param);
        return 0;
    }

    case GL_FUNC_glCopyPixels: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLsizei width;
        if (payload_size < 12) return -1;
        memcpy(&width, payload + 8, 4);
        GLsizei height;
        if (payload_size < 16) return -1;
        memcpy(&height, payload + 12, 4);
        GLenum type;
        if (payload_size < 20) return -1;
        memcpy(&type, payload + 16, 4);
        glCopyPixels(x, y, width, height, type);
        return 0;
    }

    case GL_FUNC_glFrustum: {
        GLdouble left;
        if (payload_size < 8) return -1;
        memcpy(&left, payload + 0, 8);
        GLdouble right;
        if (payload_size < 16) return -1;
        memcpy(&right, payload + 8, 8);
        GLdouble bottom;
        if (payload_size < 24) return -1;
        memcpy(&bottom, payload + 16, 8);
        GLdouble top;
        if (payload_size < 32) return -1;
        memcpy(&top, payload + 24, 8);
        GLdouble zNear;
        if (payload_size < 40) return -1;
        memcpy(&zNear, payload + 32, 8);
        GLdouble zFar;
        if (payload_size < 48) return -1;
        memcpy(&zFar, payload + 40, 8);
        glFrustum(left, right, bottom, top, zNear, zFar);
        return 0;
    }

    case GL_FUNC_glLoadIdentity: {
        glLoadIdentity();
        return 0;
    }

    case GL_FUNC_glMatrixMode: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glMatrixMode(mode);
        return 0;
    }

    case GL_FUNC_glOrtho: {
        GLdouble left;
        if (payload_size < 8) return -1;
        memcpy(&left, payload + 0, 8);
        GLdouble right;
        if (payload_size < 16) return -1;
        memcpy(&right, payload + 8, 8);
        GLdouble bottom;
        if (payload_size < 24) return -1;
        memcpy(&bottom, payload + 16, 8);
        GLdouble top;
        if (payload_size < 32) return -1;
        memcpy(&top, payload + 24, 8);
        GLdouble zNear;
        if (payload_size < 40) return -1;
        memcpy(&zNear, payload + 32, 8);
        GLdouble zFar;
        if (payload_size < 48) return -1;
        memcpy(&zFar, payload + 40, 8);
        glOrtho(left, right, bottom, top, zNear, zFar);
        return 0;
    }

    case GL_FUNC_glPopMatrix: {
        glPopMatrix();
        return 0;
    }

    case GL_FUNC_glPushMatrix: {
        glPushMatrix();
        return 0;
    }

    case GL_FUNC_glRotated: {
        GLdouble angle;
        if (payload_size < 8) return -1;
        memcpy(&angle, payload + 0, 8);
        GLdouble x;
        if (payload_size < 16) return -1;
        memcpy(&x, payload + 8, 8);
        GLdouble y;
        if (payload_size < 24) return -1;
        memcpy(&y, payload + 16, 8);
        GLdouble z;
        if (payload_size < 32) return -1;
        memcpy(&z, payload + 24, 8);
        glRotated(angle, x, y, z);
        return 0;
    }

    case GL_FUNC_glRotatef: {
        GLfloat angle;
        if (payload_size < 4) return -1;
        memcpy(&angle, payload + 0, 4);
        GLfloat x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLfloat y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLfloat z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        glRotatef(angle, x, y, z);
        return 0;
    }

    case GL_FUNC_glScaled: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        glScaled(x, y, z);
        return 0;
    }

    case GL_FUNC_glScalef: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glScalef(x, y, z);
        return 0;
    }

    case GL_FUNC_glTranslated: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        glTranslated(x, y, z);
        return 0;
    }

    case GL_FUNC_glTranslatef: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glTranslatef(x, y, z);
        return 0;
    }

    case GL_FUNC_glDrawArrays: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLint first;
        if (payload_size < 8) return -1;
        memcpy(&first, payload + 4, 4);
        GLsizei count;
        if (payload_size < 12) return -1;
        memcpy(&count, payload + 8, 4);
        glDrawArrays(mode, first, count);
        return 0;
    }

    case GL_FUNC_glPolygonOffset: {
        GLfloat factor;
        if (payload_size < 4) return -1;
        memcpy(&factor, payload + 0, 4);
        GLfloat units;
        if (payload_size < 8) return -1;
        memcpy(&units, payload + 4, 4);
        glPolygonOffset(factor, units);
        return 0;
    }

    case GL_FUNC_glCopyTexSubImage1D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint x;
        if (payload_size < 16) return -1;
        memcpy(&x, payload + 12, 4);
        GLint y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 16, 4);
        GLsizei width;
        if (payload_size < 24) return -1;
        memcpy(&width, payload + 20, 4);
        glCopyTexSubImage1D(target, level, xoffset, x, y, width);
        return 0;
    }

    case GL_FUNC_glCopyTexSubImage2D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint yoffset;
        if (payload_size < 16) return -1;
        memcpy(&yoffset, payload + 12, 4);
        GLint x;
        if (payload_size < 20) return -1;
        memcpy(&x, payload + 16, 4);
        GLint y;
        if (payload_size < 24) return -1;
        memcpy(&y, payload + 20, 4);
        GLsizei width;
        if (payload_size < 28) return -1;
        memcpy(&width, payload + 24, 4);
        GLsizei height;
        if (payload_size < 32) return -1;
        memcpy(&height, payload + 28, 4);
        glCopyTexSubImage2D(target, level, xoffset, yoffset, x, y, width, height);
        return 0;
    }

    case GL_FUNC_glBindTexture: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint texture;
        if (payload_size < 8) return -1;
        memcpy(&texture, payload + 4, 4);
        glBindTexture(target, texture);
        return 0;
    }

    case GL_FUNC_glArrayElement: {
        GLint i;
        if (payload_size < 4) return -1;
        memcpy(&i, payload + 0, 4);
        glArrayElement(i);
        return 0;
    }

    case GL_FUNC_glDisableClientState: {
        GLenum array;
        if (payload_size < 4) return -1;
        memcpy(&array, payload + 0, 4);
        glDisableClientState(array);
        return 0;
    }

    case GL_FUNC_glEnableClientState: {
        GLenum array;
        if (payload_size < 4) return -1;
        memcpy(&array, payload + 0, 4);
        glEnableClientState(array);
        return 0;
    }

    case GL_FUNC_glIndexub: {
        GLubyte c;
        if (payload_size < 1) return -1;
        memcpy(&c, payload + 0, 1);
        glIndexub(c);
        return 0;
    }

    case GL_FUNC_glPopClientAttrib: {
        glPopClientAttrib();
        return 0;
    }

    case GL_FUNC_glPushClientAttrib: {
        GLbitfield mask;
        if (payload_size < 4) return -1;
        memcpy(&mask, payload + 0, 4);
        glPushClientAttrib(mask);
        return 0;
    }

    case GL_FUNC_glCopyTexSubImage3D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint yoffset;
        if (payload_size < 16) return -1;
        memcpy(&yoffset, payload + 12, 4);
        GLint zoffset;
        if (payload_size < 20) return -1;
        memcpy(&zoffset, payload + 16, 4);
        GLint x;
        if (payload_size < 24) return -1;
        memcpy(&x, payload + 20, 4);
        GLint y;
        if (payload_size < 28) return -1;
        memcpy(&y, payload + 24, 4);
        GLsizei width;
        if (payload_size < 32) return -1;
        memcpy(&width, payload + 28, 4);
        GLsizei height;
        if (payload_size < 36) return -1;
        memcpy(&height, payload + 32, 4);
        glCopyTexSubImage3D(target, level, xoffset, yoffset, zoffset, x, y, width, height);
        return 0;
    }

    case GL_FUNC_glActiveTexture: {
        GLenum texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        glActiveTexture(texture);
        return 0;
    }

    case GL_FUNC_glSampleCoverage: {
        GLfloat value;
        if (payload_size < 4) return -1;
        memcpy(&value, payload + 0, 4);
        GLboolean invert;
        if (payload_size < 5) return -1;
        memcpy(&invert, payload + 4, 1);
        glSampleCoverage(value, invert);
        return 0;
    }

    case GL_FUNC_glClientActiveTexture: {
        GLenum texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        glClientActiveTexture(texture);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord1d: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLdouble s;
        if (payload_size < 12) return -1;
        memcpy(&s, payload + 4, 8);
        glMultiTexCoord1d(target, s);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord1f: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLfloat s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        glMultiTexCoord1f(target, s);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord1i: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        glMultiTexCoord1i(target, s);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord1s: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLshort s;
        if (payload_size < 6) return -1;
        memcpy(&s, payload + 4, 2);
        glMultiTexCoord1s(target, s);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord2d: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLdouble s;
        if (payload_size < 12) return -1;
        memcpy(&s, payload + 4, 8);
        GLdouble t;
        if (payload_size < 20) return -1;
        memcpy(&t, payload + 12, 8);
        glMultiTexCoord2d(target, s, t);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord2f: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLfloat s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        GLfloat t;
        if (payload_size < 12) return -1;
        memcpy(&t, payload + 8, 4);
        glMultiTexCoord2f(target, s, t);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord2i: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        GLint t;
        if (payload_size < 12) return -1;
        memcpy(&t, payload + 8, 4);
        glMultiTexCoord2i(target, s, t);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord2s: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLshort s;
        if (payload_size < 6) return -1;
        memcpy(&s, payload + 4, 2);
        GLshort t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 6, 2);
        glMultiTexCoord2s(target, s, t);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord3d: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLdouble s;
        if (payload_size < 12) return -1;
        memcpy(&s, payload + 4, 8);
        GLdouble t;
        if (payload_size < 20) return -1;
        memcpy(&t, payload + 12, 8);
        GLdouble r;
        if (payload_size < 28) return -1;
        memcpy(&r, payload + 20, 8);
        glMultiTexCoord3d(target, s, t, r);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord3f: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLfloat s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        GLfloat t;
        if (payload_size < 12) return -1;
        memcpy(&t, payload + 8, 4);
        GLfloat r;
        if (payload_size < 16) return -1;
        memcpy(&r, payload + 12, 4);
        glMultiTexCoord3f(target, s, t, r);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord3i: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        GLint t;
        if (payload_size < 12) return -1;
        memcpy(&t, payload + 8, 4);
        GLint r;
        if (payload_size < 16) return -1;
        memcpy(&r, payload + 12, 4);
        glMultiTexCoord3i(target, s, t, r);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord3s: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLshort s;
        if (payload_size < 6) return -1;
        memcpy(&s, payload + 4, 2);
        GLshort t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 6, 2);
        GLshort r;
        if (payload_size < 10) return -1;
        memcpy(&r, payload + 8, 2);
        glMultiTexCoord3s(target, s, t, r);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord4d: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLdouble s;
        if (payload_size < 12) return -1;
        memcpy(&s, payload + 4, 8);
        GLdouble t;
        if (payload_size < 20) return -1;
        memcpy(&t, payload + 12, 8);
        GLdouble r;
        if (payload_size < 28) return -1;
        memcpy(&r, payload + 20, 8);
        GLdouble q;
        if (payload_size < 36) return -1;
        memcpy(&q, payload + 28, 8);
        glMultiTexCoord4d(target, s, t, r, q);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord4f: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLfloat s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        GLfloat t;
        if (payload_size < 12) return -1;
        memcpy(&t, payload + 8, 4);
        GLfloat r;
        if (payload_size < 16) return -1;
        memcpy(&r, payload + 12, 4);
        GLfloat q;
        if (payload_size < 20) return -1;
        memcpy(&q, payload + 16, 4);
        glMultiTexCoord4f(target, s, t, r, q);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord4i: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLint s;
        if (payload_size < 8) return -1;
        memcpy(&s, payload + 4, 4);
        GLint t;
        if (payload_size < 12) return -1;
        memcpy(&t, payload + 8, 4);
        GLint r;
        if (payload_size < 16) return -1;
        memcpy(&r, payload + 12, 4);
        GLint q;
        if (payload_size < 20) return -1;
        memcpy(&q, payload + 16, 4);
        glMultiTexCoord4i(target, s, t, r, q);
        return 0;
    }

    case GL_FUNC_glMultiTexCoord4s: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLshort s;
        if (payload_size < 6) return -1;
        memcpy(&s, payload + 4, 2);
        GLshort t;
        if (payload_size < 8) return -1;
        memcpy(&t, payload + 6, 2);
        GLshort r;
        if (payload_size < 10) return -1;
        memcpy(&r, payload + 8, 2);
        GLshort q;
        if (payload_size < 12) return -1;
        memcpy(&q, payload + 10, 2);
        glMultiTexCoord4s(target, s, t, r, q);
        return 0;
    }

    case GL_FUNC_glBlendFuncSeparate: {
        GLenum sfactorRGB;
        if (payload_size < 4) return -1;
        memcpy(&sfactorRGB, payload + 0, 4);
        GLenum dfactorRGB;
        if (payload_size < 8) return -1;
        memcpy(&dfactorRGB, payload + 4, 4);
        GLenum sfactorAlpha;
        if (payload_size < 12) return -1;
        memcpy(&sfactorAlpha, payload + 8, 4);
        GLenum dfactorAlpha;
        if (payload_size < 16) return -1;
        memcpy(&dfactorAlpha, payload + 12, 4);
        glBlendFuncSeparate(sfactorRGB, dfactorRGB, sfactorAlpha, dfactorAlpha);
        return 0;
    }

    case GL_FUNC_glPointParameterf: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLfloat param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glPointParameterf(pname, param);
        return 0;
    }

    case GL_FUNC_glPointParameteri: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLint param;
        if (payload_size < 8) return -1;
        memcpy(&param, payload + 4, 4);
        glPointParameteri(pname, param);
        return 0;
    }

    case GL_FUNC_glFogCoordf: {
        GLfloat coord;
        if (payload_size < 4) return -1;
        memcpy(&coord, payload + 0, 4);
        glFogCoordf(coord);
        return 0;
    }

    case GL_FUNC_glFogCoordd: {
        GLdouble coord;
        if (payload_size < 8) return -1;
        memcpy(&coord, payload + 0, 8);
        glFogCoordd(coord);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3b: {
        GLbyte red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLbyte green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLbyte blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        glSecondaryColor3b(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3d: {
        GLdouble red;
        if (payload_size < 8) return -1;
        memcpy(&red, payload + 0, 8);
        GLdouble green;
        if (payload_size < 16) return -1;
        memcpy(&green, payload + 8, 8);
        GLdouble blue;
        if (payload_size < 24) return -1;
        memcpy(&blue, payload + 16, 8);
        glSecondaryColor3d(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3f: {
        GLfloat red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLfloat green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLfloat blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        glSecondaryColor3f(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3i: {
        GLint red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLint green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLint blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        glSecondaryColor3i(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3s: {
        GLshort red;
        if (payload_size < 2) return -1;
        memcpy(&red, payload + 0, 2);
        GLshort green;
        if (payload_size < 4) return -1;
        memcpy(&green, payload + 2, 2);
        GLshort blue;
        if (payload_size < 6) return -1;
        memcpy(&blue, payload + 4, 2);
        glSecondaryColor3s(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3ub: {
        GLubyte red;
        if (payload_size < 1) return -1;
        memcpy(&red, payload + 0, 1);
        GLubyte green;
        if (payload_size < 2) return -1;
        memcpy(&green, payload + 1, 1);
        GLubyte blue;
        if (payload_size < 3) return -1;
        memcpy(&blue, payload + 2, 1);
        glSecondaryColor3ub(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3ui: {
        GLuint red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLuint green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLuint blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        glSecondaryColor3ui(red, green, blue);
        return 0;
    }

    case GL_FUNC_glSecondaryColor3us: {
        GLushort red;
        if (payload_size < 2) return -1;
        memcpy(&red, payload + 0, 2);
        GLushort green;
        if (payload_size < 4) return -1;
        memcpy(&green, payload + 2, 2);
        GLushort blue;
        if (payload_size < 6) return -1;
        memcpy(&blue, payload + 4, 2);
        glSecondaryColor3us(red, green, blue);
        return 0;
    }

    case GL_FUNC_glWindowPos2d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        glWindowPos2d(x, y);
        return 0;
    }

    case GL_FUNC_glWindowPos2f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        glWindowPos2f(x, y);
        return 0;
    }

    case GL_FUNC_glWindowPos2i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        glWindowPos2i(x, y);
        return 0;
    }

    case GL_FUNC_glWindowPos2s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        glWindowPos2s(x, y);
        return 0;
    }

    case GL_FUNC_glWindowPos3d: {
        GLdouble x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 0, 8);
        GLdouble y;
        if (payload_size < 16) return -1;
        memcpy(&y, payload + 8, 8);
        GLdouble z;
        if (payload_size < 24) return -1;
        memcpy(&z, payload + 16, 8);
        glWindowPos3d(x, y, z);
        return 0;
    }

    case GL_FUNC_glWindowPos3f: {
        GLfloat x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLfloat y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLfloat z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glWindowPos3f(x, y, z);
        return 0;
    }

    case GL_FUNC_glWindowPos3i: {
        GLint x;
        if (payload_size < 4) return -1;
        memcpy(&x, payload + 0, 4);
        GLint y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 4, 4);
        GLint z;
        if (payload_size < 12) return -1;
        memcpy(&z, payload + 8, 4);
        glWindowPos3i(x, y, z);
        return 0;
    }

    case GL_FUNC_glWindowPos3s: {
        GLshort x;
        if (payload_size < 2) return -1;
        memcpy(&x, payload + 0, 2);
        GLshort y;
        if (payload_size < 4) return -1;
        memcpy(&y, payload + 2, 2);
        GLshort z;
        if (payload_size < 6) return -1;
        memcpy(&z, payload + 4, 2);
        glWindowPos3s(x, y, z);
        return 0;
    }

    case GL_FUNC_glBlendColor: {
        GLfloat red;
        if (payload_size < 4) return -1;
        memcpy(&red, payload + 0, 4);
        GLfloat green;
        if (payload_size < 8) return -1;
        memcpy(&green, payload + 4, 4);
        GLfloat blue;
        if (payload_size < 12) return -1;
        memcpy(&blue, payload + 8, 4);
        GLfloat alpha;
        if (payload_size < 16) return -1;
        memcpy(&alpha, payload + 12, 4);
        glBlendColor(red, green, blue, alpha);
        return 0;
    }

    case GL_FUNC_glBlendEquation: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glBlendEquation(mode);
        return 0;
    }

    case GL_FUNC_glBeginQuery: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint id;
        if (payload_size < 8) return -1;
        memcpy(&id, payload + 4, 4);
        glBeginQuery(target, id);
        return 0;
    }

    case GL_FUNC_glEndQuery: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        glEndQuery(target);
        return 0;
    }

    case GL_FUNC_glBindBuffer: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        glBindBuffer(target, buffer);
        return 0;
    }

    case GL_FUNC_glBlendEquationSeparate: {
        GLenum modeRGB;
        if (payload_size < 4) return -1;
        memcpy(&modeRGB, payload + 0, 4);
        GLenum modeAlpha;
        if (payload_size < 8) return -1;
        memcpy(&modeAlpha, payload + 4, 4);
        glBlendEquationSeparate(modeRGB, modeAlpha);
        return 0;
    }

    case GL_FUNC_glStencilOpSeparate: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLenum sfail;
        if (payload_size < 8) return -1;
        memcpy(&sfail, payload + 4, 4);
        GLenum dpfail;
        if (payload_size < 12) return -1;
        memcpy(&dpfail, payload + 8, 4);
        GLenum dppass;
        if (payload_size < 16) return -1;
        memcpy(&dppass, payload + 12, 4);
        glStencilOpSeparate(face, sfail, dpfail, dppass);
        return 0;
    }

    case GL_FUNC_glStencilFuncSeparate: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLenum func;
        if (payload_size < 8) return -1;
        memcpy(&func, payload + 4, 4);
        GLint ref;
        if (payload_size < 12) return -1;
        memcpy(&ref, payload + 8, 4);
        GLuint mask;
        if (payload_size < 16) return -1;
        memcpy(&mask, payload + 12, 4);
        glStencilFuncSeparate(face, func, ref, mask);
        return 0;
    }

    case GL_FUNC_glStencilMaskSeparate: {
        GLenum face;
        if (payload_size < 4) return -1;
        memcpy(&face, payload + 0, 4);
        GLuint mask;
        if (payload_size < 8) return -1;
        memcpy(&mask, payload + 4, 4);
        glStencilMaskSeparate(face, mask);
        return 0;
    }

    case GL_FUNC_glAttachShader: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLuint shader;
        if (payload_size < 8) return -1;
        memcpy(&shader, payload + 4, 4);
        glAttachShader(program, shader);
        return 0;
    }

    case GL_FUNC_glCompileShader: {
        GLuint shader;
        if (payload_size < 4) return -1;
        memcpy(&shader, payload + 0, 4);
        glCompileShader(shader);
        return 0;
    }

    case GL_FUNC_glDeleteProgram: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        glDeleteProgram(program);
        return 0;
    }

    case GL_FUNC_glDeleteShader: {
        GLuint shader;
        if (payload_size < 4) return -1;
        memcpy(&shader, payload + 0, 4);
        glDeleteShader(shader);
        return 0;
    }

    case GL_FUNC_glDetachShader: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLuint shader;
        if (payload_size < 8) return -1;
        memcpy(&shader, payload + 4, 4);
        glDetachShader(program, shader);
        return 0;
    }

    case GL_FUNC_glDisableVertexAttribArray: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        glDisableVertexAttribArray(index);
        return 0;
    }

    case GL_FUNC_glEnableVertexAttribArray: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        glEnableVertexAttribArray(index);
        return 0;
    }

    case GL_FUNC_glLinkProgram: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        glLinkProgram(program);
        return 0;
    }

    case GL_FUNC_glUseProgram: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        glUseProgram(program);
        return 0;
    }

    case GL_FUNC_glUniform1f: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLfloat v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        glUniform1f(location, v0);
        return 0;
    }

    case GL_FUNC_glUniform2f: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLfloat v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLfloat v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        glUniform2f(location, v0, v1);
        return 0;
    }

    case GL_FUNC_glUniform3f: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLfloat v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLfloat v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        GLfloat v2;
        if (payload_size < 16) return -1;
        memcpy(&v2, payload + 12, 4);
        glUniform3f(location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glUniform4f: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLfloat v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLfloat v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        GLfloat v2;
        if (payload_size < 16) return -1;
        memcpy(&v2, payload + 12, 4);
        GLfloat v3;
        if (payload_size < 20) return -1;
        memcpy(&v3, payload + 16, 4);
        glUniform4f(location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glUniform1i: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        glUniform1i(location, v0);
        return 0;
    }

    case GL_FUNC_glUniform2i: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLint v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        glUniform2i(location, v0, v1);
        return 0;
    }

    case GL_FUNC_glUniform3i: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLint v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        GLint v2;
        if (payload_size < 16) return -1;
        memcpy(&v2, payload + 12, 4);
        glUniform3i(location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glUniform4i: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLint v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        GLint v2;
        if (payload_size < 16) return -1;
        memcpy(&v2, payload + 12, 4);
        GLint v3;
        if (payload_size < 20) return -1;
        memcpy(&v3, payload + 16, 4);
        glUniform4i(location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glValidateProgram: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        glValidateProgram(program);
        return 0;
    }

    case GL_FUNC_glVertexAttrib1d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        glVertexAttrib1d(index, x);
        return 0;
    }

    case GL_FUNC_glVertexAttrib1f: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLfloat x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        glVertexAttrib1f(index, x);
        return 0;
    }

    case GL_FUNC_glVertexAttrib1s: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLshort x;
        if (payload_size < 6) return -1;
        memcpy(&x, payload + 4, 2);
        glVertexAttrib1s(index, x);
        return 0;
    }

    case GL_FUNC_glVertexAttrib2d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        glVertexAttrib2d(index, x, y);
        return 0;
    }

    case GL_FUNC_glVertexAttrib2f: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLfloat x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLfloat y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        glVertexAttrib2f(index, x, y);
        return 0;
    }

    case GL_FUNC_glVertexAttrib2s: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLshort x;
        if (payload_size < 6) return -1;
        memcpy(&x, payload + 4, 2);
        GLshort y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 6, 2);
        glVertexAttrib2s(index, x, y);
        return 0;
    }

    case GL_FUNC_glVertexAttrib3d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        GLdouble z;
        if (payload_size < 28) return -1;
        memcpy(&z, payload + 20, 8);
        glVertexAttrib3d(index, x, y, z);
        return 0;
    }

    case GL_FUNC_glVertexAttrib3f: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLfloat x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLfloat y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLfloat z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        glVertexAttrib3f(index, x, y, z);
        return 0;
    }

    case GL_FUNC_glVertexAttrib3s: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLshort x;
        if (payload_size < 6) return -1;
        memcpy(&x, payload + 4, 2);
        GLshort y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 6, 2);
        GLshort z;
        if (payload_size < 10) return -1;
        memcpy(&z, payload + 8, 2);
        glVertexAttrib3s(index, x, y, z);
        return 0;
    }

    case GL_FUNC_glVertexAttrib4Nub: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLubyte x;
        if (payload_size < 5) return -1;
        memcpy(&x, payload + 4, 1);
        GLubyte y;
        if (payload_size < 6) return -1;
        memcpy(&y, payload + 5, 1);
        GLubyte z;
        if (payload_size < 7) return -1;
        memcpy(&z, payload + 6, 1);
        GLubyte w;
        if (payload_size < 8) return -1;
        memcpy(&w, payload + 7, 1);
        glVertexAttrib4Nub(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertexAttrib4d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        GLdouble z;
        if (payload_size < 28) return -1;
        memcpy(&z, payload + 20, 8);
        GLdouble w;
        if (payload_size < 36) return -1;
        memcpy(&w, payload + 28, 8);
        glVertexAttrib4d(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertexAttrib4f: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLfloat x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLfloat y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLfloat z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        GLfloat w;
        if (payload_size < 20) return -1;
        memcpy(&w, payload + 16, 4);
        glVertexAttrib4f(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertexAttrib4s: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLshort x;
        if (payload_size < 6) return -1;
        memcpy(&x, payload + 4, 2);
        GLshort y;
        if (payload_size < 8) return -1;
        memcpy(&y, payload + 6, 2);
        GLshort z;
        if (payload_size < 10) return -1;
        memcpy(&z, payload + 8, 2);
        GLshort w;
        if (payload_size < 12) return -1;
        memcpy(&w, payload + 10, 2);
        glVertexAttrib4s(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glColorMaski: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLboolean r;
        if (payload_size < 5) return -1;
        memcpy(&r, payload + 4, 1);
        GLboolean g;
        if (payload_size < 6) return -1;
        memcpy(&g, payload + 5, 1);
        GLboolean b;
        if (payload_size < 7) return -1;
        memcpy(&b, payload + 6, 1);
        GLboolean a;
        if (payload_size < 8) return -1;
        memcpy(&a, payload + 7, 1);
        glColorMaski(index, r, g, b, a);
        return 0;
    }

    case GL_FUNC_glEnablei: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        glEnablei(target, index);
        return 0;
    }

    case GL_FUNC_glDisablei: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        glDisablei(target, index);
        return 0;
    }

    case GL_FUNC_glBeginTransformFeedback: {
        GLenum primitiveMode;
        if (payload_size < 4) return -1;
        memcpy(&primitiveMode, payload + 0, 4);
        glBeginTransformFeedback(primitiveMode);
        return 0;
    }

    case GL_FUNC_glEndTransformFeedback: {
        glEndTransformFeedback();
        return 0;
    }

    case GL_FUNC_glBindBufferRange: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        GLsizeiptr size;
        if (payload_size < 28) return -1;
        memcpy(&size, payload + 20, 8);
        glBindBufferRange(target, index, buffer, offset, size);
        return 0;
    }

    case GL_FUNC_glBindBufferBase: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        glBindBufferBase(target, index, buffer);
        return 0;
    }

    case GL_FUNC_glClampColor: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum clamp;
        if (payload_size < 8) return -1;
        memcpy(&clamp, payload + 4, 4);
        glClampColor(target, clamp);
        return 0;
    }

    case GL_FUNC_glBeginConditionalRender: {
        GLuint id;
        if (payload_size < 4) return -1;
        memcpy(&id, payload + 0, 4);
        GLenum mode;
        if (payload_size < 8) return -1;
        memcpy(&mode, payload + 4, 4);
        glBeginConditionalRender(id, mode);
        return 0;
    }

    case GL_FUNC_glEndConditionalRender: {
        glEndConditionalRender();
        return 0;
    }

    case GL_FUNC_glVertexAttribI1i: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        glVertexAttribI1i(index, x);
        return 0;
    }

    case GL_FUNC_glVertexAttribI2i: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLint y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        glVertexAttribI2i(index, x, y);
        return 0;
    }

    case GL_FUNC_glVertexAttribI3i: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLint y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLint z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        glVertexAttribI3i(index, x, y, z);
        return 0;
    }

    case GL_FUNC_glVertexAttribI4i: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLint y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLint z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        GLint w;
        if (payload_size < 20) return -1;
        memcpy(&w, payload + 16, 4);
        glVertexAttribI4i(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glVertexAttribI1ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLuint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        glVertexAttribI1ui(index, x);
        return 0;
    }

    case GL_FUNC_glVertexAttribI2ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLuint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLuint y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        glVertexAttribI2ui(index, x, y);
        return 0;
    }

    case GL_FUNC_glVertexAttribI3ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLuint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLuint y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLuint z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        glVertexAttribI3ui(index, x, y, z);
        return 0;
    }

    case GL_FUNC_glVertexAttribI4ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLuint x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLuint y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLuint z;
        if (payload_size < 16) return -1;
        memcpy(&z, payload + 12, 4);
        GLuint w;
        if (payload_size < 20) return -1;
        memcpy(&w, payload + 16, 4);
        glVertexAttribI4ui(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glUniform1ui: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLuint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        glUniform1ui(location, v0);
        return 0;
    }

    case GL_FUNC_glUniform2ui: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLuint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLuint v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        glUniform2ui(location, v0, v1);
        return 0;
    }

    case GL_FUNC_glUniform3ui: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLuint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLuint v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        GLuint v2;
        if (payload_size < 16) return -1;
        memcpy(&v2, payload + 12, 4);
        glUniform3ui(location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glUniform4ui: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLuint v0;
        if (payload_size < 8) return -1;
        memcpy(&v0, payload + 4, 4);
        GLuint v1;
        if (payload_size < 12) return -1;
        memcpy(&v1, payload + 8, 4);
        GLuint v2;
        if (payload_size < 16) return -1;
        memcpy(&v2, payload + 12, 4);
        GLuint v3;
        if (payload_size < 20) return -1;
        memcpy(&v3, payload + 16, 4);
        glUniform4ui(location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glClearBufferfi: {
        GLenum buffer;
        if (payload_size < 4) return -1;
        memcpy(&buffer, payload + 0, 4);
        GLint drawbuffer;
        if (payload_size < 8) return -1;
        memcpy(&drawbuffer, payload + 4, 4);
        GLfloat depth;
        if (payload_size < 12) return -1;
        memcpy(&depth, payload + 8, 4);
        GLint stencil;
        if (payload_size < 16) return -1;
        memcpy(&stencil, payload + 12, 4);
        glClearBufferfi(buffer, drawbuffer, depth, stencil);
        return 0;
    }

    case GL_FUNC_glBindRenderbuffer: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint renderbuffer;
        if (payload_size < 8) return -1;
        memcpy(&renderbuffer, payload + 4, 4);
        glBindRenderbuffer(target, renderbuffer);
        return 0;
    }

    case GL_FUNC_glRenderbufferStorage: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum internalformat;
        if (payload_size < 8) return -1;
        memcpy(&internalformat, payload + 4, 4);
        GLsizei width;
        if (payload_size < 12) return -1;
        memcpy(&width, payload + 8, 4);
        GLsizei height;
        if (payload_size < 16) return -1;
        memcpy(&height, payload + 12, 4);
        glRenderbufferStorage(target, internalformat, width, height);
        return 0;
    }

    case GL_FUNC_glBindFramebuffer: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint framebuffer;
        if (payload_size < 8) return -1;
        memcpy(&framebuffer, payload + 4, 4);
        glBindFramebuffer(target, framebuffer);
        return 0;
    }

    case GL_FUNC_glFramebufferTexture1D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLenum textarget;
        if (payload_size < 12) return -1;
        memcpy(&textarget, payload + 8, 4);
        GLuint texture;
        if (payload_size < 16) return -1;
        memcpy(&texture, payload + 12, 4);
        GLint level;
        if (payload_size < 20) return -1;
        memcpy(&level, payload + 16, 4);
        glFramebufferTexture1D(target, attachment, textarget, texture, level);
        return 0;
    }

    case GL_FUNC_glFramebufferTexture2D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLenum textarget;
        if (payload_size < 12) return -1;
        memcpy(&textarget, payload + 8, 4);
        GLuint texture;
        if (payload_size < 16) return -1;
        memcpy(&texture, payload + 12, 4);
        GLint level;
        if (payload_size < 20) return -1;
        memcpy(&level, payload + 16, 4);
        glFramebufferTexture2D(target, attachment, textarget, texture, level);
        return 0;
    }

    case GL_FUNC_glFramebufferTexture3D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLenum textarget;
        if (payload_size < 12) return -1;
        memcpy(&textarget, payload + 8, 4);
        GLuint texture;
        if (payload_size < 16) return -1;
        memcpy(&texture, payload + 12, 4);
        GLint level;
        if (payload_size < 20) return -1;
        memcpy(&level, payload + 16, 4);
        GLint zoffset;
        if (payload_size < 24) return -1;
        memcpy(&zoffset, payload + 20, 4);
        glFramebufferTexture3D(target, attachment, textarget, texture, level, zoffset);
        return 0;
    }

    case GL_FUNC_glFramebufferRenderbuffer: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLenum renderbuffertarget;
        if (payload_size < 12) return -1;
        memcpy(&renderbuffertarget, payload + 8, 4);
        GLuint renderbuffer;
        if (payload_size < 16) return -1;
        memcpy(&renderbuffer, payload + 12, 4);
        glFramebufferRenderbuffer(target, attachment, renderbuffertarget, renderbuffer);
        return 0;
    }

    case GL_FUNC_glGenerateMipmap: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        glGenerateMipmap(target);
        return 0;
    }

    case GL_FUNC_glBlitFramebuffer: {
        GLint srcX0;
        if (payload_size < 4) return -1;
        memcpy(&srcX0, payload + 0, 4);
        GLint srcY0;
        if (payload_size < 8) return -1;
        memcpy(&srcY0, payload + 4, 4);
        GLint srcX1;
        if (payload_size < 12) return -1;
        memcpy(&srcX1, payload + 8, 4);
        GLint srcY1;
        if (payload_size < 16) return -1;
        memcpy(&srcY1, payload + 12, 4);
        GLint dstX0;
        if (payload_size < 20) return -1;
        memcpy(&dstX0, payload + 16, 4);
        GLint dstY0;
        if (payload_size < 24) return -1;
        memcpy(&dstY0, payload + 20, 4);
        GLint dstX1;
        if (payload_size < 28) return -1;
        memcpy(&dstX1, payload + 24, 4);
        GLint dstY1;
        if (payload_size < 32) return -1;
        memcpy(&dstY1, payload + 28, 4);
        GLbitfield mask;
        if (payload_size < 36) return -1;
        memcpy(&mask, payload + 32, 4);
        GLenum filter;
        if (payload_size < 40) return -1;
        memcpy(&filter, payload + 36, 4);
        glBlitFramebuffer(srcX0, srcY0, srcX1, srcY1, dstX0, dstY0, dstX1, dstY1, mask, filter);
        return 0;
    }

    case GL_FUNC_glRenderbufferStorageMultisample: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        glRenderbufferStorageMultisample(target, samples, internalformat, width, height);
        return 0;
    }

    case GL_FUNC_glFramebufferTextureLayer: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLuint texture;
        if (payload_size < 12) return -1;
        memcpy(&texture, payload + 8, 4);
        GLint level;
        if (payload_size < 16) return -1;
        memcpy(&level, payload + 12, 4);
        GLint layer;
        if (payload_size < 20) return -1;
        memcpy(&layer, payload + 16, 4);
        glFramebufferTextureLayer(target, attachment, texture, level, layer);
        return 0;
    }

    case GL_FUNC_glFlushMappedBufferRange: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLintptr offset;
        if (payload_size < 12) return -1;
        memcpy(&offset, payload + 4, 8);
        GLsizeiptr length;
        if (payload_size < 20) return -1;
        memcpy(&length, payload + 12, 8);
        glFlushMappedBufferRange(target, offset, length);
        return 0;
    }

    case GL_FUNC_glBindVertexArray: {
        GLuint array;
        if (payload_size < 4) return -1;
        memcpy(&array, payload + 0, 4);
        glBindVertexArray(array);
        return 0;
    }

    case GL_FUNC_glDrawArraysInstanced: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLint first;
        if (payload_size < 8) return -1;
        memcpy(&first, payload + 4, 4);
        GLsizei count;
        if (payload_size < 12) return -1;
        memcpy(&count, payload + 8, 4);
        GLsizei instancecount;
        if (payload_size < 16) return -1;
        memcpy(&instancecount, payload + 12, 4);
        glDrawArraysInstanced(mode, first, count, instancecount);
        return 0;
    }

    case GL_FUNC_glTexBuffer: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum internalformat;
        if (payload_size < 8) return -1;
        memcpy(&internalformat, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        glTexBuffer(target, internalformat, buffer);
        return 0;
    }

    case GL_FUNC_glPrimitiveRestartIndex: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        glPrimitiveRestartIndex(index);
        return 0;
    }

    case GL_FUNC_glCopyBufferSubData: {
        GLenum readTarget;
        if (payload_size < 4) return -1;
        memcpy(&readTarget, payload + 0, 4);
        GLenum writeTarget;
        if (payload_size < 8) return -1;
        memcpy(&writeTarget, payload + 4, 4);
        GLintptr readOffset;
        if (payload_size < 16) return -1;
        memcpy(&readOffset, payload + 8, 8);
        GLintptr writeOffset;
        if (payload_size < 24) return -1;
        memcpy(&writeOffset, payload + 16, 8);
        GLsizeiptr size;
        if (payload_size < 32) return -1;
        memcpy(&size, payload + 24, 8);
        glCopyBufferSubData(readTarget, writeTarget, readOffset, writeOffset, size);
        return 0;
    }

    case GL_FUNC_glUniformBlockBinding: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLuint uniformBlockIndex;
        if (payload_size < 8) return -1;
        memcpy(&uniformBlockIndex, payload + 4, 4);
        GLuint uniformBlockBinding;
        if (payload_size < 12) return -1;
        memcpy(&uniformBlockBinding, payload + 8, 4);
        glUniformBlockBinding(program, uniformBlockIndex, uniformBlockBinding);
        return 0;
    }

    case GL_FUNC_glProvokingVertex: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        glProvokingVertex(mode);
        return 0;
    }

    case GL_FUNC_glFramebufferTexture: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLuint texture;
        if (payload_size < 12) return -1;
        memcpy(&texture, payload + 8, 4);
        GLint level;
        if (payload_size < 16) return -1;
        memcpy(&level, payload + 12, 4);
        glFramebufferTexture(target, attachment, texture, level);
        return 0;
    }

    case GL_FUNC_glTexImage2DMultisample: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLboolean fixedsamplelocations;
        if (payload_size < 21) return -1;
        memcpy(&fixedsamplelocations, payload + 20, 1);
        glTexImage2DMultisample(target, samples, internalformat, width, height, fixedsamplelocations);
        return 0;
    }

    case GL_FUNC_glTexImage3DMultisample: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLsizei depth;
        if (payload_size < 24) return -1;
        memcpy(&depth, payload + 20, 4);
        GLboolean fixedsamplelocations;
        if (payload_size < 25) return -1;
        memcpy(&fixedsamplelocations, payload + 24, 1);
        glTexImage3DMultisample(target, samples, internalformat, width, height, depth, fixedsamplelocations);
        return 0;
    }

    case GL_FUNC_glSampleMaski: {
        GLuint maskNumber;
        if (payload_size < 4) return -1;
        memcpy(&maskNumber, payload + 0, 4);
        GLbitfield mask;
        if (payload_size < 8) return -1;
        memcpy(&mask, payload + 4, 4);
        glSampleMaski(maskNumber, mask);
        return 0;
    }

    case GL_FUNC_glBindSampler: {
        GLuint unit;
        if (payload_size < 4) return -1;
        memcpy(&unit, payload + 0, 4);
        GLuint sampler;
        if (payload_size < 8) return -1;
        memcpy(&sampler, payload + 4, 4);
        glBindSampler(unit, sampler);
        return 0;
    }

    case GL_FUNC_glSamplerParameteri: {
        GLuint sampler;
        if (payload_size < 4) return -1;
        memcpy(&sampler, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glSamplerParameteri(sampler, pname, param);
        return 0;
    }

    case GL_FUNC_glSamplerParameterf: {
        GLuint sampler;
        if (payload_size < 4) return -1;
        memcpy(&sampler, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glSamplerParameterf(sampler, pname, param);
        return 0;
    }

    case GL_FUNC_glQueryCounter: {
        GLuint id;
        if (payload_size < 4) return -1;
        memcpy(&id, payload + 0, 4);
        GLenum target;
        if (payload_size < 8) return -1;
        memcpy(&target, payload + 4, 4);
        glQueryCounter(id, target);
        return 0;
    }

    case GL_FUNC_glVertexAttribDivisor: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLuint divisor;
        if (payload_size < 8) return -1;
        memcpy(&divisor, payload + 4, 4);
        glVertexAttribDivisor(index, divisor);
        return 0;
    }

    case GL_FUNC_glVertexAttribP1ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLboolean normalized;
        if (payload_size < 9) return -1;
        memcpy(&normalized, payload + 8, 1);
        GLuint value;
        if (payload_size < 13) return -1;
        memcpy(&value, payload + 9, 4);
        glVertexAttribP1ui(index, type, normalized, value);
        return 0;
    }

    case GL_FUNC_glVertexAttribP2ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLboolean normalized;
        if (payload_size < 9) return -1;
        memcpy(&normalized, payload + 8, 1);
        GLuint value;
        if (payload_size < 13) return -1;
        memcpy(&value, payload + 9, 4);
        glVertexAttribP2ui(index, type, normalized, value);
        return 0;
    }

    case GL_FUNC_glVertexAttribP3ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLboolean normalized;
        if (payload_size < 9) return -1;
        memcpy(&normalized, payload + 8, 1);
        GLuint value;
        if (payload_size < 13) return -1;
        memcpy(&value, payload + 9, 4);
        glVertexAttribP3ui(index, type, normalized, value);
        return 0;
    }

    case GL_FUNC_glVertexAttribP4ui: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLboolean normalized;
        if (payload_size < 9) return -1;
        memcpy(&normalized, payload + 8, 1);
        GLuint value;
        if (payload_size < 13) return -1;
        memcpy(&value, payload + 9, 4);
        glVertexAttribP4ui(index, type, normalized, value);
        return 0;
    }

    case GL_FUNC_glVertexP2ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint value;
        if (payload_size < 8) return -1;
        memcpy(&value, payload + 4, 4);
        glVertexP2ui(type, value);
        return 0;
    }

    case GL_FUNC_glVertexP3ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint value;
        if (payload_size < 8) return -1;
        memcpy(&value, payload + 4, 4);
        glVertexP3ui(type, value);
        return 0;
    }

    case GL_FUNC_glVertexP4ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint value;
        if (payload_size < 8) return -1;
        memcpy(&value, payload + 4, 4);
        glVertexP4ui(type, value);
        return 0;
    }

    case GL_FUNC_glTexCoordP1ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint coords;
        if (payload_size < 8) return -1;
        memcpy(&coords, payload + 4, 4);
        glTexCoordP1ui(type, coords);
        return 0;
    }

    case GL_FUNC_glTexCoordP2ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint coords;
        if (payload_size < 8) return -1;
        memcpy(&coords, payload + 4, 4);
        glTexCoordP2ui(type, coords);
        return 0;
    }

    case GL_FUNC_glTexCoordP3ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint coords;
        if (payload_size < 8) return -1;
        memcpy(&coords, payload + 4, 4);
        glTexCoordP3ui(type, coords);
        return 0;
    }

    case GL_FUNC_glTexCoordP4ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint coords;
        if (payload_size < 8) return -1;
        memcpy(&coords, payload + 4, 4);
        glTexCoordP4ui(type, coords);
        return 0;
    }

    case GL_FUNC_glMultiTexCoordP1ui: {
        GLenum texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLuint coords;
        if (payload_size < 12) return -1;
        memcpy(&coords, payload + 8, 4);
        glMultiTexCoordP1ui(texture, type, coords);
        return 0;
    }

    case GL_FUNC_glMultiTexCoordP2ui: {
        GLenum texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLuint coords;
        if (payload_size < 12) return -1;
        memcpy(&coords, payload + 8, 4);
        glMultiTexCoordP2ui(texture, type, coords);
        return 0;
    }

    case GL_FUNC_glMultiTexCoordP3ui: {
        GLenum texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLuint coords;
        if (payload_size < 12) return -1;
        memcpy(&coords, payload + 8, 4);
        glMultiTexCoordP3ui(texture, type, coords);
        return 0;
    }

    case GL_FUNC_glMultiTexCoordP4ui: {
        GLenum texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum type;
        if (payload_size < 8) return -1;
        memcpy(&type, payload + 4, 4);
        GLuint coords;
        if (payload_size < 12) return -1;
        memcpy(&coords, payload + 8, 4);
        glMultiTexCoordP4ui(texture, type, coords);
        return 0;
    }

    case GL_FUNC_glNormalP3ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint coords;
        if (payload_size < 8) return -1;
        memcpy(&coords, payload + 4, 4);
        glNormalP3ui(type, coords);
        return 0;
    }

    case GL_FUNC_glColorP3ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint color;
        if (payload_size < 8) return -1;
        memcpy(&color, payload + 4, 4);
        glColorP3ui(type, color);
        return 0;
    }

    case GL_FUNC_glColorP4ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint color;
        if (payload_size < 8) return -1;
        memcpy(&color, payload + 4, 4);
        glColorP4ui(type, color);
        return 0;
    }

    case GL_FUNC_glSecondaryColorP3ui: {
        GLenum type;
        if (payload_size < 4) return -1;
        memcpy(&type, payload + 0, 4);
        GLuint color;
        if (payload_size < 8) return -1;
        memcpy(&color, payload + 4, 4);
        glSecondaryColorP3ui(type, color);
        return 0;
    }

    case GL_FUNC_glMinSampleShading: {
        GLfloat value;
        if (payload_size < 4) return -1;
        memcpy(&value, payload + 0, 4);
        glMinSampleShading(value);
        return 0;
    }

    case GL_FUNC_glBlendEquationi: {
        GLuint buf;
        if (payload_size < 4) return -1;
        memcpy(&buf, payload + 0, 4);
        GLenum mode;
        if (payload_size < 8) return -1;
        memcpy(&mode, payload + 4, 4);
        glBlendEquationi(buf, mode);
        return 0;
    }

    case GL_FUNC_glBlendEquationSeparatei: {
        GLuint buf;
        if (payload_size < 4) return -1;
        memcpy(&buf, payload + 0, 4);
        GLenum modeRGB;
        if (payload_size < 8) return -1;
        memcpy(&modeRGB, payload + 4, 4);
        GLenum modeAlpha;
        if (payload_size < 12) return -1;
        memcpy(&modeAlpha, payload + 8, 4);
        glBlendEquationSeparatei(buf, modeRGB, modeAlpha);
        return 0;
    }

    case GL_FUNC_glBlendFunci: {
        GLuint buf;
        if (payload_size < 4) return -1;
        memcpy(&buf, payload + 0, 4);
        GLenum src;
        if (payload_size < 8) return -1;
        memcpy(&src, payload + 4, 4);
        GLenum dst;
        if (payload_size < 12) return -1;
        memcpy(&dst, payload + 8, 4);
        glBlendFunci(buf, src, dst);
        return 0;
    }

    case GL_FUNC_glBlendFuncSeparatei: {
        GLuint buf;
        if (payload_size < 4) return -1;
        memcpy(&buf, payload + 0, 4);
        GLenum srcRGB;
        if (payload_size < 8) return -1;
        memcpy(&srcRGB, payload + 4, 4);
        GLenum dstRGB;
        if (payload_size < 12) return -1;
        memcpy(&dstRGB, payload + 8, 4);
        GLenum srcAlpha;
        if (payload_size < 16) return -1;
        memcpy(&srcAlpha, payload + 12, 4);
        GLenum dstAlpha;
        if (payload_size < 20) return -1;
        memcpy(&dstAlpha, payload + 16, 4);
        glBlendFuncSeparatei(buf, srcRGB, dstRGB, srcAlpha, dstAlpha);
        return 0;
    }

    case GL_FUNC_glUniform1d: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        glUniform1d(location, x);
        return 0;
    }

    case GL_FUNC_glUniform2d: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        glUniform2d(location, x, y);
        return 0;
    }

    case GL_FUNC_glUniform3d: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        GLdouble z;
        if (payload_size < 28) return -1;
        memcpy(&z, payload + 20, 8);
        glUniform3d(location, x, y, z);
        return 0;
    }

    case GL_FUNC_glUniform4d: {
        GLint location;
        if (payload_size < 4) return -1;
        memcpy(&location, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        GLdouble z;
        if (payload_size < 28) return -1;
        memcpy(&z, payload + 20, 8);
        GLdouble w;
        if (payload_size < 36) return -1;
        memcpy(&w, payload + 28, 8);
        glUniform4d(location, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glPatchParameteri: {
        GLenum pname;
        if (payload_size < 4) return -1;
        memcpy(&pname, payload + 0, 4);
        GLint value;
        if (payload_size < 8) return -1;
        memcpy(&value, payload + 4, 4);
        glPatchParameteri(pname, value);
        return 0;
    }

    case GL_FUNC_glBindTransformFeedback: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint id;
        if (payload_size < 8) return -1;
        memcpy(&id, payload + 4, 4);
        glBindTransformFeedback(target, id);
        return 0;
    }

    case GL_FUNC_glPauseTransformFeedback: {
        glPauseTransformFeedback();
        return 0;
    }

    case GL_FUNC_glResumeTransformFeedback: {
        glResumeTransformFeedback();
        return 0;
    }

    case GL_FUNC_glDrawTransformFeedback: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLuint id;
        if (payload_size < 8) return -1;
        memcpy(&id, payload + 4, 4);
        glDrawTransformFeedback(mode, id);
        return 0;
    }

    case GL_FUNC_glDrawTransformFeedbackStream: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLuint id;
        if (payload_size < 8) return -1;
        memcpy(&id, payload + 4, 4);
        GLuint stream;
        if (payload_size < 12) return -1;
        memcpy(&stream, payload + 8, 4);
        glDrawTransformFeedbackStream(mode, id, stream);
        return 0;
    }

    case GL_FUNC_glBeginQueryIndexed: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        GLuint id;
        if (payload_size < 12) return -1;
        memcpy(&id, payload + 8, 4);
        glBeginQueryIndexed(target, index, id);
        return 0;
    }

    case GL_FUNC_glEndQueryIndexed: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        glEndQueryIndexed(target, index);
        return 0;
    }

    case GL_FUNC_glReleaseShaderCompiler: {
        glReleaseShaderCompiler();
        return 0;
    }

    case GL_FUNC_glDepthRangef: {
        GLfloat n;
        if (payload_size < 4) return -1;
        memcpy(&n, payload + 0, 4);
        GLfloat f;
        if (payload_size < 8) return -1;
        memcpy(&f, payload + 4, 4);
        glDepthRangef(n, f);
        return 0;
    }

    case GL_FUNC_glClearDepthf: {
        GLfloat d;
        if (payload_size < 4) return -1;
        memcpy(&d, payload + 0, 4);
        glClearDepthf(d);
        return 0;
    }

    case GL_FUNC_glProgramParameteri: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint value;
        if (payload_size < 12) return -1;
        memcpy(&value, payload + 8, 4);
        glProgramParameteri(program, pname, value);
        return 0;
    }

    case GL_FUNC_glUseProgramStages: {
        GLuint pipeline;
        if (payload_size < 4) return -1;
        memcpy(&pipeline, payload + 0, 4);
        GLbitfield stages;
        if (payload_size < 8) return -1;
        memcpy(&stages, payload + 4, 4);
        GLuint program;
        if (payload_size < 12) return -1;
        memcpy(&program, payload + 8, 4);
        glUseProgramStages(pipeline, stages, program);
        return 0;
    }

    case GL_FUNC_glActiveShaderProgram: {
        GLuint pipeline;
        if (payload_size < 4) return -1;
        memcpy(&pipeline, payload + 0, 4);
        GLuint program;
        if (payload_size < 8) return -1;
        memcpy(&program, payload + 4, 4);
        glActiveShaderProgram(pipeline, program);
        return 0;
    }

    case GL_FUNC_glBindProgramPipeline: {
        GLuint pipeline;
        if (payload_size < 4) return -1;
        memcpy(&pipeline, payload + 0, 4);
        glBindProgramPipeline(pipeline);
        return 0;
    }

    case GL_FUNC_glProgramUniform1i: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        glProgramUniform1i(program, location, v0);
        return 0;
    }

    case GL_FUNC_glProgramUniform1f: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLfloat v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        glProgramUniform1f(program, location, v0);
        return 0;
    }

    case GL_FUNC_glProgramUniform1d: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLdouble v0;
        if (payload_size < 16) return -1;
        memcpy(&v0, payload + 8, 8);
        glProgramUniform1d(program, location, v0);
        return 0;
    }

    case GL_FUNC_glProgramUniform1ui: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLuint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        glProgramUniform1ui(program, location, v0);
        return 0;
    }

    case GL_FUNC_glProgramUniform2i: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLint v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        glProgramUniform2i(program, location, v0, v1);
        return 0;
    }

    case GL_FUNC_glProgramUniform2f: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLfloat v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLfloat v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        glProgramUniform2f(program, location, v0, v1);
        return 0;
    }

    case GL_FUNC_glProgramUniform2d: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLdouble v0;
        if (payload_size < 16) return -1;
        memcpy(&v0, payload + 8, 8);
        GLdouble v1;
        if (payload_size < 24) return -1;
        memcpy(&v1, payload + 16, 8);
        glProgramUniform2d(program, location, v0, v1);
        return 0;
    }

    case GL_FUNC_glProgramUniform2ui: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLuint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLuint v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        glProgramUniform2ui(program, location, v0, v1);
        return 0;
    }

    case GL_FUNC_glProgramUniform3i: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLint v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        GLint v2;
        if (payload_size < 20) return -1;
        memcpy(&v2, payload + 16, 4);
        glProgramUniform3i(program, location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glProgramUniform3f: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLfloat v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLfloat v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        GLfloat v2;
        if (payload_size < 20) return -1;
        memcpy(&v2, payload + 16, 4);
        glProgramUniform3f(program, location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glProgramUniform3d: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLdouble v0;
        if (payload_size < 16) return -1;
        memcpy(&v0, payload + 8, 8);
        GLdouble v1;
        if (payload_size < 24) return -1;
        memcpy(&v1, payload + 16, 8);
        GLdouble v2;
        if (payload_size < 32) return -1;
        memcpy(&v2, payload + 24, 8);
        glProgramUniform3d(program, location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glProgramUniform3ui: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLuint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLuint v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        GLuint v2;
        if (payload_size < 20) return -1;
        memcpy(&v2, payload + 16, 4);
        glProgramUniform3ui(program, location, v0, v1, v2);
        return 0;
    }

    case GL_FUNC_glProgramUniform4i: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLint v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        GLint v2;
        if (payload_size < 20) return -1;
        memcpy(&v2, payload + 16, 4);
        GLint v3;
        if (payload_size < 24) return -1;
        memcpy(&v3, payload + 20, 4);
        glProgramUniform4i(program, location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glProgramUniform4f: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLfloat v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLfloat v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        GLfloat v2;
        if (payload_size < 20) return -1;
        memcpy(&v2, payload + 16, 4);
        GLfloat v3;
        if (payload_size < 24) return -1;
        memcpy(&v3, payload + 20, 4);
        glProgramUniform4f(program, location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glProgramUniform4d: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLdouble v0;
        if (payload_size < 16) return -1;
        memcpy(&v0, payload + 8, 8);
        GLdouble v1;
        if (payload_size < 24) return -1;
        memcpy(&v1, payload + 16, 8);
        GLdouble v2;
        if (payload_size < 32) return -1;
        memcpy(&v2, payload + 24, 8);
        GLdouble v3;
        if (payload_size < 40) return -1;
        memcpy(&v3, payload + 32, 8);
        glProgramUniform4d(program, location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glProgramUniform4ui: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLint location;
        if (payload_size < 8) return -1;
        memcpy(&location, payload + 4, 4);
        GLuint v0;
        if (payload_size < 12) return -1;
        memcpy(&v0, payload + 8, 4);
        GLuint v1;
        if (payload_size < 16) return -1;
        memcpy(&v1, payload + 12, 4);
        GLuint v2;
        if (payload_size < 20) return -1;
        memcpy(&v2, payload + 16, 4);
        GLuint v3;
        if (payload_size < 24) return -1;
        memcpy(&v3, payload + 20, 4);
        glProgramUniform4ui(program, location, v0, v1, v2, v3);
        return 0;
    }

    case GL_FUNC_glValidateProgramPipeline: {
        GLuint pipeline;
        if (payload_size < 4) return -1;
        memcpy(&pipeline, payload + 0, 4);
        glValidateProgramPipeline(pipeline);
        return 0;
    }

    case GL_FUNC_glVertexAttribL1d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        glVertexAttribL1d(index, x);
        return 0;
    }

    case GL_FUNC_glVertexAttribL2d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        glVertexAttribL2d(index, x, y);
        return 0;
    }

    case GL_FUNC_glVertexAttribL3d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        GLdouble z;
        if (payload_size < 28) return -1;
        memcpy(&z, payload + 20, 8);
        glVertexAttribL3d(index, x, y, z);
        return 0;
    }

    case GL_FUNC_glVertexAttribL4d: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble x;
        if (payload_size < 12) return -1;
        memcpy(&x, payload + 4, 8);
        GLdouble y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 12, 8);
        GLdouble z;
        if (payload_size < 28) return -1;
        memcpy(&z, payload + 20, 8);
        GLdouble w;
        if (payload_size < 36) return -1;
        memcpy(&w, payload + 28, 8);
        glVertexAttribL4d(index, x, y, z, w);
        return 0;
    }

    case GL_FUNC_glViewportIndexedf: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLfloat x;
        if (payload_size < 8) return -1;
        memcpy(&x, payload + 4, 4);
        GLfloat y;
        if (payload_size < 12) return -1;
        memcpy(&y, payload + 8, 4);
        GLfloat w;
        if (payload_size < 16) return -1;
        memcpy(&w, payload + 12, 4);
        GLfloat h;
        if (payload_size < 20) return -1;
        memcpy(&h, payload + 16, 4);
        glViewportIndexedf(index, x, y, w, h);
        return 0;
    }

    case GL_FUNC_glScissorIndexed: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLint left;
        if (payload_size < 8) return -1;
        memcpy(&left, payload + 4, 4);
        GLint bottom;
        if (payload_size < 12) return -1;
        memcpy(&bottom, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        glScissorIndexed(index, left, bottom, width, height);
        return 0;
    }

    case GL_FUNC_glDepthRangeIndexed: {
        GLuint index;
        if (payload_size < 4) return -1;
        memcpy(&index, payload + 0, 4);
        GLdouble n;
        if (payload_size < 12) return -1;
        memcpy(&n, payload + 4, 8);
        GLdouble f;
        if (payload_size < 20) return -1;
        memcpy(&f, payload + 12, 8);
        glDepthRangeIndexed(index, n, f);
        return 0;
    }

    case GL_FUNC_glDrawArraysInstancedBaseInstance: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLint first;
        if (payload_size < 8) return -1;
        memcpy(&first, payload + 4, 4);
        GLsizei count;
        if (payload_size < 12) return -1;
        memcpy(&count, payload + 8, 4);
        GLsizei instancecount;
        if (payload_size < 16) return -1;
        memcpy(&instancecount, payload + 12, 4);
        GLuint baseinstance;
        if (payload_size < 20) return -1;
        memcpy(&baseinstance, payload + 16, 4);
        glDrawArraysInstancedBaseInstance(mode, first, count, instancecount, baseinstance);
        return 0;
    }

    case GL_FUNC_glBindImageTexture: {
        GLuint unit;
        if (payload_size < 4) return -1;
        memcpy(&unit, payload + 0, 4);
        GLuint texture;
        if (payload_size < 8) return -1;
        memcpy(&texture, payload + 4, 4);
        GLint level;
        if (payload_size < 12) return -1;
        memcpy(&level, payload + 8, 4);
        GLboolean layered;
        if (payload_size < 13) return -1;
        memcpy(&layered, payload + 12, 1);
        GLint layer;
        if (payload_size < 17) return -1;
        memcpy(&layer, payload + 13, 4);
        GLenum access;
        if (payload_size < 21) return -1;
        memcpy(&access, payload + 17, 4);
        GLenum format;
        if (payload_size < 25) return -1;
        memcpy(&format, payload + 21, 4);
        glBindImageTexture(unit, texture, level, layered, layer, access, format);
        return 0;
    }

    case GL_FUNC_glMemoryBarrier: {
        GLbitfield barriers;
        if (payload_size < 4) return -1;
        memcpy(&barriers, payload + 0, 4);
        glMemoryBarrier(barriers);
        return 0;
    }

    case GL_FUNC_glTexStorage1D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei levels;
        if (payload_size < 8) return -1;
        memcpy(&levels, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        glTexStorage1D(target, levels, internalformat, width);
        return 0;
    }

    case GL_FUNC_glTexStorage2D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei levels;
        if (payload_size < 8) return -1;
        memcpy(&levels, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        glTexStorage2D(target, levels, internalformat, width, height);
        return 0;
    }

    case GL_FUNC_glTexStorage3D: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei levels;
        if (payload_size < 8) return -1;
        memcpy(&levels, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLsizei depth;
        if (payload_size < 24) return -1;
        memcpy(&depth, payload + 20, 4);
        glTexStorage3D(target, levels, internalformat, width, height, depth);
        return 0;
    }

    case GL_FUNC_glDrawTransformFeedbackInstanced: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLuint id;
        if (payload_size < 8) return -1;
        memcpy(&id, payload + 4, 4);
        GLsizei instancecount;
        if (payload_size < 12) return -1;
        memcpy(&instancecount, payload + 8, 4);
        glDrawTransformFeedbackInstanced(mode, id, instancecount);
        return 0;
    }

    case GL_FUNC_glDrawTransformFeedbackStreamInstanced: {
        GLenum mode;
        if (payload_size < 4) return -1;
        memcpy(&mode, payload + 0, 4);
        GLuint id;
        if (payload_size < 8) return -1;
        memcpy(&id, payload + 4, 4);
        GLuint stream;
        if (payload_size < 12) return -1;
        memcpy(&stream, payload + 8, 4);
        GLsizei instancecount;
        if (payload_size < 16) return -1;
        memcpy(&instancecount, payload + 12, 4);
        glDrawTransformFeedbackStreamInstanced(mode, id, stream, instancecount);
        return 0;
    }

    case GL_FUNC_glDispatchCompute: {
        GLuint num_groups_x;
        if (payload_size < 4) return -1;
        memcpy(&num_groups_x, payload + 0, 4);
        GLuint num_groups_y;
        if (payload_size < 8) return -1;
        memcpy(&num_groups_y, payload + 4, 4);
        GLuint num_groups_z;
        if (payload_size < 12) return -1;
        memcpy(&num_groups_z, payload + 8, 4);
        glDispatchCompute(num_groups_x, num_groups_y, num_groups_z);
        return 0;
    }

    case GL_FUNC_glDispatchComputeIndirect: {
        GLintptr indirect;
        if (payload_size < 8) return -1;
        memcpy(&indirect, payload + 0, 8);
        glDispatchComputeIndirect(indirect);
        return 0;
    }

    case GL_FUNC_glCopyImageSubData: {
        GLuint srcName;
        if (payload_size < 4) return -1;
        memcpy(&srcName, payload + 0, 4);
        GLenum srcTarget;
        if (payload_size < 8) return -1;
        memcpy(&srcTarget, payload + 4, 4);
        GLint srcLevel;
        if (payload_size < 12) return -1;
        memcpy(&srcLevel, payload + 8, 4);
        GLint srcX;
        if (payload_size < 16) return -1;
        memcpy(&srcX, payload + 12, 4);
        GLint srcY;
        if (payload_size < 20) return -1;
        memcpy(&srcY, payload + 16, 4);
        GLint srcZ;
        if (payload_size < 24) return -1;
        memcpy(&srcZ, payload + 20, 4);
        GLuint dstName;
        if (payload_size < 28) return -1;
        memcpy(&dstName, payload + 24, 4);
        GLenum dstTarget;
        if (payload_size < 32) return -1;
        memcpy(&dstTarget, payload + 28, 4);
        GLint dstLevel;
        if (payload_size < 36) return -1;
        memcpy(&dstLevel, payload + 32, 4);
        GLint dstX;
        if (payload_size < 40) return -1;
        memcpy(&dstX, payload + 36, 4);
        GLint dstY;
        if (payload_size < 44) return -1;
        memcpy(&dstY, payload + 40, 4);
        GLint dstZ;
        if (payload_size < 48) return -1;
        memcpy(&dstZ, payload + 44, 4);
        GLsizei srcWidth;
        if (payload_size < 52) return -1;
        memcpy(&srcWidth, payload + 48, 4);
        GLsizei srcHeight;
        if (payload_size < 56) return -1;
        memcpy(&srcHeight, payload + 52, 4);
        GLsizei srcDepth;
        if (payload_size < 60) return -1;
        memcpy(&srcDepth, payload + 56, 4);
        glCopyImageSubData(srcName, srcTarget, srcLevel, srcX, srcY, srcZ, dstName, dstTarget, dstLevel, dstX, dstY, dstZ, srcWidth, srcHeight, srcDepth);
        return 0;
    }

    case GL_FUNC_glFramebufferParameteri: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glFramebufferParameteri(target, pname, param);
        return 0;
    }

    case GL_FUNC_glInvalidateTexSubImage: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint yoffset;
        if (payload_size < 16) return -1;
        memcpy(&yoffset, payload + 12, 4);
        GLint zoffset;
        if (payload_size < 20) return -1;
        memcpy(&zoffset, payload + 16, 4);
        GLsizei width;
        if (payload_size < 24) return -1;
        memcpy(&width, payload + 20, 4);
        GLsizei height;
        if (payload_size < 28) return -1;
        memcpy(&height, payload + 24, 4);
        GLsizei depth;
        if (payload_size < 32) return -1;
        memcpy(&depth, payload + 28, 4);
        glInvalidateTexSubImage(texture, level, xoffset, yoffset, zoffset, width, height, depth);
        return 0;
    }

    case GL_FUNC_glInvalidateTexImage: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        glInvalidateTexImage(texture, level);
        return 0;
    }

    case GL_FUNC_glInvalidateBufferSubData: {
        GLuint buffer;
        if (payload_size < 4) return -1;
        memcpy(&buffer, payload + 0, 4);
        GLintptr offset;
        if (payload_size < 12) return -1;
        memcpy(&offset, payload + 4, 8);
        GLsizeiptr length;
        if (payload_size < 20) return -1;
        memcpy(&length, payload + 12, 8);
        glInvalidateBufferSubData(buffer, offset, length);
        return 0;
    }

    case GL_FUNC_glInvalidateBufferData: {
        GLuint buffer;
        if (payload_size < 4) return -1;
        memcpy(&buffer, payload + 0, 4);
        glInvalidateBufferData(buffer);
        return 0;
    }

    case GL_FUNC_glShaderStorageBlockBinding: {
        GLuint program;
        if (payload_size < 4) return -1;
        memcpy(&program, payload + 0, 4);
        GLuint storageBlockIndex;
        if (payload_size < 8) return -1;
        memcpy(&storageBlockIndex, payload + 4, 4);
        GLuint storageBlockBinding;
        if (payload_size < 12) return -1;
        memcpy(&storageBlockBinding, payload + 8, 4);
        glShaderStorageBlockBinding(program, storageBlockIndex, storageBlockBinding);
        return 0;
    }

    case GL_FUNC_glTexBufferRange: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLenum internalformat;
        if (payload_size < 8) return -1;
        memcpy(&internalformat, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        GLsizeiptr size;
        if (payload_size < 28) return -1;
        memcpy(&size, payload + 20, 8);
        glTexBufferRange(target, internalformat, buffer, offset, size);
        return 0;
    }

    case GL_FUNC_glTexStorage2DMultisample: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLboolean fixedsamplelocations;
        if (payload_size < 21) return -1;
        memcpy(&fixedsamplelocations, payload + 20, 1);
        glTexStorage2DMultisample(target, samples, internalformat, width, height, fixedsamplelocations);
        return 0;
    }

    case GL_FUNC_glTexStorage3DMultisample: {
        GLenum target;
        if (payload_size < 4) return -1;
        memcpy(&target, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLsizei depth;
        if (payload_size < 24) return -1;
        memcpy(&depth, payload + 20, 4);
        GLboolean fixedsamplelocations;
        if (payload_size < 25) return -1;
        memcpy(&fixedsamplelocations, payload + 24, 1);
        glTexStorage3DMultisample(target, samples, internalformat, width, height, depth, fixedsamplelocations);
        return 0;
    }

    case GL_FUNC_glTextureView: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum target;
        if (payload_size < 8) return -1;
        memcpy(&target, payload + 4, 4);
        GLuint origtexture;
        if (payload_size < 12) return -1;
        memcpy(&origtexture, payload + 8, 4);
        GLenum internalformat;
        if (payload_size < 16) return -1;
        memcpy(&internalformat, payload + 12, 4);
        GLuint minlevel;
        if (payload_size < 20) return -1;
        memcpy(&minlevel, payload + 16, 4);
        GLuint numlevels;
        if (payload_size < 24) return -1;
        memcpy(&numlevels, payload + 20, 4);
        GLuint minlayer;
        if (payload_size < 28) return -1;
        memcpy(&minlayer, payload + 24, 4);
        GLuint numlayers;
        if (payload_size < 32) return -1;
        memcpy(&numlayers, payload + 28, 4);
        glTextureView(texture, target, origtexture, internalformat, minlevel, numlevels, minlayer, numlayers);
        return 0;
    }

    case GL_FUNC_glBindVertexBuffer: {
        GLuint bindingindex;
        if (payload_size < 4) return -1;
        memcpy(&bindingindex, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        GLintptr offset;
        if (payload_size < 16) return -1;
        memcpy(&offset, payload + 8, 8);
        GLsizei stride;
        if (payload_size < 20) return -1;
        memcpy(&stride, payload + 16, 4);
        glBindVertexBuffer(bindingindex, buffer, offset, stride);
        return 0;
    }

    case GL_FUNC_glVertexAttribFormat: {
        GLuint attribindex;
        if (payload_size < 4) return -1;
        memcpy(&attribindex, payload + 0, 4);
        GLint size;
        if (payload_size < 8) return -1;
        memcpy(&size, payload + 4, 4);
        GLenum type;
        if (payload_size < 12) return -1;
        memcpy(&type, payload + 8, 4);
        GLboolean normalized;
        if (payload_size < 13) return -1;
        memcpy(&normalized, payload + 12, 1);
        GLuint relativeoffset;
        if (payload_size < 17) return -1;
        memcpy(&relativeoffset, payload + 13, 4);
        glVertexAttribFormat(attribindex, size, type, normalized, relativeoffset);
        return 0;
    }

    case GL_FUNC_glVertexAttribIFormat: {
        GLuint attribindex;
        if (payload_size < 4) return -1;
        memcpy(&attribindex, payload + 0, 4);
        GLint size;
        if (payload_size < 8) return -1;
        memcpy(&size, payload + 4, 4);
        GLenum type;
        if (payload_size < 12) return -1;
        memcpy(&type, payload + 8, 4);
        GLuint relativeoffset;
        if (payload_size < 16) return -1;
        memcpy(&relativeoffset, payload + 12, 4);
        glVertexAttribIFormat(attribindex, size, type, relativeoffset);
        return 0;
    }

    case GL_FUNC_glVertexAttribLFormat: {
        GLuint attribindex;
        if (payload_size < 4) return -1;
        memcpy(&attribindex, payload + 0, 4);
        GLint size;
        if (payload_size < 8) return -1;
        memcpy(&size, payload + 4, 4);
        GLenum type;
        if (payload_size < 12) return -1;
        memcpy(&type, payload + 8, 4);
        GLuint relativeoffset;
        if (payload_size < 16) return -1;
        memcpy(&relativeoffset, payload + 12, 4);
        glVertexAttribLFormat(attribindex, size, type, relativeoffset);
        return 0;
    }

    case GL_FUNC_glVertexAttribBinding: {
        GLuint attribindex;
        if (payload_size < 4) return -1;
        memcpy(&attribindex, payload + 0, 4);
        GLuint bindingindex;
        if (payload_size < 8) return -1;
        memcpy(&bindingindex, payload + 4, 4);
        glVertexAttribBinding(attribindex, bindingindex);
        return 0;
    }

    case GL_FUNC_glVertexBindingDivisor: {
        GLuint bindingindex;
        if (payload_size < 4) return -1;
        memcpy(&bindingindex, payload + 0, 4);
        GLuint divisor;
        if (payload_size < 8) return -1;
        memcpy(&divisor, payload + 4, 4);
        glVertexBindingDivisor(bindingindex, divisor);
        return 0;
    }

    case GL_FUNC_glPopDebugGroup: {
        glPopDebugGroup();
        return 0;
    }

    case GL_FUNC_glClipControl: {
        GLenum origin;
        if (payload_size < 4) return -1;
        memcpy(&origin, payload + 0, 4);
        GLenum depth;
        if (payload_size < 8) return -1;
        memcpy(&depth, payload + 4, 4);
        glClipControl(origin, depth);
        return 0;
    }

    case GL_FUNC_glTransformFeedbackBufferBase: {
        GLuint xfb;
        if (payload_size < 4) return -1;
        memcpy(&xfb, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        glTransformFeedbackBufferBase(xfb, index, buffer);
        return 0;
    }

    case GL_FUNC_glTransformFeedbackBufferRange: {
        GLuint xfb;
        if (payload_size < 4) return -1;
        memcpy(&xfb, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        GLsizeiptr size;
        if (payload_size < 28) return -1;
        memcpy(&size, payload + 20, 8);
        glTransformFeedbackBufferRange(xfb, index, buffer, offset, size);
        return 0;
    }

    case GL_FUNC_glCopyNamedBufferSubData: {
        GLuint readBuffer;
        if (payload_size < 4) return -1;
        memcpy(&readBuffer, payload + 0, 4);
        GLuint writeBuffer;
        if (payload_size < 8) return -1;
        memcpy(&writeBuffer, payload + 4, 4);
        GLintptr readOffset;
        if (payload_size < 16) return -1;
        memcpy(&readOffset, payload + 8, 8);
        GLintptr writeOffset;
        if (payload_size < 24) return -1;
        memcpy(&writeOffset, payload + 16, 8);
        GLsizeiptr size;
        if (payload_size < 32) return -1;
        memcpy(&size, payload + 24, 8);
        glCopyNamedBufferSubData(readBuffer, writeBuffer, readOffset, writeOffset, size);
        return 0;
    }

    case GL_FUNC_glFlushMappedNamedBufferRange: {
        GLuint buffer;
        if (payload_size < 4) return -1;
        memcpy(&buffer, payload + 0, 4);
        GLintptr offset;
        if (payload_size < 12) return -1;
        memcpy(&offset, payload + 4, 8);
        GLsizeiptr length;
        if (payload_size < 20) return -1;
        memcpy(&length, payload + 12, 8);
        glFlushMappedNamedBufferRange(buffer, offset, length);
        return 0;
    }

    case GL_FUNC_glNamedFramebufferRenderbuffer: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLenum renderbuffertarget;
        if (payload_size < 12) return -1;
        memcpy(&renderbuffertarget, payload + 8, 4);
        GLuint renderbuffer;
        if (payload_size < 16) return -1;
        memcpy(&renderbuffer, payload + 12, 4);
        glNamedFramebufferRenderbuffer(framebuffer, attachment, renderbuffertarget, renderbuffer);
        return 0;
    }

    case GL_FUNC_glNamedFramebufferParameteri: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glNamedFramebufferParameteri(framebuffer, pname, param);
        return 0;
    }

    case GL_FUNC_glNamedFramebufferTexture: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLuint texture;
        if (payload_size < 12) return -1;
        memcpy(&texture, payload + 8, 4);
        GLint level;
        if (payload_size < 16) return -1;
        memcpy(&level, payload + 12, 4);
        glNamedFramebufferTexture(framebuffer, attachment, texture, level);
        return 0;
    }

    case GL_FUNC_glNamedFramebufferTextureLayer: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum attachment;
        if (payload_size < 8) return -1;
        memcpy(&attachment, payload + 4, 4);
        GLuint texture;
        if (payload_size < 12) return -1;
        memcpy(&texture, payload + 8, 4);
        GLint level;
        if (payload_size < 16) return -1;
        memcpy(&level, payload + 12, 4);
        GLint layer;
        if (payload_size < 20) return -1;
        memcpy(&layer, payload + 16, 4);
        glNamedFramebufferTextureLayer(framebuffer, attachment, texture, level, layer);
        return 0;
    }

    case GL_FUNC_glNamedFramebufferDrawBuffer: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum buf;
        if (payload_size < 8) return -1;
        memcpy(&buf, payload + 4, 4);
        glNamedFramebufferDrawBuffer(framebuffer, buf);
        return 0;
    }

    case GL_FUNC_glNamedFramebufferReadBuffer: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum src;
        if (payload_size < 8) return -1;
        memcpy(&src, payload + 4, 4);
        glNamedFramebufferReadBuffer(framebuffer, src);
        return 0;
    }

    case GL_FUNC_glClearNamedFramebufferfi: {
        GLuint framebuffer;
        if (payload_size < 4) return -1;
        memcpy(&framebuffer, payload + 0, 4);
        GLenum buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        GLint drawbuffer;
        if (payload_size < 12) return -1;
        memcpy(&drawbuffer, payload + 8, 4);
        GLfloat depth;
        if (payload_size < 16) return -1;
        memcpy(&depth, payload + 12, 4);
        GLint stencil;
        if (payload_size < 20) return -1;
        memcpy(&stencil, payload + 16, 4);
        glClearNamedFramebufferfi(framebuffer, buffer, drawbuffer, depth, stencil);
        return 0;
    }

    case GL_FUNC_glBlitNamedFramebuffer: {
        GLuint readFramebuffer;
        if (payload_size < 4) return -1;
        memcpy(&readFramebuffer, payload + 0, 4);
        GLuint drawFramebuffer;
        if (payload_size < 8) return -1;
        memcpy(&drawFramebuffer, payload + 4, 4);
        GLint srcX0;
        if (payload_size < 12) return -1;
        memcpy(&srcX0, payload + 8, 4);
        GLint srcY0;
        if (payload_size < 16) return -1;
        memcpy(&srcY0, payload + 12, 4);
        GLint srcX1;
        if (payload_size < 20) return -1;
        memcpy(&srcX1, payload + 16, 4);
        GLint srcY1;
        if (payload_size < 24) return -1;
        memcpy(&srcY1, payload + 20, 4);
        GLint dstX0;
        if (payload_size < 28) return -1;
        memcpy(&dstX0, payload + 24, 4);
        GLint dstY0;
        if (payload_size < 32) return -1;
        memcpy(&dstY0, payload + 28, 4);
        GLint dstX1;
        if (payload_size < 36) return -1;
        memcpy(&dstX1, payload + 32, 4);
        GLint dstY1;
        if (payload_size < 40) return -1;
        memcpy(&dstY1, payload + 36, 4);
        GLbitfield mask;
        if (payload_size < 44) return -1;
        memcpy(&mask, payload + 40, 4);
        GLenum filter;
        if (payload_size < 48) return -1;
        memcpy(&filter, payload + 44, 4);
        glBlitNamedFramebuffer(readFramebuffer, drawFramebuffer, srcX0, srcY0, srcX1, srcY1, dstX0, dstY0, dstX1, dstY1, mask, filter);
        return 0;
    }

    case GL_FUNC_glNamedRenderbufferStorage: {
        GLuint renderbuffer;
        if (payload_size < 4) return -1;
        memcpy(&renderbuffer, payload + 0, 4);
        GLenum internalformat;
        if (payload_size < 8) return -1;
        memcpy(&internalformat, payload + 4, 4);
        GLsizei width;
        if (payload_size < 12) return -1;
        memcpy(&width, payload + 8, 4);
        GLsizei height;
        if (payload_size < 16) return -1;
        memcpy(&height, payload + 12, 4);
        glNamedRenderbufferStorage(renderbuffer, internalformat, width, height);
        return 0;
    }

    case GL_FUNC_glNamedRenderbufferStorageMultisample: {
        GLuint renderbuffer;
        if (payload_size < 4) return -1;
        memcpy(&renderbuffer, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        glNamedRenderbufferStorageMultisample(renderbuffer, samples, internalformat, width, height);
        return 0;
    }

    case GL_FUNC_glTextureBuffer: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum internalformat;
        if (payload_size < 8) return -1;
        memcpy(&internalformat, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        glTextureBuffer(texture, internalformat, buffer);
        return 0;
    }

    case GL_FUNC_glTextureBufferRange: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum internalformat;
        if (payload_size < 8) return -1;
        memcpy(&internalformat, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        GLsizeiptr size;
        if (payload_size < 28) return -1;
        memcpy(&size, payload + 20, 8);
        glTextureBufferRange(texture, internalformat, buffer, offset, size);
        return 0;
    }

    case GL_FUNC_glTextureStorage1D: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLsizei levels;
        if (payload_size < 8) return -1;
        memcpy(&levels, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        glTextureStorage1D(texture, levels, internalformat, width);
        return 0;
    }

    case GL_FUNC_glTextureStorage2D: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLsizei levels;
        if (payload_size < 8) return -1;
        memcpy(&levels, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        glTextureStorage2D(texture, levels, internalformat, width, height);
        return 0;
    }

    case GL_FUNC_glTextureStorage3D: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLsizei levels;
        if (payload_size < 8) return -1;
        memcpy(&levels, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLsizei depth;
        if (payload_size < 24) return -1;
        memcpy(&depth, payload + 20, 4);
        glTextureStorage3D(texture, levels, internalformat, width, height, depth);
        return 0;
    }

    case GL_FUNC_glTextureStorage2DMultisample: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLboolean fixedsamplelocations;
        if (payload_size < 21) return -1;
        memcpy(&fixedsamplelocations, payload + 20, 1);
        glTextureStorage2DMultisample(texture, samples, internalformat, width, height, fixedsamplelocations);
        return 0;
    }

    case GL_FUNC_glTextureStorage3DMultisample: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLsizei samples;
        if (payload_size < 8) return -1;
        memcpy(&samples, payload + 4, 4);
        GLenum internalformat;
        if (payload_size < 12) return -1;
        memcpy(&internalformat, payload + 8, 4);
        GLsizei width;
        if (payload_size < 16) return -1;
        memcpy(&width, payload + 12, 4);
        GLsizei height;
        if (payload_size < 20) return -1;
        memcpy(&height, payload + 16, 4);
        GLsizei depth;
        if (payload_size < 24) return -1;
        memcpy(&depth, payload + 20, 4);
        GLboolean fixedsamplelocations;
        if (payload_size < 25) return -1;
        memcpy(&fixedsamplelocations, payload + 24, 1);
        glTextureStorage3DMultisample(texture, samples, internalformat, width, height, depth, fixedsamplelocations);
        return 0;
    }

    case GL_FUNC_glCopyTextureSubImage1D: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint x;
        if (payload_size < 16) return -1;
        memcpy(&x, payload + 12, 4);
        GLint y;
        if (payload_size < 20) return -1;
        memcpy(&y, payload + 16, 4);
        GLsizei width;
        if (payload_size < 24) return -1;
        memcpy(&width, payload + 20, 4);
        glCopyTextureSubImage1D(texture, level, xoffset, x, y, width);
        return 0;
    }

    case GL_FUNC_glCopyTextureSubImage2D: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint yoffset;
        if (payload_size < 16) return -1;
        memcpy(&yoffset, payload + 12, 4);
        GLint x;
        if (payload_size < 20) return -1;
        memcpy(&x, payload + 16, 4);
        GLint y;
        if (payload_size < 24) return -1;
        memcpy(&y, payload + 20, 4);
        GLsizei width;
        if (payload_size < 28) return -1;
        memcpy(&width, payload + 24, 4);
        GLsizei height;
        if (payload_size < 32) return -1;
        memcpy(&height, payload + 28, 4);
        glCopyTextureSubImage2D(texture, level, xoffset, yoffset, x, y, width, height);
        return 0;
    }

    case GL_FUNC_glCopyTextureSubImage3D: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLint level;
        if (payload_size < 8) return -1;
        memcpy(&level, payload + 4, 4);
        GLint xoffset;
        if (payload_size < 12) return -1;
        memcpy(&xoffset, payload + 8, 4);
        GLint yoffset;
        if (payload_size < 16) return -1;
        memcpy(&yoffset, payload + 12, 4);
        GLint zoffset;
        if (payload_size < 20) return -1;
        memcpy(&zoffset, payload + 16, 4);
        GLint x;
        if (payload_size < 24) return -1;
        memcpy(&x, payload + 20, 4);
        GLint y;
        if (payload_size < 28) return -1;
        memcpy(&y, payload + 24, 4);
        GLsizei width;
        if (payload_size < 32) return -1;
        memcpy(&width, payload + 28, 4);
        GLsizei height;
        if (payload_size < 36) return -1;
        memcpy(&height, payload + 32, 4);
        glCopyTextureSubImage3D(texture, level, xoffset, yoffset, zoffset, x, y, width, height);
        return 0;
    }

    case GL_FUNC_glTextureParameterf: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLfloat param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTextureParameterf(texture, pname, param);
        return 0;
    }

    case GL_FUNC_glTextureParameteri: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        GLenum pname;
        if (payload_size < 8) return -1;
        memcpy(&pname, payload + 4, 4);
        GLint param;
        if (payload_size < 12) return -1;
        memcpy(&param, payload + 8, 4);
        glTextureParameteri(texture, pname, param);
        return 0;
    }

    case GL_FUNC_glGenerateTextureMipmap: {
        GLuint texture;
        if (payload_size < 4) return -1;
        memcpy(&texture, payload + 0, 4);
        glGenerateTextureMipmap(texture);
        return 0;
    }

    case GL_FUNC_glBindTextureUnit: {
        GLuint unit;
        if (payload_size < 4) return -1;
        memcpy(&unit, payload + 0, 4);
        GLuint texture;
        if (payload_size < 8) return -1;
        memcpy(&texture, payload + 4, 4);
        glBindTextureUnit(unit, texture);
        return 0;
    }

    case GL_FUNC_glDisableVertexArrayAttrib: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        glDisableVertexArrayAttrib(vaobj, index);
        return 0;
    }

    case GL_FUNC_glEnableVertexArrayAttrib: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint index;
        if (payload_size < 8) return -1;
        memcpy(&index, payload + 4, 4);
        glEnableVertexArrayAttrib(vaobj, index);
        return 0;
    }

    case GL_FUNC_glVertexArrayElementBuffer: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        glVertexArrayElementBuffer(vaobj, buffer);
        return 0;
    }

    case GL_FUNC_glVertexArrayVertexBuffer: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint bindingindex;
        if (payload_size < 8) return -1;
        memcpy(&bindingindex, payload + 4, 4);
        GLuint buffer;
        if (payload_size < 12) return -1;
        memcpy(&buffer, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        GLsizei stride;
        if (payload_size < 24) return -1;
        memcpy(&stride, payload + 20, 4);
        glVertexArrayVertexBuffer(vaobj, bindingindex, buffer, offset, stride);
        return 0;
    }

    case GL_FUNC_glVertexArrayAttribBinding: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint attribindex;
        if (payload_size < 8) return -1;
        memcpy(&attribindex, payload + 4, 4);
        GLuint bindingindex;
        if (payload_size < 12) return -1;
        memcpy(&bindingindex, payload + 8, 4);
        glVertexArrayAttribBinding(vaobj, attribindex, bindingindex);
        return 0;
    }

    case GL_FUNC_glVertexArrayAttribFormat: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint attribindex;
        if (payload_size < 8) return -1;
        memcpy(&attribindex, payload + 4, 4);
        GLint size;
        if (payload_size < 12) return -1;
        memcpy(&size, payload + 8, 4);
        GLenum type;
        if (payload_size < 16) return -1;
        memcpy(&type, payload + 12, 4);
        GLboolean normalized;
        if (payload_size < 17) return -1;
        memcpy(&normalized, payload + 16, 1);
        GLuint relativeoffset;
        if (payload_size < 21) return -1;
        memcpy(&relativeoffset, payload + 17, 4);
        glVertexArrayAttribFormat(vaobj, attribindex, size, type, normalized, relativeoffset);
        return 0;
    }

    case GL_FUNC_glVertexArrayAttribIFormat: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint attribindex;
        if (payload_size < 8) return -1;
        memcpy(&attribindex, payload + 4, 4);
        GLint size;
        if (payload_size < 12) return -1;
        memcpy(&size, payload + 8, 4);
        GLenum type;
        if (payload_size < 16) return -1;
        memcpy(&type, payload + 12, 4);
        GLuint relativeoffset;
        if (payload_size < 20) return -1;
        memcpy(&relativeoffset, payload + 16, 4);
        glVertexArrayAttribIFormat(vaobj, attribindex, size, type, relativeoffset);
        return 0;
    }

    case GL_FUNC_glVertexArrayAttribLFormat: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint attribindex;
        if (payload_size < 8) return -1;
        memcpy(&attribindex, payload + 4, 4);
        GLint size;
        if (payload_size < 12) return -1;
        memcpy(&size, payload + 8, 4);
        GLenum type;
        if (payload_size < 16) return -1;
        memcpy(&type, payload + 12, 4);
        GLuint relativeoffset;
        if (payload_size < 20) return -1;
        memcpy(&relativeoffset, payload + 16, 4);
        glVertexArrayAttribLFormat(vaobj, attribindex, size, type, relativeoffset);
        return 0;
    }

    case GL_FUNC_glVertexArrayBindingDivisor: {
        GLuint vaobj;
        if (payload_size < 4) return -1;
        memcpy(&vaobj, payload + 0, 4);
        GLuint bindingindex;
        if (payload_size < 8) return -1;
        memcpy(&bindingindex, payload + 4, 4);
        GLuint divisor;
        if (payload_size < 12) return -1;
        memcpy(&divisor, payload + 8, 4);
        glVertexArrayBindingDivisor(vaobj, bindingindex, divisor);
        return 0;
    }

    case GL_FUNC_glGetQueryBufferObjecti64v: {
        GLuint id;
        if (payload_size < 4) return -1;
        memcpy(&id, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        GLenum pname;
        if (payload_size < 12) return -1;
        memcpy(&pname, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        glGetQueryBufferObjecti64v(id, buffer, pname, offset);
        return 0;
    }

    case GL_FUNC_glGetQueryBufferObjectiv: {
        GLuint id;
        if (payload_size < 4) return -1;
        memcpy(&id, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        GLenum pname;
        if (payload_size < 12) return -1;
        memcpy(&pname, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        glGetQueryBufferObjectiv(id, buffer, pname, offset);
        return 0;
    }

    case GL_FUNC_glGetQueryBufferObjectui64v: {
        GLuint id;
        if (payload_size < 4) return -1;
        memcpy(&id, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        GLenum pname;
        if (payload_size < 12) return -1;
        memcpy(&pname, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        glGetQueryBufferObjectui64v(id, buffer, pname, offset);
        return 0;
    }

    case GL_FUNC_glGetQueryBufferObjectuiv: {
        GLuint id;
        if (payload_size < 4) return -1;
        memcpy(&id, payload + 0, 4);
        GLuint buffer;
        if (payload_size < 8) return -1;
        memcpy(&buffer, payload + 4, 4);
        GLenum pname;
        if (payload_size < 12) return -1;
        memcpy(&pname, payload + 8, 4);
        GLintptr offset;
        if (payload_size < 20) return -1;
        memcpy(&offset, payload + 12, 8);
        glGetQueryBufferObjectuiv(id, buffer, pname, offset);
        return 0;
    }

    case GL_FUNC_glMemoryBarrierByRegion: {
        GLbitfield barriers;
        if (payload_size < 4) return -1;
        memcpy(&barriers, payload + 0, 4);
        glMemoryBarrierByRegion(barriers);
        return 0;
    }

    case GL_FUNC_glTextureBarrier: {
        glTextureBarrier();
        return 0;
    }

    case GL_FUNC_glPolygonOffsetClamp: {
        GLfloat factor;
        if (payload_size < 4) return -1;
        memcpy(&factor, payload + 0, 4);
        GLfloat units;
        if (payload_size < 8) return -1;
        memcpy(&units, payload + 4, 4);
        GLfloat clamp;
        if (payload_size < 12) return -1;
        memcpy(&clamp, payload + 8, 4);
        glPolygonOffsetClamp(factor, units, clamp);
        return 0;
    }

    /* -----------------------------------------------------------
     *  B — Synchronous commands (return value)
     * ----------------------------------------------------------- */

    case GL_FUNC_glGetError: {
        GLenum result = glGetError();
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsEnabled: {
        GLenum cap;
        if (rpc_read(conn, &cap, sizeof(GLenum)) < 0) return -1;
        GLboolean result = glIsEnabled(cap);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glGenLists: {
        GLsizei range;
        if (rpc_read(conn, &range, sizeof(GLsizei)) < 0) return -1;
        GLuint result = glGenLists(range);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glRenderMode: {
        GLenum mode;
        if (rpc_read(conn, &mode, sizeof(GLenum)) < 0) return -1;
        GLint result = glRenderMode(mode);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsList: {
        GLuint list;
        if (rpc_read(conn, &list, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsList(list);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsTexture: {
        GLuint texture;
        if (rpc_read(conn, &texture, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsTexture(texture);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsQuery: {
        GLuint id;
        if (rpc_read(conn, &id, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsQuery(id);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsBuffer: {
        GLuint buffer;
        if (rpc_read(conn, &buffer, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsBuffer(buffer);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glCreateProgram: {
        GLuint result = glCreateProgram();
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glCreateShader: {
        GLenum type;
        if (rpc_read(conn, &type, sizeof(GLenum)) < 0) return -1;
        GLuint result = glCreateShader(type);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsProgram: {
        GLuint program;
        if (rpc_read(conn, &program, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsProgram(program);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsShader: {
        GLuint shader;
        if (rpc_read(conn, &shader, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsShader(shader);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsEnabledi: {
        GLenum target;
        if (rpc_read(conn, &target, sizeof(GLenum)) < 0) return -1;
        GLuint index;
        if (rpc_read(conn, &index, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsEnabledi(target, index);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsRenderbuffer: {
        GLuint renderbuffer;
        if (rpc_read(conn, &renderbuffer, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsRenderbuffer(renderbuffer);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsFramebuffer: {
        GLuint framebuffer;
        if (rpc_read(conn, &framebuffer, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsFramebuffer(framebuffer);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glCheckFramebufferStatus: {
        GLenum target;
        if (rpc_read(conn, &target, sizeof(GLenum)) < 0) return -1;
        GLenum result = glCheckFramebufferStatus(target);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsVertexArray: {
        GLuint array;
        if (rpc_read(conn, &array, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsVertexArray(array);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsSampler: {
        GLuint sampler;
        if (rpc_read(conn, &sampler, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsSampler(sampler);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsTransformFeedback: {
        GLuint id;
        if (rpc_read(conn, &id, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsTransformFeedback(id);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glIsProgramPipeline: {
        GLuint pipeline;
        if (rpc_read(conn, &pipeline, sizeof(GLuint)) < 0) return -1;
        GLboolean result = glIsProgramPipeline(pipeline);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glCheckNamedFramebufferStatus: {
        GLuint framebuffer;
        if (rpc_read(conn, &framebuffer, sizeof(GLuint)) < 0) return -1;
        GLenum target;
        if (rpc_read(conn, &target, sizeof(GLenum)) < 0) return -1;
        GLenum result = glCheckNamedFramebufferStatus(framebuffer, target);
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    case GL_FUNC_glGetGraphicsResetStatus: {
        GLenum result = glGetGraphicsResetStatus();
        if (rpc_write(conn, &result, sizeof(result)) < 0 ||
            rpc_write_end(conn) < 0) return -1;
        return 0;
    }

    /* -----------------------------------------------------------
     *  C — Special / pointer-heavy commands (manual)
     * ----------------------------------------------------------- */

    case GL_FUNC_glTexParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexParameterfv_manual(conn);

    case GL_FUNC_glTexParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexParameteriv_manual(conn);

    case GL_FUNC_glTexImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexImage1D_manual(conn);

    case GL_FUNC_glTexImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexImage2D_manual(conn);

    case GL_FUNC_glReadPixels:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glReadPixels_manual(conn);

    case GL_FUNC_glGetBooleanv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetBooleanv_manual(conn);

    case GL_FUNC_glGetDoublev:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetDoublev_manual(conn);

    case GL_FUNC_glGetFloatv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetFloatv_manual(conn);

    case GL_FUNC_glGetIntegerv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetIntegerv_manual(conn);

    case GL_FUNC_glGetString:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetString_manual(conn);

    case GL_FUNC_glGetTexImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexImage_manual(conn);

    case GL_FUNC_glGetTexParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexParameterfv_manual(conn);

    case GL_FUNC_glGetTexParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexParameteriv_manual(conn);

    case GL_FUNC_glGetTexLevelParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexLevelParameterfv_manual(conn);

    case GL_FUNC_glGetTexLevelParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexLevelParameteriv_manual(conn);

    case GL_FUNC_glCallLists:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCallLists_manual(conn);

    case GL_FUNC_glBitmap:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBitmap_manual(conn);

    case GL_FUNC_glColor3bv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3bv_manual(conn);

    case GL_FUNC_glColor3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3dv_manual(conn);

    case GL_FUNC_glColor3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3fv_manual(conn);

    case GL_FUNC_glColor3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3iv_manual(conn);

    case GL_FUNC_glColor3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3sv_manual(conn);

    case GL_FUNC_glColor3ubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3ubv_manual(conn);

    case GL_FUNC_glColor3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3uiv_manual(conn);

    case GL_FUNC_glColor3usv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor3usv_manual(conn);

    case GL_FUNC_glColor4bv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4bv_manual(conn);

    case GL_FUNC_glColor4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4dv_manual(conn);

    case GL_FUNC_glColor4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4fv_manual(conn);

    case GL_FUNC_glColor4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4iv_manual(conn);

    case GL_FUNC_glColor4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4sv_manual(conn);

    case GL_FUNC_glColor4ubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4ubv_manual(conn);

    case GL_FUNC_glColor4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4uiv_manual(conn);

    case GL_FUNC_glColor4usv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColor4usv_manual(conn);

    case GL_FUNC_glEdgeFlagv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glEdgeFlagv_manual(conn);

    case GL_FUNC_glIndexdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIndexdv_manual(conn);

    case GL_FUNC_glIndexfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIndexfv_manual(conn);

    case GL_FUNC_glIndexiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIndexiv_manual(conn);

    case GL_FUNC_glIndexsv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIndexsv_manual(conn);

    case GL_FUNC_glNormal3bv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormal3bv_manual(conn);

    case GL_FUNC_glNormal3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormal3dv_manual(conn);

    case GL_FUNC_glNormal3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormal3fv_manual(conn);

    case GL_FUNC_glNormal3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormal3iv_manual(conn);

    case GL_FUNC_glNormal3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormal3sv_manual(conn);

    case GL_FUNC_glRasterPos2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos2dv_manual(conn);

    case GL_FUNC_glRasterPos2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos2fv_manual(conn);

    case GL_FUNC_glRasterPos2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos2iv_manual(conn);

    case GL_FUNC_glRasterPos2sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos2sv_manual(conn);

    case GL_FUNC_glRasterPos3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos3dv_manual(conn);

    case GL_FUNC_glRasterPos3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos3fv_manual(conn);

    case GL_FUNC_glRasterPos3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos3iv_manual(conn);

    case GL_FUNC_glRasterPos3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos3sv_manual(conn);

    case GL_FUNC_glRasterPos4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos4dv_manual(conn);

    case GL_FUNC_glRasterPos4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos4fv_manual(conn);

    case GL_FUNC_glRasterPos4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos4iv_manual(conn);

    case GL_FUNC_glRasterPos4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRasterPos4sv_manual(conn);

    case GL_FUNC_glRectdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRectdv_manual(conn);

    case GL_FUNC_glRectfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRectfv_manual(conn);

    case GL_FUNC_glRectiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRectiv_manual(conn);

    case GL_FUNC_glRectsv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glRectsv_manual(conn);

    case GL_FUNC_glTexCoord1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord1dv_manual(conn);

    case GL_FUNC_glTexCoord1fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord1fv_manual(conn);

    case GL_FUNC_glTexCoord1iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord1iv_manual(conn);

    case GL_FUNC_glTexCoord1sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord1sv_manual(conn);

    case GL_FUNC_glTexCoord2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord2dv_manual(conn);

    case GL_FUNC_glTexCoord2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord2fv_manual(conn);

    case GL_FUNC_glTexCoord2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord2iv_manual(conn);

    case GL_FUNC_glTexCoord2sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord2sv_manual(conn);

    case GL_FUNC_glTexCoord3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord3dv_manual(conn);

    case GL_FUNC_glTexCoord3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord3fv_manual(conn);

    case GL_FUNC_glTexCoord3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord3iv_manual(conn);

    case GL_FUNC_glTexCoord3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord3sv_manual(conn);

    case GL_FUNC_glTexCoord4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord4dv_manual(conn);

    case GL_FUNC_glTexCoord4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord4fv_manual(conn);

    case GL_FUNC_glTexCoord4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord4iv_manual(conn);

    case GL_FUNC_glTexCoord4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoord4sv_manual(conn);

    case GL_FUNC_glVertex2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex2dv_manual(conn);

    case GL_FUNC_glVertex2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex2fv_manual(conn);

    case GL_FUNC_glVertex2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex2iv_manual(conn);

    case GL_FUNC_glVertex2sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex2sv_manual(conn);

    case GL_FUNC_glVertex3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex3dv_manual(conn);

    case GL_FUNC_glVertex3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex3fv_manual(conn);

    case GL_FUNC_glVertex3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex3iv_manual(conn);

    case GL_FUNC_glVertex3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex3sv_manual(conn);

    case GL_FUNC_glVertex4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex4dv_manual(conn);

    case GL_FUNC_glVertex4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex4fv_manual(conn);

    case GL_FUNC_glVertex4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex4iv_manual(conn);

    case GL_FUNC_glVertex4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertex4sv_manual(conn);

    case GL_FUNC_glClipPlane:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClipPlane_manual(conn);

    case GL_FUNC_glFogfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFogfv_manual(conn);

    case GL_FUNC_glFogiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFogiv_manual(conn);

    case GL_FUNC_glLightfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLightfv_manual(conn);

    case GL_FUNC_glLightiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLightiv_manual(conn);

    case GL_FUNC_glLightModelfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLightModelfv_manual(conn);

    case GL_FUNC_glLightModeliv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLightModeliv_manual(conn);

    case GL_FUNC_glMaterialfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMaterialfv_manual(conn);

    case GL_FUNC_glMaterialiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMaterialiv_manual(conn);

    case GL_FUNC_glPolygonStipple:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPolygonStipple_manual(conn);

    case GL_FUNC_glTexEnvfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexEnvfv_manual(conn);

    case GL_FUNC_glTexEnviv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexEnviv_manual(conn);

    case GL_FUNC_glTexGendv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexGendv_manual(conn);

    case GL_FUNC_glTexGenfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexGenfv_manual(conn);

    case GL_FUNC_glTexGeniv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexGeniv_manual(conn);

    case GL_FUNC_glFeedbackBuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFeedbackBuffer_manual(conn);

    case GL_FUNC_glSelectBuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSelectBuffer_manual(conn);

    case GL_FUNC_glMap1d:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMap1d_manual(conn);

    case GL_FUNC_glMap1f:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMap1f_manual(conn);

    case GL_FUNC_glMap2d:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMap2d_manual(conn);

    case GL_FUNC_glMap2f:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMap2f_manual(conn);

    case GL_FUNC_glEvalCoord1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glEvalCoord1dv_manual(conn);

    case GL_FUNC_glEvalCoord1fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glEvalCoord1fv_manual(conn);

    case GL_FUNC_glEvalCoord2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glEvalCoord2dv_manual(conn);

    case GL_FUNC_glEvalCoord2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glEvalCoord2fv_manual(conn);

    case GL_FUNC_glPixelMapfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPixelMapfv_manual(conn);

    case GL_FUNC_glPixelMapuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPixelMapuiv_manual(conn);

    case GL_FUNC_glPixelMapusv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPixelMapusv_manual(conn);

    case GL_FUNC_glDrawPixels:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawPixels_manual(conn);

    case GL_FUNC_glGetClipPlane:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetClipPlane_manual(conn);

    case GL_FUNC_glGetLightfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetLightfv_manual(conn);

    case GL_FUNC_glGetLightiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetLightiv_manual(conn);

    case GL_FUNC_glGetMapdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetMapdv_manual(conn);

    case GL_FUNC_glGetMapfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetMapfv_manual(conn);

    case GL_FUNC_glGetMapiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetMapiv_manual(conn);

    case GL_FUNC_glGetMaterialfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetMaterialfv_manual(conn);

    case GL_FUNC_glGetMaterialiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetMaterialiv_manual(conn);

    case GL_FUNC_glGetPixelMapfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetPixelMapfv_manual(conn);

    case GL_FUNC_glGetPixelMapuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetPixelMapuiv_manual(conn);

    case GL_FUNC_glGetPixelMapusv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetPixelMapusv_manual(conn);

    case GL_FUNC_glGetPolygonStipple:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetPolygonStipple_manual(conn);

    case GL_FUNC_glGetTexEnvfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexEnvfv_manual(conn);

    case GL_FUNC_glGetTexEnviv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexEnviv_manual(conn);

    case GL_FUNC_glGetTexGendv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexGendv_manual(conn);

    case GL_FUNC_glGetTexGenfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexGenfv_manual(conn);

    case GL_FUNC_glGetTexGeniv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexGeniv_manual(conn);

    case GL_FUNC_glLoadMatrixf:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLoadMatrixf_manual(conn);

    case GL_FUNC_glLoadMatrixd:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLoadMatrixd_manual(conn);

    case GL_FUNC_glMultMatrixf:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultMatrixf_manual(conn);

    case GL_FUNC_glMultMatrixd:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultMatrixd_manual(conn);

    case GL_FUNC_glDrawElements:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElements_manual(conn);

    case GL_FUNC_glGetPointerv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetPointerv_manual(conn);

    case GL_FUNC_glCopyTexImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCopyTexImage1D_manual(conn);

    case GL_FUNC_glCopyTexImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCopyTexImage2D_manual(conn);

    case GL_FUNC_glTexSubImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexSubImage1D_manual(conn);

    case GL_FUNC_glTexSubImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexSubImage2D_manual(conn);

    case GL_FUNC_glDeleteTextures:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteTextures_manual(conn);

    case GL_FUNC_glGenTextures:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenTextures_manual(conn);

    case GL_FUNC_glColorPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColorPointer_manual(conn);

    case GL_FUNC_glEdgeFlagPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glEdgeFlagPointer_manual(conn);

    case GL_FUNC_glIndexPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIndexPointer_manual(conn);

    case GL_FUNC_glInterleavedArrays:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glInterleavedArrays_manual(conn);

    case GL_FUNC_glNormalPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormalPointer_manual(conn);

    case GL_FUNC_glTexCoordPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoordPointer_manual(conn);

    case GL_FUNC_glVertexPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexPointer_manual(conn);

    case GL_FUNC_glAreTexturesResident:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glAreTexturesResident_manual(conn);

    case GL_FUNC_glPrioritizeTextures:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPrioritizeTextures_manual(conn);

    case GL_FUNC_glIndexubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIndexubv_manual(conn);

    case GL_FUNC_glDrawRangeElements:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawRangeElements_manual(conn);

    case GL_FUNC_glTexImage3D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexImage3D_manual(conn);

    case GL_FUNC_glTexSubImage3D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexSubImage3D_manual(conn);

    case GL_FUNC_glCompressedTexImage3D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTexImage3D_manual(conn);

    case GL_FUNC_glCompressedTexImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTexImage2D_manual(conn);

    case GL_FUNC_glCompressedTexImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTexImage1D_manual(conn);

    case GL_FUNC_glCompressedTexSubImage3D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTexSubImage3D_manual(conn);

    case GL_FUNC_glCompressedTexSubImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTexSubImage2D_manual(conn);

    case GL_FUNC_glCompressedTexSubImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTexSubImage1D_manual(conn);

    case GL_FUNC_glGetCompressedTexImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetCompressedTexImage_manual(conn);

    case GL_FUNC_glMultiTexCoord1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord1dv_manual(conn);

    case GL_FUNC_glMultiTexCoord1fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord1fv_manual(conn);

    case GL_FUNC_glMultiTexCoord1iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord1iv_manual(conn);

    case GL_FUNC_glMultiTexCoord1sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord1sv_manual(conn);

    case GL_FUNC_glMultiTexCoord2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord2dv_manual(conn);

    case GL_FUNC_glMultiTexCoord2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord2fv_manual(conn);

    case GL_FUNC_glMultiTexCoord2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord2iv_manual(conn);

    case GL_FUNC_glMultiTexCoord2sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord2sv_manual(conn);

    case GL_FUNC_glMultiTexCoord3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord3dv_manual(conn);

    case GL_FUNC_glMultiTexCoord3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord3fv_manual(conn);

    case GL_FUNC_glMultiTexCoord3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord3iv_manual(conn);

    case GL_FUNC_glMultiTexCoord3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord3sv_manual(conn);

    case GL_FUNC_glMultiTexCoord4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord4dv_manual(conn);

    case GL_FUNC_glMultiTexCoord4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord4fv_manual(conn);

    case GL_FUNC_glMultiTexCoord4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord4iv_manual(conn);

    case GL_FUNC_glMultiTexCoord4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoord4sv_manual(conn);

    case GL_FUNC_glLoadTransposeMatrixf:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLoadTransposeMatrixf_manual(conn);

    case GL_FUNC_glLoadTransposeMatrixd:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glLoadTransposeMatrixd_manual(conn);

    case GL_FUNC_glMultTransposeMatrixf:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultTransposeMatrixf_manual(conn);

    case GL_FUNC_glMultTransposeMatrixd:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultTransposeMatrixd_manual(conn);

    case GL_FUNC_glMultiDrawArrays:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawArrays_manual(conn);

    case GL_FUNC_glMultiDrawElements:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawElements_manual(conn);

    case GL_FUNC_glPointParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPointParameterfv_manual(conn);

    case GL_FUNC_glPointParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPointParameteriv_manual(conn);

    case GL_FUNC_glFogCoordfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFogCoordfv_manual(conn);

    case GL_FUNC_glFogCoorddv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFogCoorddv_manual(conn);

    case GL_FUNC_glFogCoordPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFogCoordPointer_manual(conn);

    case GL_FUNC_glSecondaryColor3bv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3bv_manual(conn);

    case GL_FUNC_glSecondaryColor3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3dv_manual(conn);

    case GL_FUNC_glSecondaryColor3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3fv_manual(conn);

    case GL_FUNC_glSecondaryColor3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3iv_manual(conn);

    case GL_FUNC_glSecondaryColor3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3sv_manual(conn);

    case GL_FUNC_glSecondaryColor3ubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3ubv_manual(conn);

    case GL_FUNC_glSecondaryColor3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3uiv_manual(conn);

    case GL_FUNC_glSecondaryColor3usv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColor3usv_manual(conn);

    case GL_FUNC_glSecondaryColorPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColorPointer_manual(conn);

    case GL_FUNC_glWindowPos2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos2dv_manual(conn);

    case GL_FUNC_glWindowPos2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos2fv_manual(conn);

    case GL_FUNC_glWindowPos2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos2iv_manual(conn);

    case GL_FUNC_glWindowPos2sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos2sv_manual(conn);

    case GL_FUNC_glWindowPos3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos3dv_manual(conn);

    case GL_FUNC_glWindowPos3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos3fv_manual(conn);

    case GL_FUNC_glWindowPos3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos3iv_manual(conn);

    case GL_FUNC_glWindowPos3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWindowPos3sv_manual(conn);

    case GL_FUNC_glGenQueries:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenQueries_manual(conn);

    case GL_FUNC_glDeleteQueries:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteQueries_manual(conn);

    case GL_FUNC_glGetQueryiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetQueryiv_manual(conn);

    case GL_FUNC_glGetQueryObjectiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetQueryObjectiv_manual(conn);

    case GL_FUNC_glGetQueryObjectuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetQueryObjectuiv_manual(conn);

    case GL_FUNC_glDeleteBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteBuffers_manual(conn);

    case GL_FUNC_glGenBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenBuffers_manual(conn);

    case GL_FUNC_glBufferData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBufferData_manual(conn);

    case GL_FUNC_glBufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBufferSubData_manual(conn);

    case GL_FUNC_glGetBufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetBufferSubData_manual(conn);

    case GL_FUNC_glMapBuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMapBuffer_manual(conn);

    case GL_FUNC_glUnmapBuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUnmapBuffer_manual(conn);

    case GL_FUNC_glGetBufferParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetBufferParameteriv_manual(conn);

    case GL_FUNC_glGetBufferPointerv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetBufferPointerv_manual(conn);

    case GL_FUNC_glDrawBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawBuffers_manual(conn);

    case GL_FUNC_glBindAttribLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindAttribLocation_manual(conn);

    case GL_FUNC_glGetActiveAttrib:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveAttrib_manual(conn);

    case GL_FUNC_glGetActiveUniform:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveUniform_manual(conn);

    case GL_FUNC_glGetAttachedShaders:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetAttachedShaders_manual(conn);

    case GL_FUNC_glGetAttribLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetAttribLocation_manual(conn);

    case GL_FUNC_glGetProgramiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramiv_manual(conn);

    case GL_FUNC_glGetProgramInfoLog:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramInfoLog_manual(conn);

    case GL_FUNC_glGetShaderiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetShaderiv_manual(conn);

    case GL_FUNC_glGetShaderInfoLog:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetShaderInfoLog_manual(conn);

    case GL_FUNC_glGetShaderSource:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetShaderSource_manual(conn);

    case GL_FUNC_glGetUniformLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformLocation_manual(conn);

    case GL_FUNC_glGetUniformfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformfv_manual(conn);

    case GL_FUNC_glGetUniformiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformiv_manual(conn);

    case GL_FUNC_glGetVertexAttribdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribdv_manual(conn);

    case GL_FUNC_glGetVertexAttribfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribfv_manual(conn);

    case GL_FUNC_glGetVertexAttribiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribiv_manual(conn);

    case GL_FUNC_glGetVertexAttribPointerv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribPointerv_manual(conn);

    case GL_FUNC_glShaderSource:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glShaderSource_manual(conn);

    case GL_FUNC_glUniform1fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform1fv_manual(conn);

    case GL_FUNC_glUniform2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform2fv_manual(conn);

    case GL_FUNC_glUniform3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform3fv_manual(conn);

    case GL_FUNC_glUniform4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform4fv_manual(conn);

    case GL_FUNC_glUniform1iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform1iv_manual(conn);

    case GL_FUNC_glUniform2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform2iv_manual(conn);

    case GL_FUNC_glUniform3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform3iv_manual(conn);

    case GL_FUNC_glUniform4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform4iv_manual(conn);

    case GL_FUNC_glUniformMatrix2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix2fv_manual(conn);

    case GL_FUNC_glUniformMatrix3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix3fv_manual(conn);

    case GL_FUNC_glUniformMatrix4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix4fv_manual(conn);

    case GL_FUNC_glVertexAttrib1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib1dv_manual(conn);

    case GL_FUNC_glVertexAttrib1fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib1fv_manual(conn);

    case GL_FUNC_glVertexAttrib1sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib1sv_manual(conn);

    case GL_FUNC_glVertexAttrib2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib2dv_manual(conn);

    case GL_FUNC_glVertexAttrib2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib2fv_manual(conn);

    case GL_FUNC_glVertexAttrib2sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib2sv_manual(conn);

    case GL_FUNC_glVertexAttrib3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib3dv_manual(conn);

    case GL_FUNC_glVertexAttrib3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib3fv_manual(conn);

    case GL_FUNC_glVertexAttrib3sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib3sv_manual(conn);

    case GL_FUNC_glVertexAttrib4Nbv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4Nbv_manual(conn);

    case GL_FUNC_glVertexAttrib4Niv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4Niv_manual(conn);

    case GL_FUNC_glVertexAttrib4Nsv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4Nsv_manual(conn);

    case GL_FUNC_glVertexAttrib4Nubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4Nubv_manual(conn);

    case GL_FUNC_glVertexAttrib4Nuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4Nuiv_manual(conn);

    case GL_FUNC_glVertexAttrib4Nusv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4Nusv_manual(conn);

    case GL_FUNC_glVertexAttrib4bv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4bv_manual(conn);

    case GL_FUNC_glVertexAttrib4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4dv_manual(conn);

    case GL_FUNC_glVertexAttrib4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4fv_manual(conn);

    case GL_FUNC_glVertexAttrib4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4iv_manual(conn);

    case GL_FUNC_glVertexAttrib4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4sv_manual(conn);

    case GL_FUNC_glVertexAttrib4ubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4ubv_manual(conn);

    case GL_FUNC_glVertexAttrib4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4uiv_manual(conn);

    case GL_FUNC_glVertexAttrib4usv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttrib4usv_manual(conn);

    case GL_FUNC_glVertexAttribPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribPointer_manual(conn);

    case GL_FUNC_glUniformMatrix2x3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix2x3fv_manual(conn);

    case GL_FUNC_glUniformMatrix3x2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix3x2fv_manual(conn);

    case GL_FUNC_glUniformMatrix2x4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix2x4fv_manual(conn);

    case GL_FUNC_glUniformMatrix4x2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix4x2fv_manual(conn);

    case GL_FUNC_glUniformMatrix3x4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix3x4fv_manual(conn);

    case GL_FUNC_glUniformMatrix4x3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix4x3fv_manual(conn);

    case GL_FUNC_glGetBooleani_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetBooleani_v_manual(conn);

    case GL_FUNC_glGetIntegeri_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetIntegeri_v_manual(conn);

    case GL_FUNC_glTransformFeedbackVaryings:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTransformFeedbackVaryings_manual(conn);

    case GL_FUNC_glGetTransformFeedbackVarying:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTransformFeedbackVarying_manual(conn);

    case GL_FUNC_glVertexAttribIPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribIPointer_manual(conn);

    case GL_FUNC_glGetVertexAttribIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribIiv_manual(conn);

    case GL_FUNC_glGetVertexAttribIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribIuiv_manual(conn);

    case GL_FUNC_glVertexAttribI1iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI1iv_manual(conn);

    case GL_FUNC_glVertexAttribI2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI2iv_manual(conn);

    case GL_FUNC_glVertexAttribI3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI3iv_manual(conn);

    case GL_FUNC_glVertexAttribI4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI4iv_manual(conn);

    case GL_FUNC_glVertexAttribI1uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI1uiv_manual(conn);

    case GL_FUNC_glVertexAttribI2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI2uiv_manual(conn);

    case GL_FUNC_glVertexAttribI3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI3uiv_manual(conn);

    case GL_FUNC_glVertexAttribI4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI4uiv_manual(conn);

    case GL_FUNC_glVertexAttribI4bv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI4bv_manual(conn);

    case GL_FUNC_glVertexAttribI4sv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI4sv_manual(conn);

    case GL_FUNC_glVertexAttribI4ubv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI4ubv_manual(conn);

    case GL_FUNC_glVertexAttribI4usv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribI4usv_manual(conn);

    case GL_FUNC_glGetUniformuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformuiv_manual(conn);

    case GL_FUNC_glBindFragDataLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindFragDataLocation_manual(conn);

    case GL_FUNC_glGetFragDataLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetFragDataLocation_manual(conn);

    case GL_FUNC_glUniform1uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform1uiv_manual(conn);

    case GL_FUNC_glUniform2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform2uiv_manual(conn);

    case GL_FUNC_glUniform3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform3uiv_manual(conn);

    case GL_FUNC_glUniform4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform4uiv_manual(conn);

    case GL_FUNC_glTexParameterIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexParameterIiv_manual(conn);

    case GL_FUNC_glTexParameterIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexParameterIuiv_manual(conn);

    case GL_FUNC_glGetTexParameterIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexParameterIiv_manual(conn);

    case GL_FUNC_glGetTexParameterIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTexParameterIuiv_manual(conn);

    case GL_FUNC_glClearBufferiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearBufferiv_manual(conn);

    case GL_FUNC_glClearBufferuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearBufferuiv_manual(conn);

    case GL_FUNC_glClearBufferfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearBufferfv_manual(conn);

    case GL_FUNC_glGetStringi:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetStringi_manual(conn);

    case GL_FUNC_glDeleteRenderbuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteRenderbuffers_manual(conn);

    case GL_FUNC_glGenRenderbuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenRenderbuffers_manual(conn);

    case GL_FUNC_glGetRenderbufferParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetRenderbufferParameteriv_manual(conn);

    case GL_FUNC_glDeleteFramebuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteFramebuffers_manual(conn);

    case GL_FUNC_glGenFramebuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenFramebuffers_manual(conn);

    case GL_FUNC_glGetFramebufferAttachmentParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetFramebufferAttachmentParameteriv_manual(conn);

    case GL_FUNC_glMapBufferRange:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMapBufferRange_manual(conn);

    case GL_FUNC_glDeleteVertexArrays:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteVertexArrays_manual(conn);

    case GL_FUNC_glGenVertexArrays:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenVertexArrays_manual(conn);

    case GL_FUNC_glDrawElementsInstanced:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElementsInstanced_manual(conn);

    case GL_FUNC_glGetUniformIndices:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformIndices_manual(conn);

    case GL_FUNC_glGetActiveUniformsiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveUniformsiv_manual(conn);

    case GL_FUNC_glGetActiveUniformName:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveUniformName_manual(conn);

    case GL_FUNC_glGetUniformBlockIndex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformBlockIndex_manual(conn);

    case GL_FUNC_glGetActiveUniformBlockiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveUniformBlockiv_manual(conn);

    case GL_FUNC_glGetActiveUniformBlockName:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveUniformBlockName_manual(conn);

    case GL_FUNC_glDrawElementsBaseVertex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElementsBaseVertex_manual(conn);

    case GL_FUNC_glDrawRangeElementsBaseVertex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawRangeElementsBaseVertex_manual(conn);

    case GL_FUNC_glDrawElementsInstancedBaseVertex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElementsInstancedBaseVertex_manual(conn);

    case GL_FUNC_glMultiDrawElementsBaseVertex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawElementsBaseVertex_manual(conn);

    case GL_FUNC_glFenceSync:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glFenceSync_manual(conn);

    case GL_FUNC_glIsSync:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glIsSync_manual(conn);

    case GL_FUNC_glDeleteSync:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteSync_manual(conn);

    case GL_FUNC_glClientWaitSync:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClientWaitSync_manual(conn);

    case GL_FUNC_glWaitSync:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glWaitSync_manual(conn);

    case GL_FUNC_glGetInteger64v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetInteger64v_manual(conn);

    case GL_FUNC_glGetSynciv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSynciv_manual(conn);

    case GL_FUNC_glGetInteger64i_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetInteger64i_v_manual(conn);

    case GL_FUNC_glGetBufferParameteri64v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetBufferParameteri64v_manual(conn);

    case GL_FUNC_glGetMultisamplefv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetMultisamplefv_manual(conn);

    case GL_FUNC_glBindFragDataLocationIndexed:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindFragDataLocationIndexed_manual(conn);

    case GL_FUNC_glGetFragDataIndex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetFragDataIndex_manual(conn);

    case GL_FUNC_glGenSamplers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenSamplers_manual(conn);

    case GL_FUNC_glDeleteSamplers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteSamplers_manual(conn);

    case GL_FUNC_glSamplerParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSamplerParameteriv_manual(conn);

    case GL_FUNC_glSamplerParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSamplerParameterfv_manual(conn);

    case GL_FUNC_glSamplerParameterIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSamplerParameterIiv_manual(conn);

    case GL_FUNC_glSamplerParameterIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSamplerParameterIuiv_manual(conn);

    case GL_FUNC_glGetSamplerParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSamplerParameteriv_manual(conn);

    case GL_FUNC_glGetSamplerParameterIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSamplerParameterIiv_manual(conn);

    case GL_FUNC_glGetSamplerParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSamplerParameterfv_manual(conn);

    case GL_FUNC_glGetSamplerParameterIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSamplerParameterIuiv_manual(conn);

    case GL_FUNC_glGetQueryObjecti64v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetQueryObjecti64v_manual(conn);

    case GL_FUNC_glGetQueryObjectui64v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetQueryObjectui64v_manual(conn);

    case GL_FUNC_glVertexAttribP1uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribP1uiv_manual(conn);

    case GL_FUNC_glVertexAttribP2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribP2uiv_manual(conn);

    case GL_FUNC_glVertexAttribP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribP3uiv_manual(conn);

    case GL_FUNC_glVertexAttribP4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribP4uiv_manual(conn);

    case GL_FUNC_glVertexP2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexP2uiv_manual(conn);

    case GL_FUNC_glVertexP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexP3uiv_manual(conn);

    case GL_FUNC_glVertexP4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexP4uiv_manual(conn);

    case GL_FUNC_glTexCoordP1uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoordP1uiv_manual(conn);

    case GL_FUNC_glTexCoordP2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoordP2uiv_manual(conn);

    case GL_FUNC_glTexCoordP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoordP3uiv_manual(conn);

    case GL_FUNC_glTexCoordP4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTexCoordP4uiv_manual(conn);

    case GL_FUNC_glMultiTexCoordP1uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoordP1uiv_manual(conn);

    case GL_FUNC_glMultiTexCoordP2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoordP2uiv_manual(conn);

    case GL_FUNC_glMultiTexCoordP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoordP3uiv_manual(conn);

    case GL_FUNC_glMultiTexCoordP4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiTexCoordP4uiv_manual(conn);

    case GL_FUNC_glNormalP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNormalP3uiv_manual(conn);

    case GL_FUNC_glColorP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColorP3uiv_manual(conn);

    case GL_FUNC_glColorP4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glColorP4uiv_manual(conn);

    case GL_FUNC_glSecondaryColorP3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSecondaryColorP3uiv_manual(conn);

    case GL_FUNC_glDrawArraysIndirect:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawArraysIndirect_manual(conn);

    case GL_FUNC_glDrawElementsIndirect:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElementsIndirect_manual(conn);

    case GL_FUNC_glUniform1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform1dv_manual(conn);

    case GL_FUNC_glUniform2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform2dv_manual(conn);

    case GL_FUNC_glUniform3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform3dv_manual(conn);

    case GL_FUNC_glUniform4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniform4dv_manual(conn);

    case GL_FUNC_glUniformMatrix2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix2dv_manual(conn);

    case GL_FUNC_glUniformMatrix3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix3dv_manual(conn);

    case GL_FUNC_glUniformMatrix4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix4dv_manual(conn);

    case GL_FUNC_glUniformMatrix2x3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix2x3dv_manual(conn);

    case GL_FUNC_glUniformMatrix2x4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix2x4dv_manual(conn);

    case GL_FUNC_glUniformMatrix3x2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix3x2dv_manual(conn);

    case GL_FUNC_glUniformMatrix3x4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix3x4dv_manual(conn);

    case GL_FUNC_glUniformMatrix4x2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix4x2dv_manual(conn);

    case GL_FUNC_glUniformMatrix4x3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformMatrix4x3dv_manual(conn);

    case GL_FUNC_glGetUniformdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformdv_manual(conn);

    case GL_FUNC_glGetSubroutineUniformLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSubroutineUniformLocation_manual(conn);

    case GL_FUNC_glGetSubroutineIndex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetSubroutineIndex_manual(conn);

    case GL_FUNC_glGetActiveSubroutineUniformiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveSubroutineUniformiv_manual(conn);

    case GL_FUNC_glGetActiveSubroutineUniformName:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveSubroutineUniformName_manual(conn);

    case GL_FUNC_glGetActiveSubroutineName:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveSubroutineName_manual(conn);

    case GL_FUNC_glUniformSubroutinesuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUniformSubroutinesuiv_manual(conn);

    case GL_FUNC_glGetUniformSubroutineuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetUniformSubroutineuiv_manual(conn);

    case GL_FUNC_glGetProgramStageiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramStageiv_manual(conn);

    case GL_FUNC_glPatchParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPatchParameterfv_manual(conn);

    case GL_FUNC_glDeleteTransformFeedbacks:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteTransformFeedbacks_manual(conn);

    case GL_FUNC_glGenTransformFeedbacks:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenTransformFeedbacks_manual(conn);

    case GL_FUNC_glGetQueryIndexediv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetQueryIndexediv_manual(conn);

    case GL_FUNC_glShaderBinary:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glShaderBinary_manual(conn);

    case GL_FUNC_glGetShaderPrecisionFormat:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetShaderPrecisionFormat_manual(conn);

    case GL_FUNC_glGetProgramBinary:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramBinary_manual(conn);

    case GL_FUNC_glProgramBinary:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramBinary_manual(conn);

    case GL_FUNC_glCreateShaderProgramv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateShaderProgramv_manual(conn);

    case GL_FUNC_glDeleteProgramPipelines:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDeleteProgramPipelines_manual(conn);

    case GL_FUNC_glGenProgramPipelines:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGenProgramPipelines_manual(conn);

    case GL_FUNC_glGetProgramPipelineiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramPipelineiv_manual(conn);

    case GL_FUNC_glProgramUniform1iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform1iv_manual(conn);

    case GL_FUNC_glProgramUniform1fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform1fv_manual(conn);

    case GL_FUNC_glProgramUniform1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform1dv_manual(conn);

    case GL_FUNC_glProgramUniform1uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform1uiv_manual(conn);

    case GL_FUNC_glProgramUniform2iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform2iv_manual(conn);

    case GL_FUNC_glProgramUniform2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform2fv_manual(conn);

    case GL_FUNC_glProgramUniform2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform2dv_manual(conn);

    case GL_FUNC_glProgramUniform2uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform2uiv_manual(conn);

    case GL_FUNC_glProgramUniform3iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform3iv_manual(conn);

    case GL_FUNC_glProgramUniform3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform3fv_manual(conn);

    case GL_FUNC_glProgramUniform3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform3dv_manual(conn);

    case GL_FUNC_glProgramUniform3uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform3uiv_manual(conn);

    case GL_FUNC_glProgramUniform4iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform4iv_manual(conn);

    case GL_FUNC_glProgramUniform4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform4fv_manual(conn);

    case GL_FUNC_glProgramUniform4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform4dv_manual(conn);

    case GL_FUNC_glProgramUniform4uiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniform4uiv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix2fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix3fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix4fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix2dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix3dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix4dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix2x3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix2x3fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix3x2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix3x2fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix2x4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix2x4fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix4x2fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix4x2fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix3x4fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix3x4fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix4x3fv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix4x3fv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix2x3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix2x3dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix3x2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix3x2dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix2x4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix2x4dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix4x2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix4x2dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix3x4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix3x4dv_manual(conn);

    case GL_FUNC_glProgramUniformMatrix4x3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glProgramUniformMatrix4x3dv_manual(conn);

    case GL_FUNC_glGetProgramPipelineInfoLog:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramPipelineInfoLog_manual(conn);

    case GL_FUNC_glVertexAttribL1dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribL1dv_manual(conn);

    case GL_FUNC_glVertexAttribL2dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribL2dv_manual(conn);

    case GL_FUNC_glVertexAttribL3dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribL3dv_manual(conn);

    case GL_FUNC_glVertexAttribL4dv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribL4dv_manual(conn);

    case GL_FUNC_glVertexAttribLPointer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexAttribLPointer_manual(conn);

    case GL_FUNC_glGetVertexAttribLdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexAttribLdv_manual(conn);

    case GL_FUNC_glViewportArrayv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glViewportArrayv_manual(conn);

    case GL_FUNC_glViewportIndexedfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glViewportIndexedfv_manual(conn);

    case GL_FUNC_glScissorArrayv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glScissorArrayv_manual(conn);

    case GL_FUNC_glScissorIndexedv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glScissorIndexedv_manual(conn);

    case GL_FUNC_glDepthRangeArrayv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDepthRangeArrayv_manual(conn);

    case GL_FUNC_glGetFloati_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetFloati_v_manual(conn);

    case GL_FUNC_glGetDoublei_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetDoublei_v_manual(conn);

    case GL_FUNC_glDrawElementsInstancedBaseInstance:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElementsInstancedBaseInstance_manual(conn);

    case GL_FUNC_glDrawElementsInstancedBaseVertexBaseInstance:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDrawElementsInstancedBaseVertexBaseInstance_manual(conn);

    case GL_FUNC_glGetInternalformativ:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetInternalformativ_manual(conn);

    case GL_FUNC_glGetActiveAtomicCounterBufferiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetActiveAtomicCounterBufferiv_manual(conn);

    case GL_FUNC_glClearBufferData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearBufferData_manual(conn);

    case GL_FUNC_glClearBufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearBufferSubData_manual(conn);

    case GL_FUNC_glGetFramebufferParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetFramebufferParameteriv_manual(conn);

    case GL_FUNC_glGetInternalformati64v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetInternalformati64v_manual(conn);

    case GL_FUNC_glInvalidateFramebuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glInvalidateFramebuffer_manual(conn);

    case GL_FUNC_glInvalidateSubFramebuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glInvalidateSubFramebuffer_manual(conn);

    case GL_FUNC_glMultiDrawArraysIndirect:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawArraysIndirect_manual(conn);

    case GL_FUNC_glMultiDrawElementsIndirect:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawElementsIndirect_manual(conn);

    case GL_FUNC_glGetProgramInterfaceiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramInterfaceiv_manual(conn);

    case GL_FUNC_glGetProgramResourceIndex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramResourceIndex_manual(conn);

    case GL_FUNC_glGetProgramResourceName:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramResourceName_manual(conn);

    case GL_FUNC_glGetProgramResourceiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramResourceiv_manual(conn);

    case GL_FUNC_glGetProgramResourceLocation:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramResourceLocation_manual(conn);

    case GL_FUNC_glGetProgramResourceLocationIndex:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetProgramResourceLocationIndex_manual(conn);

    case GL_FUNC_glDebugMessageControl:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDebugMessageControl_manual(conn);

    case GL_FUNC_glDebugMessageInsert:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDebugMessageInsert_manual(conn);

    case GL_FUNC_glDebugMessageCallback:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glDebugMessageCallback_manual(conn);

    case GL_FUNC_glGetDebugMessageLog:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetDebugMessageLog_manual(conn);

    case GL_FUNC_glPushDebugGroup:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glPushDebugGroup_manual(conn);

    case GL_FUNC_glObjectLabel:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glObjectLabel_manual(conn);

    case GL_FUNC_glGetObjectLabel:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetObjectLabel_manual(conn);

    case GL_FUNC_glObjectPtrLabel:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glObjectPtrLabel_manual(conn);

    case GL_FUNC_glGetObjectPtrLabel:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetObjectPtrLabel_manual(conn);

    case GL_FUNC_glBufferStorage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBufferStorage_manual(conn);

    case GL_FUNC_glClearTexImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearTexImage_manual(conn);

    case GL_FUNC_glClearTexSubImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearTexSubImage_manual(conn);

    case GL_FUNC_glBindBuffersBase:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindBuffersBase_manual(conn);

    case GL_FUNC_glBindBuffersRange:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindBuffersRange_manual(conn);

    case GL_FUNC_glBindTextures:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindTextures_manual(conn);

    case GL_FUNC_glBindSamplers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindSamplers_manual(conn);

    case GL_FUNC_glBindImageTextures:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindImageTextures_manual(conn);

    case GL_FUNC_glBindVertexBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glBindVertexBuffers_manual(conn);

    case GL_FUNC_glCreateTransformFeedbacks:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateTransformFeedbacks_manual(conn);

    case GL_FUNC_glGetTransformFeedbackiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTransformFeedbackiv_manual(conn);

    case GL_FUNC_glGetTransformFeedbacki_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTransformFeedbacki_v_manual(conn);

    case GL_FUNC_glGetTransformFeedbacki64_v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTransformFeedbacki64_v_manual(conn);

    case GL_FUNC_glCreateBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateBuffers_manual(conn);

    case GL_FUNC_glNamedBufferStorage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNamedBufferStorage_manual(conn);

    case GL_FUNC_glNamedBufferData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNamedBufferData_manual(conn);

    case GL_FUNC_glNamedBufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNamedBufferSubData_manual(conn);

    case GL_FUNC_glClearNamedBufferData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearNamedBufferData_manual(conn);

    case GL_FUNC_glClearNamedBufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearNamedBufferSubData_manual(conn);

    case GL_FUNC_glMapNamedBuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMapNamedBuffer_manual(conn);

    case GL_FUNC_glMapNamedBufferRange:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMapNamedBufferRange_manual(conn);

    case GL_FUNC_glUnmapNamedBuffer:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glUnmapNamedBuffer_manual(conn);

    case GL_FUNC_glGetNamedBufferParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedBufferParameteriv_manual(conn);

    case GL_FUNC_glGetNamedBufferParameteri64v:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedBufferParameteri64v_manual(conn);

    case GL_FUNC_glGetNamedBufferPointerv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedBufferPointerv_manual(conn);

    case GL_FUNC_glGetNamedBufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedBufferSubData_manual(conn);

    case GL_FUNC_glCreateFramebuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateFramebuffers_manual(conn);

    case GL_FUNC_glNamedFramebufferDrawBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glNamedFramebufferDrawBuffers_manual(conn);

    case GL_FUNC_glInvalidateNamedFramebufferData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glInvalidateNamedFramebufferData_manual(conn);

    case GL_FUNC_glInvalidateNamedFramebufferSubData:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glInvalidateNamedFramebufferSubData_manual(conn);

    case GL_FUNC_glClearNamedFramebufferiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearNamedFramebufferiv_manual(conn);

    case GL_FUNC_glClearNamedFramebufferuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearNamedFramebufferuiv_manual(conn);

    case GL_FUNC_glClearNamedFramebufferfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glClearNamedFramebufferfv_manual(conn);

    case GL_FUNC_glGetNamedFramebufferParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedFramebufferParameteriv_manual(conn);

    case GL_FUNC_glGetNamedFramebufferAttachmentParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedFramebufferAttachmentParameteriv_manual(conn);

    case GL_FUNC_glCreateRenderbuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateRenderbuffers_manual(conn);

    case GL_FUNC_glGetNamedRenderbufferParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetNamedRenderbufferParameteriv_manual(conn);

    case GL_FUNC_glCreateTextures:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateTextures_manual(conn);

    case GL_FUNC_glTextureSubImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureSubImage1D_manual(conn);

    case GL_FUNC_glTextureSubImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureSubImage2D_manual(conn);

    case GL_FUNC_glTextureSubImage3D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureSubImage3D_manual(conn);

    case GL_FUNC_glCompressedTextureSubImage1D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTextureSubImage1D_manual(conn);

    case GL_FUNC_glCompressedTextureSubImage2D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTextureSubImage2D_manual(conn);

    case GL_FUNC_glCompressedTextureSubImage3D:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCompressedTextureSubImage3D_manual(conn);

    case GL_FUNC_glTextureParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureParameterfv_manual(conn);

    case GL_FUNC_glTextureParameterIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureParameterIiv_manual(conn);

    case GL_FUNC_glTextureParameterIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureParameterIuiv_manual(conn);

    case GL_FUNC_glTextureParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glTextureParameteriv_manual(conn);

    case GL_FUNC_glGetTextureImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureImage_manual(conn);

    case GL_FUNC_glGetCompressedTextureImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetCompressedTextureImage_manual(conn);

    case GL_FUNC_glGetTextureLevelParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureLevelParameterfv_manual(conn);

    case GL_FUNC_glGetTextureLevelParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureLevelParameteriv_manual(conn);

    case GL_FUNC_glGetTextureParameterfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureParameterfv_manual(conn);

    case GL_FUNC_glGetTextureParameterIiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureParameterIiv_manual(conn);

    case GL_FUNC_glGetTextureParameterIuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureParameterIuiv_manual(conn);

    case GL_FUNC_glGetTextureParameteriv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureParameteriv_manual(conn);

    case GL_FUNC_glCreateVertexArrays:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateVertexArrays_manual(conn);

    case GL_FUNC_glVertexArrayVertexBuffers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glVertexArrayVertexBuffers_manual(conn);

    case GL_FUNC_glGetVertexArrayiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexArrayiv_manual(conn);

    case GL_FUNC_glGetVertexArrayIndexediv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexArrayIndexediv_manual(conn);

    case GL_FUNC_glGetVertexArrayIndexed64iv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetVertexArrayIndexed64iv_manual(conn);

    case GL_FUNC_glCreateSamplers:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateSamplers_manual(conn);

    case GL_FUNC_glCreateProgramPipelines:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateProgramPipelines_manual(conn);

    case GL_FUNC_glCreateQueries:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glCreateQueries_manual(conn);

    case GL_FUNC_glGetTextureSubImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetTextureSubImage_manual(conn);

    case GL_FUNC_glGetCompressedTextureSubImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetCompressedTextureSubImage_manual(conn);

    case GL_FUNC_glGetnCompressedTexImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnCompressedTexImage_manual(conn);

    case GL_FUNC_glGetnTexImage:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnTexImage_manual(conn);

    case GL_FUNC_glGetnUniformdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnUniformdv_manual(conn);

    case GL_FUNC_glGetnUniformfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnUniformfv_manual(conn);

    case GL_FUNC_glGetnUniformiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnUniformiv_manual(conn);

    case GL_FUNC_glGetnUniformuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnUniformuiv_manual(conn);

    case GL_FUNC_glReadnPixels:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glReadnPixels_manual(conn);

    case GL_FUNC_glGetnMapdv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnMapdv_manual(conn);

    case GL_FUNC_glGetnMapfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnMapfv_manual(conn);

    case GL_FUNC_glGetnMapiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnMapiv_manual(conn);

    case GL_FUNC_glGetnPixelMapfv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnPixelMapfv_manual(conn);

    case GL_FUNC_glGetnPixelMapuiv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnPixelMapuiv_manual(conn);

    case GL_FUNC_glGetnPixelMapusv:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnPixelMapusv_manual(conn);

    case GL_FUNC_glGetnPolygonStipple:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnPolygonStipple_manual(conn);

    case GL_FUNC_glGetnColorTable:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnColorTable_manual(conn);

    case GL_FUNC_glGetnConvolutionFilter:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnConvolutionFilter_manual(conn);

    case GL_FUNC_glGetnSeparableFilter:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnSeparableFilter_manual(conn);

    case GL_FUNC_glGetnHistogram:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnHistogram_manual(conn);

    case GL_FUNC_glGetnMinmax:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glGetnMinmax_manual(conn);

    case GL_FUNC_glSpecializeShader:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glSpecializeShader_manual(conn);

    case GL_FUNC_glMultiDrawArraysIndirectCount:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawArraysIndirectCount_manual(conn);

    case GL_FUNC_glMultiDrawElementsIndirectCount:
        /* MANUAL — see manual_gl_server.cpp */
        return handle_glMultiDrawElementsIndirectCount_manual(conn);

    default:
        return -1;
    } /* switch */
}
